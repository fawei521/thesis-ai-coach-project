# 更新日志（CHANGELOG）

> **本文件是本项目唯一的版本历史**（单文件 · 日期标签 · 版本倒序）。
> 每次发版在此追加一条。`README.md` / `PROJECT_PLAN.md` / `ROADMAP.md` 只保留当前版本指针，
> 不再各自维护一份列表，避免同一个版本要手改四处。
>
> 发布包与构建记录（zip、体积、构建验证副本）由维护者侧保管，不在本包内（项目外的 `_发布包/` 目录）。

---

## 版本索引（含日期）

| 版本 | 发布日期 | 主题 | 提交 |
|---|---|---|---|
| **v1.80** | 2026-09-19 | 开题产出物接进菜单：第 21 项大纲排 `.pptx`、第 22 项补齐 `我的工作区/`（菜单 20→22 项，标签分母同改）；修 `outline_to_ppt --dry-run` 不查图片存在性（自检说通过、真跑却硬失败）与 PPT 模板里 `02-开题报告` 错目录号；consistency_check 补"命令里嵌的工作区目录也须真实存在"；START/README/QUICKSTART/proposal-guide/先读我 接线；full_e2e 640→643 项 | 见下方详情 |
| **v1.79** | 2026-09-19 | 菜单拆分解除接线门禁：`menu.py`（700 行撞顶）拆为入口 + `menu_io.py`（交互件）+ `menu_data.py`（数据统计组 12 项）+ `menu_lit.py`（文献产出组 8 项），行为逐字节不变；+4 条结构不变量（模块齐全/入口瘦身/真 import 核对 20 项且无漏接处理器/菜单可运行列全 20 项）；AGENTS 目录速查校正工具数 21→23；full_e2e 636→640 项 | 见下方详情 |
| **v1.78** | 2026-09-19 | 手机合并单文件构建链路闭环（补丁）：`build_mobile_single.py` 默认输出改到技能目录内、与 `validate.py` 检查的路径统一（旧默认按当前目录解析，从仓库根跑会把 180 KB 产物写进仓库根且永不受检）；自检的"不同步"提示给出可直接执行的修复命令；`旧语气名` 断言不再把构建产物当源码问题报；consistency_check 两处误报修正（技能脚本开关、构建产物文档引用）；Skill v1.4.1；full_e2e 632→636 项 | 见下方详情 |
| **v1.77** | 2026-09-18 | 开题产出物闭环（并行复核会话交付）：新增 pptx_writer + outline_to_ppt（大纲→真 .pptx，只排版不代写）与 opening-ppt-outline 12页模板；setup_workspace 把工作区 4→9 目录覆盖论文全流程；+21 条行为锁定钉住本会话前四轮修复；ROADMAP 幽灵脚本断言由点名禁令改为真实不变量；full_e2e 611→632 项 | 见下方详情 |
| **v1.76** | 2026-09-18 | 文档勘误：CHANGELOG 行数表述改实计值；README 行数守卫断言收紧到 ≤180；full_e2e 616→617 项 | 见下方详情 |
| **v1.75** | 2026-09-18 | 路线图挂账维护：ROADMAP 补 Friedman/polychoric/分层ω/HTMT2/回归诊断扩展/PPT 在制等真实缺口，MI 不内置落档；README 版本区收敛；full_e2e 612→616 项 | 见下方详情 |
| **v1.74** | 2026-09-18 | 论文模板接线维护：paper-outline 方法章/表3 补单样本 t 与回归残差诊断（VIF/DW/残差正态）规范，START/QUICKSTART 同步；full_e2e 608→612 项 | 见下方详情 |
| **v1.73** | 2026-09-18 | 回归残差诊断闭环：auto_stats 回归自动报 Durbin-Watson 与残差 Shapiro-Wilk；shapiro_wilk 下沉 stats/mathx 消除层间倒挂；DW 定义级＋AR(1)/白噪声黄金；full_e2e 600→608 项 | 见下方详情 |
| **v1.72** | 2026-09-18 | paired_compare 新增单样本模式（--onesample/--constant，对标称常数如 Likert 中值 3 的单样本 t/Wilcoxon/d_z/r，菜单 19 选单样本，支持分组）；300 组对 scipy 零误差；full_e2e 589→600 项 | 见下方详情 |
| **v1.71** | 2026-09-18 | 多重插补/FIML 教学指引闭环：新增 psychology/missing-imputation-guide.md（决策树、SPSS MI/PMM、AMOS FIML、R mice＋Rubin 池化公式、MNAR 敏感性、论文模板）；missing_report 拒绝 MCAR 时控制台与论文段落直接指向指南；四处文档接线；full_e2e 577→589 项 | 见下方详情 |
| **v1.70** | 2026-09-18 | 配对检验效应量与口径增强：paired_compare.py 的 Wilcoxon 新增 rank-biserial r 效应量（(W+−W−)/(W++W−)，R effectsize/JASP 同口径，600 组对 scipy 零误差），控制台/论文段落/CSV 同步；n<30 有结正态近似 p 偏乐观警示；dataio 反向计分越界硬提示；full_e2e 570→577 项 | 见下方详情 |
| **v1.69** | 2026-09-18 | 多重比较校正闭环：新增 mult_compare.py（菜单20，Bonferroni/Holm 控 FWER、BH/BY 控 FDR，支持 --ps 直给或 CSV 读 p 值列/名称列，四法同列对照并给可粘论文段落，与 R p.adjust/scipy false_discovery_control 同口径；导出 _多重比较校正.csv/_多重比较报告.txt；3000 组随机向量黄金对照 Bonferroni/Holm/BH 零误差、BY ≤4.4e-16），full_e2e 541→570 项 | 见下方详情 |
| **v1.68** | 2026-09-18 | 配对设计差异检验闭环：新增 paired_compare.py（菜单19，前后测/两条件配对样本 t、Cohen's d_z 及近似95%CI、差值 Shapiro-Wilk 正态前提、Wilcoxon 符号秩（n≤25 无结精确 p，否则结校正/连续性校正 z，SPSS 口径）；单文件宽表与两文件按编号配对（强制 --id，缺一方整对剔除计数）、--group/--level 组内配对、scales 量表均分；导出 _配对检验.csv/_配对检验报告.txt；462 组对 scipy：t 1.8e-15、Wilcoxon 精确 p 零误差、近似 z 1.3e-15），full_e2e 504→541 项 | 见下方详情 |
| **v1.67** | 2026-09-18 | 参数检验前提假设闭环：新增 assumption_check.py（菜单18，Shapiro-Wilk 正态性 W/p（Royston AS R94，3≤n≤5000，与 R/scipy 同源）＋调整偏度/超额峰度及 z＋Kline 判据；--group 逐组正态＋Brown-Forsythe 方差齐性（Levene 基于中位数）；量表均分自动反向计分；分档给 Welch/Bootstrap/非参数建议，导出 _前提假设检验.csv/_前提假设报告.txt；W 对 scipy 误差 4e-10、p<1e-8 数量级、偏度峰度 1e-14、Levene 2e-13），full_e2e 475→504 项 | 见下方详情 |
| **v1.66** | 2026-09-18 | 缺失值分析与 Little's MCAR 检验闭环：新增 missing_report.py（菜单17，总/逐题缺失率、缺失模式、成列删除完整样本量；EM 多元正态 ML 估计＋Little(1988) T_MLμ 统计量，与 R naniar::mcar_test/Enders 同口径，df=Σk_j−k；p≥.05 可成列删除、p<.05 指引多重插补/FIML；EM 与 χ² 对 numpy 参照差<2e-8，400 次模拟校准 MCAR 拒绝率 6.5%/MAR 检出 98%），full_e2e 448→475 项 | 见下方详情 |
| **v1.65** | 2026-09-18 | GB/T 7714 参考文献格式化闭环：新增 reference_formatter.py（菜单16，六类文献[J/M/D/C/N/EB-OL]顺序编码制，吃 paper_search/整理表/13列模板，作者超3人截等/et al、全角与2025姓氏口径开关、缺字段【待补】不伪造、坏输入中文rc1），full_e2e 427→448 项 | 见下方详情 |
| **v1.64** | 2026-09-18 | 全流程三轮演练健壮性闭环（ROADMAP 挂账"自行走三遍流程收 bug"）：模型图补 direct 二变量直接效应、菜单按变量数自动选型＋scales 文件缺失/题项错配在统计/清洗/项目分析/HTMT 四处统一硬失败（校验下沉 dataio 共享）＋清洗器数值参数中文校验＋效应量 16 处坏参统一 rc=1＋文献整理空文件硬失败，full_e2e 414→427 项 | 见下方详情 |
| **v1.63** | 2026-09-18 | 高频量表库再扩充：scale-library 29→39组、10→11大类（新增坚毅Grit-S、交往焦虑IAS、人际信任ITS、社会比较INCOM、错失焦虑FoMOs、自我同情SCS-SF、积极心理资本PPQ、经验性回避AAQ-II、学习投入UWES-S、学业倦怠连榕量表/MBI-SS，新开学习心理与教育情境类；版本分歧全部标注以题本为准），full_e2e 404→414 项 | 见下方详情 |
| **v1.62** | 2026-09-18 | 现代信效度指标补齐：auto_stats 信度节自动报告 McDonald's ω（单因子PAF，写入_信度分析.csv）＋validity_cr_ave 新增 HTMT 模式（原始问卷数据直算异质-单质比率，.85/.90双门槛+Bootstrap 95%CI上限<1，导出_HTMT区分效度.csv）＋反向计分等价/同因子阴性夹具/三类守卫，full_e2e 390→404 项 | 见下方详情 |
| **v1.61** | 2026-09-18 | Skill补丁集v1.4：手机能力边界修正（阶段7预处理/阶段8统计必须回电脑、阶段6后即交接提醒）＋规则优先级六层＋语气文件去人设化命名＋统计阈值标争议＋量表授权分级本地化＋进度卡更新块＋AI可生成可编辑正文草稿（强制草稿标注/风险报告/核对清单）＋合并单文件截断与完整版获取提示 | 见下方详情 |
| **v1.60** | 2026-09-18 | 测量学闭环补齐：聚合/区分效度CR/AVE工具（菜单13）＋量表库扩至29组10大类＋预试项目分析决断值CR工具（菜单14）＋样本量补独立/配对t设计＋自编量表内容效度CVI工具（菜单15）＋共同方法偏差程序控制与ULMC/标记变量法 | 见下方详情 |
| **v1.59** | 2026-09-18 | 数据隐私与统计严谨性：去标识化隐私闸（菜单11）＋多元异常值Mahalanobis只标记不删除＋量表库扩至24组（新增8组+BSMAS）＋第4个网页范例（问答式统计方法选择器）＋效应量换算复核工具（菜单12） | 见下方详情 |
| **v1.58** | 2026-09-17 | 定位为 AI 工具/学习伙伴（去老师化、稳定陪伴不越界）＋鼓励自然化＋多词检索与≥90篇候选池＋重点文献卡片网页＋手机原生适配（Skill v1.3） | 见下方详情 |
| **v1.57** | 2026-09-17 | 文献检索闭环：知网链路实测入册＋整理器吃中文CSV（UTF-8/GBK）＋凭据红线回归锁死＋测试与打包自身缺陷修复 | 见下方详情 |
| **v1.56.6** | 2026-09-17 | 专业文档接线：psychology三份加使用约定指针（伦理危机/统计数据P0）＋行为自测补盲T33-T38并做可追溯走查 | 见下方详情 |
| **v1.56.5** | 2026-09-17 | 测试加固：补 Welch ANOVA 强异方差数值回归断言，锁死可翻转结论的 df2 缺陷（224→225 项） | 见下方详情 |
| **v1.56.4** | 2026-09-17 | 反馈规则接线：10份workflow接入带教约定指针（规则单源不复制）＋鼓励四理论补规范出处＋文献防假空白 | 见下方详情 |
| **v1.56.3** | 2026-09-17 | 系统测试缺陷修复：人口学调节列/列名友好报错/文献检索重试换源/模型图系数口径与弧线/样本量入参校验 | 见下方详情 |
| **v1.56.2** | 2026-09-17 | 本体口径硬化：人格挫折协议一致＋反攀比回归＋规则单源 | 见下方详情 |
| **v1.56.1** | 2026-09-17 | doubao-skill 口径追平完整版＋行为规范纠错＋两处门禁修复 | 见下方详情 |
| **v1.56** | 2026-09-17 | 本体反馈协议+鼓励系统（默认可关）+行为自测，学习成熟技能范式 | 见下方详情 |
| **v1.55** | 2026-09-17 | 豆包 Skill 轻量版入包（doubao-skill/）：协议化引导+鼓励系统+自检门禁 | 见下方详情 |
| **v1.54** | 2026-09-17 | 学生自己做网页（引导手册+预览器+3个范例）+ 治理补齐 | 见下方详情 |
| **v1.53.1** | 2026-09-17 | 管道编码崩溃修复+auto_stats拆包+版本日志单文件化 | 见下方详情 |
| **v1.53** | 2026-09-17 | 问卷模板人口学适配中学生/混合样本 | `4139e3b` |
| **v1.52** | 2026-09-17 | stats-guide PROCESS域名纠错+网络分析稳定性 | `792b3b9` |
| **v1.51** | 2026-09-17 | 未成年人研究伦理补强 | `d2fffa9` |
| **v1.50** | 2026-09-17 | 数据库批量下载红线+归类/结果顺序 | `95ca9ed` |
| **v1.49** | 2026-09-17 | 工具链插件职责纠错 | `4bb30e2` |
| **v1.48** | 2026-09-17 | 一键全量回归固化 | `b386a9e` |
| **v1.47** | 2026-09-17 | 量表库硬事实联网核查纠错 | `0a5cb7a` |
| **v1.46** | 2026-09-17 | data-analysis-auto走查 | `593a045` |
| **v1.45** | 2026-09-17 | 新增workflows/communication-guide.md跟老师沟通全程指南(五原… | `ff18327` |
| **v1.44** | 2026-09-17 | AI使用声明如实化 | `7d4ad85` |
| **v1.43** | 2026-09-17 | proposal-guide走查 | `0490d12` |
| **v1.42** | 2026-09-17 | coach-rules整体走查 | `7505780` |
| **v1.41** | 2026-09-17 | 环境搭建指南事实走查 | `cde29da` |
| **v1.40** | 2026-09-17 | 联网核实心理援助热线 | `16afbf1` |
| **v1.39** | 2026-09-17 | ai-literacy补会用工具的AI能力边界去魅(联网/虚拟电脑/跑脚本;关键动作人工确认… | `b15e663` |
| **v1.38** | 2026-09-17 | 菜单4英文检索默认归位工作区01+paper_search自动建目录; stats-guid… | `463176a` |
| **v1.37** | 2026-09-17 | 演示数据生成到统计练手闭环走查并固化回归(12断言); 菜单7回车默认归位工作区02; 启动… | `8f05c5d` |
| **v1.36** | 2026-09-17 | 新增缺第三方库优雅降级回归测试test_graceful_degradation(9断言)+… | `c36b1d8` |
| **v1.35** | 2026-09-17 | 修复模型图脚本coefs必填导致菜单6回车崩溃(改可选自动补0/数量健壮处理/去bold警告… | `3219661` |
| **v1.34** | 2026-09-17 | 进度卡模板对齐(剔除五指标/功效N/分半/差异结果栏)修复与工作区进度文件断档; 走查Zot… | `912d6ea` |
| **v1.33** | 2026-09-17 | 开题/答辩PPT模板对齐 | `5e7d06e` |
| **v1.32** | 2026-09-17 | 论文大纲结果章对齐(正态/EFA/差异/卡方/调节)、程序补未成年人伦理与质量埋点; 核实s… | `bcad940` |
| **v1.31** | 2026-09-17 | 紧急模式三红线+逐日任务; 量表库NSSI循证纠错(删杜撰ASFQ/SBI, 改FASM/D… | `84dfcbe` |
| **v1.30** | 2026-09-17 | 问卷星多选/填空/0-1哑变量列健壮处理 | `de18fbc` |
| **v1.29** | 2026-09-17 | 后半程走查 | `3974d03` |
| **v1.28** | 2026-09-17 | 真人视角走查 | `9cebd3a` |
| **v1.27** | 2026-09-17 | 相关矩阵热图 | `b7e9f89` |
| **v1.26** | 2026-09-17 | 文档导航完整性 | `3018184` |
| **v1.25** | 2026-09-17 | 文档代码一致性自检 | `4625a60` |
| **v1.24** | 2026-09-17 | 数据质量增强 | `7086e77` |
| **v1.23** | 2026-09-17 | 分半信度 | `4a37fca` |
| **v1.22** | 2026-09-17 | 卡方独立性检验 | `a6cef75` |
| **v1.21** | 2026-09-17 | Spearman秩相关与偏相关 | `3c3bf28` |
| **v1.20** | 2026-09-17 | 非参数差异检验 | `19cc17d` |
| **v1.19** | 2026-09-17 | 开题样本量/功效估算 | `6273ebf` |
| **v1.18** | 2026-09-17 | 调节效应简单斜率图 | `9976e3b` |
| **v1.17** | 2026-09-17 | 方差齐性+Welch稳健检验 | `8f5928e` |
| **v1.16** | 2026-09-17 | 共线性诊断 | `3670427` |
| **v1.15** | 2026-09-17 | 调节效应(PROCESS模型1) | `31f0817` |
| **v1.14** | 2026-09-17 | 人口学差异分析 | `ae3e0f5` |
| **v1.13** | 2026-09-17 | 正态性与整合三线表 | `183df94` |
| **v1.12** | 2026-09-17 | 题项分析 | `55fe7fd` |
| **v1.11** | 2026-09-17 | Horn平行分析 | `bfc177e` |
| **v1.10** | 2026-09-17 | EFA碎石图自动出图 | `80e2df4` |
| **v1.9** | 2026-09-17 | 完整探索性因子分析EFA | `8078578` |
| **v1.8.1** | 2026-09-17 | 量表库扩充 | `364cb9e` |
| **v1.8** | 2026-09-17 | 预置学生工作区，下载即有清晰归档结构 | `3cc450d` |
| **v1.7.1** | 2026-09-17 | 编码健壮性补丁+审计修正 | `a474b3f` |
| **v1.7** | 2026-09-17 | 研究对象频数自动化 | `4bfc23e` |
| **v1.6** | 2026-09-17 | 结构效度自动化（KMO/Bartlett/因子载荷） | `a39df3d` |
| **v1.5** | 2026-09-17 | Bootstrap中介分析自动化（模型4/6） | `3f5d97c` |
| **v1.4** | 2026-09-17 | 共同方法偏差自动化+练手数据+全链路走查 | `832d8b3` |
| **v1.3** | 2026-09-17 | 统计闭环增强 | `44d472a` |
| **v1.2** | 2026-09-17 | 零门槛闭环版 | `df89c6a` |
| **v1.1** | 2026-09-17 | 超级向导版 | `adcbcc0` |
| **v1.0** | 2026-09-17 |  | `5f82d4d` |

---

## 版本详情

> 以下为各版本变更说明，按版本倒序。

**v1.80 开题产出物接进菜单（第 21/22 项）＋两处真实缺陷修复**
- **接线**：v1.77 交付的 `outline_to_ppt.py`（大纲排成 `.pptx`）与 `setup_workspace.py`（补齐九目录）
  因 `menu.py` 撞 700 行门禁只能在命令行用，学生看不见；v1.79 拆完菜单后本版正式接入
  **第 21 项**（默认吃 `我的工作区/05-开题报告/我的开题大纲.md`，可先只 `--dry-run` 自检）与
  **第 22 项**（`c` 走 `--check` 只报告，回车直接补齐）。菜单 20→22 项，20 个旧处理器标签分母同改为 `/22】`，
  1–20 编号不动 → 文档里"菜单第 8/14/15/19 项"等引用全部继续有效。
- **断言去硬编码**：把 `菜单标签全部20项制` 这种"把项数写死"的断言换成
  `菜单标签编号连续且分母等于项数`（从标签自数 N），v179 的两条结构断言同步改为按 N 核对。
  今后加一项只需改标签分母，不再需要改一批断言（这颗维护地雷当场拆掉）。
- **修缺陷 1（自检误导）**：`outline_to_ppt.py --dry-run` 过去**不校验图片是否存在**，
  于是一份自检"通过"的大纲在正式生成时硬失败。现把路径解析与 PNG 判定抽成 `resolve_image()`/`check_images()`，
  自检与生成走同一把尺子；顺带修该报错文案里"模型图请先生成（菜单第10项）"的错指（模型图是第 6 项，
  第 10 项是文献卡片）。新增断言 `PPT自检与生成同口径拒坏图`。
- **修缺陷 2（文档路径错）**：`templates/opening-ppt-outline.md` 与 `outline_to_ppt.py` 文档字符串
  把开题大纲写成放工作区的 `02-开题报告/`——02 是问卷数据，开题是 **05**；学生照做会撞"目录不存在"。
  三处一并改为 `05-开题报告`（含本次移入工作区的那份示例大纲）。为防再犯，
  `consistency_check.py` 补规则 5b：文档里任何 `我的工作区/NN-xxx` 目录名（**包括嵌在命令和图片语法里的**，
  旧规则只认整段被反引号包住的路径）都必须是真实存在的目录；植入 `98-幽灵目录` 实测判非 0。
- **同一条新规则被阶段 G 抓出误报，当场把尺子改准**：干净解压副本里 `full_e2e` 红 2 条——
  文档教学生"把大纲存成 `我的工作区/05-开题报告/我的开题大纲.md`"，而那是**学生自己产出**的文件，
  包里不可能带着它，按"文档链接必须存在"判就必然误报。现：规则 4 跳过 `我的工作区/` 开头的引用；
  规则 5 只核路径**第一段**（目录号写错照样抓，目录里存什么学生说了算）；规则 5b 不变。
  两条阴性探针（反引号里的 `98-幽灵/`、命令里嵌的 `02-开题报告/x.md`）复测仍判非 0。
- **文档接线**：`START.md` 工具清单补 `outline_to_ppt.py`/`setup_workspace.py` 两条 ＋
  "开题/答辩PPT用法""工作区用法"两段（含不代写与"换机器放映前查中文字体"红线）；第六步的工作区目录列表
  从 4 个补到 9 个（v1.77 起就漏改）；`QUICKSTART.md` 菜单清单补 21/22；
  `workflows/proposal-guide.md` 第四节接工具落地路径；`我的工作区/先读我.md` 两处补菜单号；
  `README.md` 目录树工具数 21→23；`tests/e2e-test.md` 补 测试76/77/78 三节（v1.77 当时未同步用例清单，一并补平）。
- **验证**：第 21/22 项**真跑**（喂输入走完整交互）：21 项 dry-run 解析使用者的 13 页大纲通过、
  正式生成 cover=1/bullets=8/table=3/picture=1；22 项 `--check` 报"九个目录齐全，无需补齐"且不动文件。
  full_e2e 640→**643 项全过**；consistency、validate、py_compile 全绿；菜单四件套最大 402 行，远低于 700。
- `doubao-skill` 无改动（轻量版本就不带脚本），手机版与技能包沿用 v1.4.1 产物。

**v1.79 菜单拆分：解除"新工具进不了菜单"的接线门禁（行为零变化）**
- **为什么非做不可**：`tools/menu.py` 恰好 700 行，正撞 `原大文件已分解` 门禁（`tools/**.py` 不得 >700 行）。
  实测加两个菜单项会红 4 条 → v1.77 交付的 `outline_to_ppt.py`（大纲→开题 PPT）与 `setup_workspace.py`
  只能走命令行，学生看不见、用不上。这颗雷自 v1.77 起一直挂在 PROJECT_PLAN 第 4 条与本文件"已知欠账"。
- **拆法**（纯搬家，不改一行交互逻辑）：入口 `menu.py`（102 行：说明、菜单表、主循环）
  ＋ `menu_io.py`（`ask_path`/`run`/`pause`/`_ask_num` 共用交互件）
  ＋ `menu_data.py`（数据与统计组 12 项）＋ `menu_lit.py`（文献与产出组 8 项）。
  四份都带项目统一的输出编码守卫（`全部脚本有编码守卫` 断言覆盖 `tools/*.py`）。
- **行为不变的证据**：拆分前抓主菜单基线输出，拆分后同输入再抓一次，**逐字节一致**；
  另真跑一次第 6 项（模型图）的取消路径确认子进程链没断。
- **测试**：`full_e2e.py` 的 18 条菜单文本断言原本各自 `tx("tools/menu.py")`，改为统一读 `tools/menu*.py`
  合并文本（今后再拆不用改一批断言）；+4 条结构不变量：模块齐全、入口 <200 行、
  **真 import 核对**（菜单表 20 项、每项可调用、首末编号 1/20、`t_*` 定义集合与挂接集合相等）、
  喂 `0` 真跑菜单须列全 1–20。第三条是这次拆分的靶心——文本匹配抓不到"函数搬家了却忘了挂进菜单表"，
  实测把最后一项摘掉即报 `False ['t_multcomp']`。full_e2e 636→**640 项全过**。
- **文档**：`AGENTS.md` 目录速查的工具数由 21（v1.77 加了两件工具后一直未更）校正为 23 可调脚本并注明菜单四件套；
  `DEVELOPMENT.md` 阶段 D 写明"加一项＝在对应分组模块写 `t_xxx` ＋ 在 `MENU` 表挂一行 ＋ 标签分母同步改"。
- 工具 23 项、菜单 20 项、`doubao-skill` 均不变（手机版与技能包不携带脚本）。

**v1.78 手机合并单文件构建链路闭环（补丁；doubao-skill 维护者工具改动，规则文字零变化）**
- **现象**：开发树里跑 `tests/full_e2e.py` 有两条红——`Skill自检退出0` 与 `v161旧语气名清零`；
  干净解压副本却是全绿。两条都指向 `doubao-skill/thesis-ai-coach-手机版.md`：
  它是 `.gitignore` 排除的构建产物，停在语气文件改名（gentle-sister/strict-ceo/puppy）之前，
  所以只有开发树会红，报的还是"旧语气名"这种指不到真因的方向。
- **根因是一处路径不一致**：`build_mobile_single.py` 的 `--out` 默认值是裸文件名，按**当前目录**解析。
  从仓库根执行就把 180 KB 产物写进**仓库根**（该路径不在 .gitignore 内，`git add -A` 会把整份重复规则
  误提交），而 `validate.py` 检查的是 `doubao-skill/` 那份 → 重新生成一百次自检也报"不同步"。
  `doubao-skill/README.md` 发布流程第 2 步又写成 `--out <分发包目录>/…`，等于规定了一条
  "只更新分发包副本、永不更新受检副本"的路径，产物必然长期过期。
- **修**：默认输出改为 `ROOT / DEFAULT_NAME`（技能目录内，正是 validate 检查、.gitignore 排除的那份），
  显式 `--out` 的相对路径语义不变；validate 的"不同步"提示改成可执行命令（跑一次生成即可）；
  README 用法与发布流程改为"先在本目录生成、再拷进分发包"；`旧语气名` 断言排除该构建产物
  （产物过期已由 validate 抓，源码断言不该替构建产物报警）。
  **分发包里那份从头到尾是对的**（与重新生成结果字节相同、无旧语气名），本版修的是开发树与工具路径，不是学生拿到的内容。
- **测试**：+2 条行为锁定，真跑子进程断言"生成方与校验方同一路径"这条不变量
  （`v178默认输出落技能目录而非当前目录`、`v178换目录跑--check仍判同步`），测试结束还原产物原状；
  full_e2e 632→**636 项全过**，consistency/validate/py_compile 全绿。Skill v1.4.1（仅维护者工具）。
- **连带修 `consistency_check.py` 两处误报**（都是"防漂移的尺子量错了对象"，与 v1.77 那颗幽灵脚本雷同类）：
  ① 全局 CLI 开关集合只扫 `tools/`，把版本历史里对技能生成器 `--out` 的真实引用判成未定义开关 →
  现并入 `doubao-skill/*.py` 的开关（仍不做按脚本归属核对，也不放松抓假开关）；
  ② 文档引用 `doubao-skill/thesis-ai-coach-手机版.md` 在干净副本里必然悬空（它是 .gitignore 排除的构建产物）→
  加构建产物白名单，引用产物路径不再算漂移。两处都做过阴性验证：植入假开关与真不存在的文档仍被抓住。
- **同收（仓库侧维护，不改包内容）**：三个 git 工作区（主仓 `master` 落后 37 个提交、`thesis-coach-advance`、
  `thesis-coach-overnight`）合并为唯一目录 `thesis-ai-coach-project`，master 快进跟齐 v1.77（未重写历史），
  合并前全量 ref 备份为项目外 `_归档/bundles/all-refs-before-consolidate-20260919.bundle`；
  顶层 20 多个一次性验证副本目录（约 190 MB）清理，可用 `git archive <tag>` 重建，
  黄金对照脚本与审查报告移入 `_归档/` 并把里面指向旧工作区的路径改到主仓。

**v1.77 开题产出物闭环（并行复核会话交付；doubao-skill 本轮无改动）**
- **背景**：v1.62–v1.76 连续 15 个版本集中在统计分析（第 7–8 阶段），而使用者当前处在**开题**阶段；
  且全项目零 PPT 生成能力（`defense-ppt-outline.md` 只是带【】的 markdown 大纲）。
  本版补的是"学生现在真正拿得出去的东西"，不是又多一个统计工具。
- **新增 `tools/pptx_writer.py`**：python-pptx 之上的生成层（标题/多级要点/表格/PNG 图片/
  节标题/演讲者备注/4:3 与 16:9）。依赖缺失时 `available()` 返回 False，调用方退回 markdown，
  rc=1 不崩 ImportError。16:9 只缩放"自己写了坐标"的形状，不碰继承关系——
  曾把母版与版式各缩放一次，导致占位符实际按 (4/3)² 放大、顶出页面 3.56 英寸，已修并写进注释。
- **新增 `tools/outline_to_ppt.py` + `templates/opening-ppt-outline.md`**：纯文本大纲 → 真 .pptx，
  12 页结构照抄 `workflows/proposal-guide.md` 第四节口径（未另发明）。
  **只排版不代写**：内容全部留【】占位，不生成连贯正文，避开 CONSTITUTION 红线。
  坏输入（图片路径不存在、`![题注】` 缺路径、比例非法、首页前出现正文）统一中文报错 rc=1。
- **新增 `tools/setup_workspace.py`**：`我的工作区/` 由 4 个目录扩到 9 个（+05-开题报告、
  06-论文正文、07-答辩材料、08-量表与伦理、09-导师沟通记录）。只新增、不重命名/移动/删除、
  幂等；01–04 编号不动以兼容老学生本地数据；`先读我.md` 同步说明每个目录放什么。
- **测试**：`full_e2e.py` 611→**632 项全过**（+21 条 `v176guard` 行为锁定，钉住本会话前四轮
  已修但零覆盖的行为：方差齐性 df 按实际参与组算、n=1 组必须报警且进 CSV、单变量段落不得
  出现"介于"、反向计分越界守卫的报与不报、大纲解析与坏输入 rc=1、生成的 .pptx 过
  zip 完整性与 Content_Types 无重复 PartName、工作区补齐幂等）。
  另把已过时的 `v175 ROADMAP无幽灵脚本`（点名禁止出现两个脚本名）改回其本意的不变量：
  ROADMAP 引用的 `*.py` 必须真实存在——原写法会在脚本入库后反向逼人把它们从文档删掉。
- **验证**：干净解压副本 632/632；consistency 无漂移；全量 py_compile 通过。
  PPT 产物用一次性隔离 venv 中的 python-pptx 作**只读裁判**回读：13 页 / 3 个表格尺寸 /
  图片字节与源文件一致 / 12 页备注 / 模板说明零泄漏 / 形状零越界。
- **已知未做**：① `outline_to_ppt.py`、`setup_workspace.py` 未注册进 menu.py——`tools/menu.py`
  恰好 700 行，撞"单文件不得超 700 行"门禁（实测加两项即红 4 条），须先拆 menu.py，
  且 `菜单标签全部20项制` 断言里硬编码的菜单数要一起改；② 开题正文装配器不做（把量表库、
  样本量、伦理条款装配成连贯正文会踩代写红线），需先由使用者定"装配到哪一步算代写"的口径。

**v1.76 文档勘误与行数守卫闭环（完整版，doubao-skill 本轮无改动）**
- **背景**：v1.75 CHANGELOG 详情把 README 收敛后行数写成"约 150 行"，实际为 170 行——数字必须可追溯，发版后自查发现即勘误；同时全局断言只守 README <200 行硬上限，版本区轮换时缺少提前量，本次把版本区行数单独收紧到 ≤180，逼近即提示该做版本区收敛。
- **改动**：①CHANGELOG v1.75 详情行数改为"184→170 行，实计"；②full_e2e 新增 README 版本区行数守卫（≤180），616→617 项；③START 版本号、README 版本区（v1.76 当前块、v1.75 一行）、PROJECT_PLAN §二十六、e2e-test 测试75 同步。
- **测试（P0）**：隔离 worktree full_e2e **617 项全过**；consistency、validate、全量 py_compile 全绿。
- **范围控制**：无代码改动；工具脚本 21、菜单 20 不变；doubao-skill 无改动。

**v1.75 路线图挂账维护闭环（完整版，doubao-skill 本轮无改动）**
- **背景**：v1.62–v1.74 夜间连续闭环后，ROADMAP 的 v1.x 待办仍停在旧清单，今晚暴露的真实缺口（3+ 时点非参数、有序相关、高阶信度、回归诊断深化）没有挂账，已做的"MI 不内置"架构决策也没落档；同时 README 版本区经多次轮换残留了孤立的旧版本要点，偏离"只保留当前与上一版摘要"的自定规约（行数逼近 200 行硬约束）。
- **改动**：①ROADMAP v1.x 新增 5 项待办（Friedman＋Nemenyi/Kendall's W、polychoric/polyserial、分层 ω 与 Schmid-Leiman、HTMT2、回归诊断扩展 Cook 距离/杠杆/ΔR²、PPT 导出在制说明）与 2 项已完成决策（MI 教学指引路线、v1.62–v1.74 一行指针），仍 <60 行硬约束；②README 版本区收敛为"当前版本块＋上一版一行"，v1.71–v1.73 摘要回归 CHANGELOG 单一事实源（184→170 行，实计；v1.76 勘误）；③START 版本号、CHANGELOG、PROJECT_PLAN §二十五、e2e-test 测试74 同步。
- **测试（P0）**：full_e2e 612→**616 项全过**（+4：ROADMAP 新增挂账关键词与 MI 决策落档、README 收敛后仍含当前/上一版、行数约束）；consistency、validate、全量 py_compile 全绿。
- **范围控制**：无代码改动；工具脚本 21、菜单 20 不变；doubao-skill 无改动。

**v1.74 论文模板接线维护闭环（完整版，doubao-skill 本轮无改动）**
- **背景**：v1.72 单样本检验、v1.73 回归残差诊断上线后，工具层与 stats-guide 已接线，但学生真正照着写方法章的 `templates/paper-outline.md`、START 能力清单与 QUICKSTART 流程没有同步，属于"能力到了产出链断了"的维护缺口（用户明确要求闭环维护旧内容）。
- **改动**：①paper-outline 4.4 配对设计条目补单样本 t 适用场景（Likert 中值/常模分/理论值）、报告项（t(df)、p、d_z；非正态单样本 Wilcoxon 报 r）与"偏离中点≠干预效果"边界；②4.5 表3 规范补回归前提诊断交代（容差/VIF<5、Durbin-Watson 接近 2 与 dL/dU 严格判定、残差 Shapiro-Wilk、残差非正态且小样本报 Bootstrap CI），注明三项 auto_stats 自动给出；③START auto_stats 能力描述补 DW/残差正态；④QUICKSTART 第3步补回归诊断与单样本入口。
- **测试（P0）**：full_e2e 608→**612 项全过**（+4：outline 两处接线、START/QUICKSTART 各一处）；consistency、validate、全量 py_compile 全绿。
- **范围控制**：无代码改动、无数值口径变化；工具脚本 21、菜单 20 不变；doubao-skill 无改动。

**v1.73 回归残差诊断闭环（完整版，doubao-skill 本轮无改动）**
- **缺口**：心理学论文多元回归表常被要求报告 Durbin-Watson（残差独立性）与残差正态性，此前工具只报 VIF 共线性，学生要手点 SPSS 或漏掉这两项；同时 Shapiro-Wilk 实现位于工具层 `assumption_check.py`，stats 统计包无法复用（结构倒挂）。
- **重构（P1）**：`shapiro_wilk` 连同 Royston AS R94 多项式系数、`_poly`/`_norm_upper` 助手整体下沉到 `stats/mathx.py`（函数体逐行未动），`assumption_check.py` 与 `paired_compare.py` 改为导入；新增 `durbin_watson(residuals)`（DW=Σ(e_i−e_{i-1})²/Σe_i²，n<2/残差常量返回 None）。
- **功能**：`linear_regression` 末尾新增"残差诊断（回归前提）"块——D-W 数值与三档提示（1.5~2.5 经验区间，明示严格判定查 dL/dU 临界值表、阈值教材不一）、残差 Shapiro-Wilk W/p 与判读；返回字典新增"残差诊断"。auto_stats 两处回归入口自动受益。
- **黄金验证（P0）**：下沉后 SW 200 组（n=5…89，正态/指数/均匀/含结）对 scipy W 误差 4.6e-10、p 4.6e-9（Royston 近似固有精度，迁移前一致）；DW 定义级用例（常量→0、[1,-1]×10 有限样本→3.8、[1,2,3]→1/7、n<2→None）＋100 组随机序列对 numpy `diff²/Σ²` 零误差＋AR(1,0.8) 序列 DW=0.577 触发正自相关、白噪声 1.77 不报；n=120 双预测变量回归夹具 DW=2.355、残差 W=0.9792/p=.0600 与 numpy OLS＋scipy 逐位一致。
- **测试（P0）**：full_e2e 600→**608 项全过**（+8：下沉结构断言、DW 手算、SW 可用、回归夹具 R²/F/DW/残差 SW 输出、AR(1) 报警、前提工具迁移回归）；consistency、validate、全量 py_compile 全绿；regress.py 703→压缩至恰好 700 行（硬约束）。
- **范围控制**：工具脚本 21 个、菜单项 20 不变；doubao-skill 无改动；黄金脚本与补丁不入库。

**v1.72 配对检验扩展单样本模式（完整版，doubao-skill 本轮无改动）**
- **需求**："这组人的 Likert 均分是否高于中值 3""与常模分是否一致"是高频问题，此前工具只支持配对/独立样本，学生只能用 SPSS 手动点或误用独立样本 t；单样本 t 在数学上等价于 d=x−C 的配对检验，可零风险复用 v1.68/v1.70 的全套机器（差值 Shapiro、d_z 及 CI、Wilcoxon 精确/近似、rank-biserial r、有结小样本警示）。
- **CLI**：单文件模式新增 `--onesample 列1,列2 --constant C`（可配 `--group/--level`）；互斥校验四类（缺 constant、constant 无 onesample、列不存在、与两文件/--scales/--id 混用）均 rc=1 中文提示；菜单第19项开头选"单样本"，问答收集列与常数。
- **呈现**：控制台标题/行/尾注、可粘论文段落（"单样本 t 检验显示…与检验常数 C 相比…"）、CSV 备注（"单样本(vs C)"）按模式切换；CSV 表头与配对模式同构（前测M 列放常数 C），配对模式零回归。
- **黄金验证（P0）**：300 组模拟（n=5…100，连续/含结，常数随机）对 scipy.stats.ttest_1samp：t 最大误差 4.3e-14、p 1.3e-13、d_z 4.9e-15；Wilcoxon 校正 z 1.8e-15；手工 x=[3,4,5] vs C=3 → t(2)=√3≈1.732、均值差=1。
- **测试（P0）**：full_e2e 589→**600 项全过**（+11：开关、全样本/分组/手工例跑通与口径、报告与 CSV 备注、四类坏参数、配对回归）；consistency、validate、全量 py_compile 全绿；菜单新增内容受"单文件 ≤700 行"硬约束，同步压缩至恰好 700 行。
- **教学边界**：stats-guide/workflow 明示"与中值比较只能说明偏离中点，不能声称干预效果"，并交代 Likert 差值多零值/结、n<30 精确法复核、与常模比较须交代常模口径。
- **范围控制**：工具脚本数 21、菜单项 20 不变；doubao-skill 无改动；测试与补丁不入库。

**v1.71 多重插补/FIML 教学指引闭环版（缺失处理最后一公里；完整版，doubao-skill 本轮无改动）**
- **背景**：v1.66 的缺失分析工具在拒绝 MCAR 时只给"建议多重插补/FIML"的结论，学生拿到建议后不知道在 SPSS/R/AMOS 里具体怎么点、m 取多少、PROCESS 不自动池化怎么办、论文怎么写；网络教程口径混杂（均值插补、LOCF 等错误做法常见）。本闭环补齐可照做、可核对、带红线的操作指引，工具本体仍坚持"只检验不插补"，避免学生把插补当一键补全。
- **新增 `psychology/missing-imputation-guide.md`**：①缺失机制×缺失率决策树（<5% 且 MCAR 可成列删除；5～10%/MAR 上 MI/FIML；>20% 或单变量 >40% 降级结论；MNAR 必须敏感性分析）与量表单题题均替代的适用边界；②七条红线（禁均值/LOCF/单点回归插补、禁看结果换方法、插补模型含全部分析变量与辅助变量、无效码先清洗等）；③SPSS 多重插补菜单步骤（分析→多重插补→填充缺失值；连续/量表变量推荐 PMM 预测均值匹配，插补值取自真实观测、不越量表范围；m 默认 5、建议 20+；支持自动池化的过程与 PROCESS 等不池化时的两条出路）；④AMOS FIML（分析属性→估计→勾选估计均值与截距即自动 FIML；辅助变量纳入）；⑤R mice 代码（mice/pmm、with/pool、lavaan runMI、FIML missing="ml"）与 Rubin 池化公式（Q̄、Ū、B、T=Ū+(1+1/m)B、校正 df）；⑥m 次数依据（Rubin 1987；Graham 2007；von Hippel 2018，m≈缺失率百分数、20～100）；⑦敏感性分析（完整病例对照、m/方法对照、delta/tipping point）；⑧方法/结果/局限三章论文模板。
- **事实核查**：SPSS PMM 定义、默认插补 5 次、菜单路径经 IBM SPSS Missing Values 官方手册核对；AMOS FIML 勾选位置经 SPSS/Amos 官方说明核对；池化公式与 m 建议引自 Rubin/Graham/von Hippel 经典文献。
- **接线**：`missing_report.py` 拒绝 MCAR 时控制台解读行与可粘论文段落均追加指南路径；stats-guide 缺失处理处、data-analysis-auto 第 1.5 步、START 查证用资料表（新增一行并调整"最长"说明）、README 专业知识库列表同步登记。
- **测试（P0）**：`full_e2e.py` 577→**589 项全过**（+12：指南存在/决策与红线/三软件步骤/Rubin 与敏感性/论文模板、缺失工具与三处文档接线、README/START 登记、MAR 夹具 n=300 控制台与报告均含指引）；v1566 专业文档断言由三份更新为四份（新指南同样含使用约定块与 P0 处置指针）；consistency_check（Markdown 40→41 个，开发中捕获并修掉一处误写脚本名 data_clean→data_cleaner）、validate、全量 py_compile 全绿。
- **范围控制**：不新增可执行插补代码（运行时纯标准库约束与"先理解再插补"的教学定位不变）；doubao-skill 本轮无改动；测试与补丁不入库。

**v1.70 配对检验效应量与口径增强版（非参数结果可解释性；完整版，doubao-skill 本轮无改动）**
- **背景**：v1.68 的 Wilcoxon 符号秩只给 W+/z/p，学生在论文里报了显著性却没有效应量（评审常问"差异多大"）；Likert 前后测差值几乎必有结，n 小时被迫走正态近似，该口径在小样本偏乐观却无任何提示；反向计分时 0 起编数据误用 1 起编公式会静默毁掉均分/信度，此前无告警。
- **`paired_compare.py` 增强**：Wilcoxon 结果新增 rank-biserial 相关 **r_rb=（W+−W−）/（W++W−）**（Kerby 2014 简单差公式，与 R `effectsize::rank_biserial`、JASP 的 r_rb 同口径），符号与差值方向一致（d=后−前，正=后测更高），|r| .1/.3/.5 分小/中/大；控制台精确/近似两分支、可粘论文段落（仅在以 Wilcoxon 为结论时随句给出）、导出 CSV 新增 `Wilcoxon_r_rb` 列。
- **小样本有结警示**：n<30 且差值存在结时，控制台与论文段落均明示"有结时精确分布不适用、正态近似 p 偏乐观（实测可与精确值差近一倍），以 SPSS/JASP 精确法或蒙特卡洛复核，并同时报告配对 t"，避免给小样本 Likert 研究虚假的精确感。
- **`stats/dataio.py` 增强**：`recoded_item_series` 反向计分后若结果越出 1~likert，硬提示并列出题项与原始值，指明三类原因（0 起编数据点数写错、混入 99/77 无效码、0/1 计数题误配反向题）；正向题 0/1 计分不误报。
- **黄金验证**：600 组模拟（n=4…80 × 连续正态/Likert 结/指数偏态/含零差值）对 scipy.stats.wilcoxon：r_rb 最大误差 8.9e-16、连续性校正 z 同步回归；手工例（全正 r=1、对称 r=0）逐位一致；full_e2e 570→**577 项全过**（+7：公式在源码/手工 n=5 r=.333/CSV 表头/偏态报告句/大样本 r=.820 有界/小样本结警示回归）；consistency_check、validate、全量 py_compile 全绿。
- **文档**：stats-guide 配对小节（r_rb 定义、报告格式、有结小样本复核要求）、data-analysis-auto 第 6.6 步、START、README（版本轮换）、AGENTS、QUICKSTART、paper-outline、PROJECT_PLAN §二十、e2e-test 测试69 同步。

**v1.69 多重比较校正闭环版（多检验假阳性膨胀断档；完整版，doubao-skill 本轮无改动）**
- **背景**：多组两两比较、多个量表同时比较、相关矩阵、多个时点配对检验等场景下，"有一个 p<.05 就显著"会让假阳性随检验数膨胀（m=10 时族系假阳性最高约 40%）；工具箱此前只有 auto_stats 内部的 Bonferroni 事后，没有能对任意一组 p 值统一校正、并区分 FWER/FDR 口径的独立出口，学生容易漏校正或在 SPSS 各对话框里得到不一致的结果。
- **新增 `tools/mult_compare.py`（菜单第 20 项）**：四种方法同列——Bonferroni（min(1,m·p)，最保守）、Holm 逐步法（排序后 (m−i+1)·p 前缀累积取大，同控 FWER 且一致不弱于 Bonferroni，默认推荐）、Benjamini-Hochberg 逐步法（控 FDR，探索性/检验数多）、Benjamini-Yekutieli（除以调和数 c(m)，任意相关结构下控 FDR）；每种给校正后 p 与 α 下显著判定、显著项汇总，并给可粘论文段落；导出 `_多重比较校正.csv`（四法校正 p＋四列显著判定，UTF-8-BOM）与 `_多重比较报告.txt`。
- **两种输入**：`--ps .002,.033,.12`（可 `--names` 命名）直接给 p；或把 CSV 作为位置参数加 `--pcol p值列`（可 `--namecol 名称列`），自动跳过空值；`--method bonferroni/holm/bh/by/all`、`--alpha` 可调。
- **黄金验证**：R `p.adjust` 经典向量（.01…05 与 11 向量含结）逐位一致；3000 组随机向量（m=2…65，含 ties/0/1）对 R 口径独立实现与 scipy.stats.false_discovery_control（BH/BY）：Bonferroni/Holm/BH 零误差、BY ≤4.4e-16；发现并对齐了临界值运算顺序（先 m/i 再乘 p，与 scipy 一致），避免 .05 边界因浮点被误判显著；20 组 CLI 端到端场景全过。
- **方法学红线（写进输出与文档）**："家族"范围与校正方法必须分析前确定，禁止几种都跑挑最宽松的报；原始 p 与校正后 p（或校正阈值）一起报告；校正后不显著也是结果，不得删并检验；等方差、各组 n 相近的 ANOVA 全两两比较 Tukey HSD 更合适（指引 SPSS 事后比较），本工具用于计划比较、跨方法汇总与 SPSS 不支持的场景。
- **健壮性**：p 越界/非数字、少于 2 个 p、无来源、CSV 缺 pcol/文件不存在/空文件、名称数不符、坏 alpha 等统一中文提示（argparse 错误 rc=2，业务错误 rc=1）；UTF-8/GBK 自适应；纯标准库（复用 stats.dataio/mathx）。
- **文档**：stats-guide 新增"多重比较校正"方法学小节（FWER vs FDR、家族界定、报告规范）；data-analysis-auto 新增第 6.7 步与总览行；START、README（版本轮换）、AGENTS（21 脚本）、QUICKSTART（菜单20）、paper-outline、PROJECT_PLAN §十九、e2e-test 测试68 同步。
- **测试（P0）**：`full_e2e.py` 541→**570 项全过**（+29：纯标准库/编码守卫/菜单20项与编号制/三处文档接线/R 经典向量四法黄金值/四法全列/名称/CSV 与 GBK/13 类坏输入/产物表头/报告与源码红线）；黄金脚本与 20 组 CLI 场景全绿；consistency_check、validate、全量 py_compile 全绿。

**v1.68 配对设计差异检验闭环版（干预研究前后测断档；完整版，doubao-skill 本轮无改动）**
- **背景**：干预类本科论文（实验组/对照组×前测/后测）普遍要报前后测变化，但工具箱此前只有样本量设计（ttest-paired）与由 t 值反算 d_z 的效应量工具，没有能直接吃前后测数据、一次给齐配对检验＋前提＋非参数＋可粘论文段落的出口；学生容易误用独立样本 t（破坏配对信息）或只报组内显著就宣称干预有效。
- **新增 `tools/paired_compare.py`（菜单第 19 项）**：对每对配对变量给配对 n（无法配对者整对剔除并计数）、前/后测均值、差值 M/SD、配对样本 t(df) 双侧 p、Cohen's d_z 及近似 95%CI（Becker SE≈√(1/n+d_z²/2n)）、**差值** Shapiro-Wilk（复用 v1.67 的 Royston AS R94 实现）、Wilcoxon 符号秩（零差值剔除、结平均秩；n≤25 且无结用 W+ 精确分布动态规划，否则用含结校正 σ² 与连续性校正的正态近似 z，与 SPSS 口径一致）；导出 `_配对检验.csv` 与 `_配对检验报告.txt`。
- **两种数据口径**：①单文件宽表 `--pairs 前测列:后测列`（可 `--id` 按编号配对，不给按行序）；②两文件 `前测.csv 后测.csv --id 编号`（强制编号列，内连接配对，前/后测独有人员分别计数），可配 `--scales` 按反向计分题项算量表均分（两文件题项列名一致）；`--group 列 --level 取值` 支持只在某一组内做前后测。
- **黄金验证**：462 组模拟（n=3…100 × 正态/含结四舍五入/指数偏态）对 scipy.stats.ttest_rel 与 wilcoxon：t 最大误差 1.8e-15、p 对数误差 9e-13、d_z 3.3e-16、Wilcoxon 近似 z 1.3e-15、精确双侧 p 零误差（新版 scipy 双侧统计量取 min(W+,W−)，工具两个秩和都报告）；20 组 CLI 端到端场景全过。
- **方法学红线（写进输出与文档）**：配对 t 的前提是**差值**近似正态而非原始分数；配对必须是同一个体，两文件禁止按行硬凑；Wilcoxon 检验位置移动而非均值差；3 个及以上时点用重复测量 ANOVA/线性混合模型，两两比较 Bonferroni（α/k）；"实验组变化比对照组大"必须比较两组差值（独立样本 t on 差值）或做组别×时点交互作用，不能只报组内前后测显著。
- **健壮性**：常量差值、n<3、缺编号列、两文件无 --id、--pairs 格式错、列不存在、组缺失、坏 alpha、空文件、单文件误用 --scales、GBK 等统一中文提示 rc=1/优雅降级；纯标准库。
- **文档**：stats-guide 新增"配对设计：前后测/两条件"方法学小节；data-analysis-auto 新增第 6.6 步与总览行；START、README（版本轮换）、AGENTS（20 脚本）、QUICKSTART（菜单19）、paper-outline、PROJECT_PLAN §十八、e2e-test 测试67 同步。
- **测试（P0）**：`full_e2e.py` 504→**541 项全过**（+37：纯标准库/编码守卫/菜单19项与编号制/三处文档接线/配对 t 与 d_z 黄金值/差值正态/Wilcoxon 精确与近似/分组 n=30/三段产物/常量降级/偏态推 Wilcoxon/双文件 id 配对计数/双文件 scales n=48/n<3/12 类坏输入/GBK/三条红线）；黄金脚本与 20 组 CLI 场景全绿；consistency_check、validate、全量 py_compile 全绿。

**v1.67 参数检验前提假设闭环版（t/ANOVA/回归方法章断档；完整版，doubao-skill 本轮无改动）**
- **背景**：参数检验（独立样本 t、方差分析、线性回归）方法章普遍要交代正态性与方差齐性两个前提；此前工具箱只有描述统计里的偏度/峰度 Kline 启发式判读，正式的 Shapiro-Wilk 检验要去 SPSS/JASP 跑，auto_stats 的 Levene 也只在差异分析内部自动使用，没有独立的"前提检验＋可粘论文结论"出口。
- **新增 `tools/assumption_check.py`（菜单第 18 项）**：吃清洗后问卷 CSV（建议带 scales.txt，按反向计分后的题项计算**量表均分**，仅题项全答记录纳入并如实报 n；不给 scales 则自动识别数值列，可直接吃 `_量表总分.csv`），输出每个因变量的 n、M、SD、调整偏度/超额峰度（SPSS 口径）及其 z（SE≈√(6/N)、√(24/N)）、Shapiro-Wilk W/p 与分档判读；`--group 列名` 时逐组做 Shapiro-Wilk，并给 **Brown-Forsythe 方差齐性检验**（Levene 基于中位数，与 SPSS"基于中位数"同口径，复用 stats.compare._levene）；导出 `_前提假设检验.csv`（总体/分组/方差齐性三段式 UTF-8-BOM）与 `_前提假设报告.txt`（含可粘论文段落）。
- **统计口径（可追溯）**：Shapiro-Wilk 权重用 Royston 对期望正态序次统计量的多项式近似（与 EnvStats `swGofTestStatistic` 逐项核对），p 值用 Royston (1992) AS R94 正态化变换（n=3 精确分布 6/π·(arcsin√W−π/3)，n=4…11 gamma 分支，n≥12 ln n 多项式分支），系数取自 AS R94 公开源码（matrixscience 镜像的 swilk C++ 移植，与 R `src/library/stats/src/swilk.c`、scipy 编译的 swilk.f 同源）；正态分位数用 Acklam 逼近入 stats/mathx.py，极小 p 用 AS66 Mills 比连分式（可算到约 1e-150，不被过早截到 1e-19）。
- **黄金验证**：295 组（n=3…4000 × 正态/均匀/指数/5点Likert/混合正态）对 scipy.stats.shapiro，W 最大绝对误差 4.3e-10、p 误差 <8e-9 个数量级（远尾一致）；偏度/峰度对 scipy 无偏估计误差 1.4e-14；200 组随机分组 Brown-Forsythe F/p 对 scipy.stats.levene(center='median') 误差 2.5e-13；n=3 三个特例 W/p 逐位一致。
- **分档判读（辅助而不限制）**：p≥.05 且 Kline 通过→不拒绝正态；p<.05 但 |偏度|<3、|峰度|<10 时，n≥300 提示检验过敏感可视为近似正态、50≤n<300 属轻度偏离参数检验仍稳健、偏度/峰度 |z|>3.29 建议以 Bootstrap 偏差校正区间或 Welch 为主分析；超 Kline 判据才指引 Welch/非参数。方差不齐指引 Welch t/Welch ANOVA（事后 Games-Howell）。
- **红线**：Shapiro 不显著≠证明正态，大样本几乎必显著，以偏度峰度数值＋Q-Q 图综合判断；Likert 单个题项是有序分类不要求正态（看总分/均分）；不得为"通过"检验删数据、删离群值或反复变换挑 p；n>5000 提示 p 值口径不稳；Q-Q 图/直方图仍在 SPSS/JASP 生成。
- **健壮性**：常量列/组内 n<3 优雅降级并明示；文件不存在/空文件/坏 scales/题项缺失/分组列不存在/仅一组/坏 alpha/无参统一中文 rc=1；UTF-8/GBK 自适应；纯标准库。
- **文档**：stats-guide 新增"参数检验前提：正态性与方差齐性"方法学小节并改写旧"脚本不替代正式正态检验"表述、t/ANOVA 节交叉引用；data-analysis-auto 新增第 6.5 步并在总览登记；START/README/AGENTS/QUICKSTART/paper-outline/PROJECT_PLAN §十七/e2e-test 测试66 同步。
- **测试（P0）**：`full_e2e.py` 475→**504 项全过**（+29：纯标准库/编码守卫/菜单18项与编号制/三处文档接线/分组跑通/X1 W 黄金值/X2 方差不齐黄金 F=31.993/三段式 CSV/报告段落/指数 W 黄金值/z 极显著 Bootstrap 主分析/强偏态超 Kline/scales 均分模式/n>5000 提示/组内 n<3 降级/常量列/八类坏输入 rc1/红线声明）；黄金脚本与 18 组 CLI 端到端场景全绿；consistency_check、validate、全量 py_compile 全绿。

**v1.66 缺失值分析与 Little's MCAR 检验闭环版（数据清洗前方法章断档；完整版，doubao-skill 本轮无改动）**
- **背景**：清洗器只负责剔除高缺失个案，但方法章要求交代"缺了多少、怎么缺、能否直接删/插补"；SPSS 缺失值分析与 R naniar 的 Little's MCAR 是心理学论文常见报告项，此前工具箱无对应出口。
- **新增 `tools/missing_report.py`（菜单第 17 项）**：吃预处理后问卷 CSV（建议带 scales.txt 只分析题项），输出总缺失率、逐题缺失率/观测均值、缺失模式分组（○/× 掩码与缺失变量）、成列删除完整样本量；EM（Dempster-Laird-Rubin，ML 除 N，tol 1e-7）估计 μ、Σ 后做 Little (1988) MCAR 检验；导出 `_缺失值分析.csv`（逐题/模式/检验三段式 UTF-8-BOM）与 `_缺失值报告.txt`（可直接粘论文的段落）。
- **统计口径（可追溯）**：先联网核查并下载 naniar 1.0.0 源码（CRAN）逐行核对，确认 `mcar_test` 用的是**均值项统计量** d²=Σ_j n_j(x̄_j−μ̂_j)'Σ̂_j⁻¹(x̄_j−μ̂_j)、df=Σ_j k_j−k（misty、Enders 2010 同口径），而非 SPSS 的含协方差似然项完整版；模拟校准中均值项口径一类错误 6.5%（名义 5%）、MAR 检出 98%，含协方差项的 (n−1) 无偏协方差口径严重保守（0.8%）、朴素 LR 口径反保守（45.8%），文档中注明 SPSS 数值会不同、两口径均合法。
- **黄金验证**：纯 Python EM 与 numpy 独立实现 μ 差 2.2e-9、Σ 差 1.7e-8、d² 差 1.1e-8、df 一致；EM 估计对完整数据 MLE 的恢复在抽样误差内；两模式 df 手算=5；MCAR 200 次拒绝率 6.5%（p 中位 .47）、MAR 200 次检出率 98%。开发中修掉一个真 bug：E 步 Σ_mo 取列误用局部下标（`sig[a][b]`→`sig[a][o[b]]`），修复前 Σ 最大偏差 2.3、MAR 场景 500 次不收敛。
- **健壮性**：无缺失只出描述统计不出检验；整列全缺失、Σ 奇异（总分与题项同列）、EM 500 次不收敛、文件不存在/空文件/坏 scales/坏 alpha 统一中文 rc=1；整行全缺失剔除计数；UTF-8/GBK 自适应；纯标准库。
- **边界**：工具只检验与给处理建议，**不做插补**（拒绝 MCAR 时指引 SPSS 多重插补/R mice/FIML）；明确声明检验基于连续多元正态假设、Likert 谨慎解读、不显著≠证明 MCAR、任何处理不得改动真实作答。
- **菜单与计数**：菜单 16→17 项（16 处步骤标签同步 /17，主屏补提示行）；工具脚本 17→18 个；stats 子包不变（复用 dataio/linalg/mathx）。
- **文档**：stats-guide 描述统计节加"缺失值分析与 Little's MCAR 检验"方法学小节（三机制/判读/红线/SPSS 口径差异）、data-analysis-auto 新增第 1.5 步并修订 2.3 均值替代旧说法、START 工具清单与用法、README 工具清单/目录树/版本轮换、AGENTS 文件地图、QUICKSTART 菜单、e2e-test 测试65、本文件。
- **验证**：`full_e2e.py` 448 → **475 项全过**（+27）；黄金模拟与 16 组 CLI 端到端场景全绿；`consistency_check.py`、`doubao-skill/validate.py`、全量 py_compile 全绿。

**v1.65 GB/T 7714 参考文献格式化闭环版（承接 v1.64 产物链核对发现的唯一断档；完整版，doubao-skill 本轮无改动）**
- **背景**：v1.64 三轮演练对照 `templates/paper-outline.md` 逐节核对"论文每个产物都有工具出口"，结果章要素已齐备，唯一定稿断档是参考文献著录——学生手里有 paper_search 导出与文献整理表，却要手工按 GB/T 7714-2015 拼 [J]/[D]/[EB/OL]，作者截断、卷期页码、DOI、全角半角极易出错。
- **新增 `tools/reference_formatter.py`（菜单第 16 项；工具脚本 16→17、菜单 15→16）**：吃三类题录 CSV（paper_search 导出、literature_organizer 整理表、`--save-template` 生成的 13 列模板，模板附 4 个示例行），输出顺序编码制 [n] 编号文本（默认 `<输入名>_参考文献.txt`）。支持 [J][M][D][C][N][EB/OL] 六类；作者解析兼容国标式"HENSELER J"、APA 式"Wang, Y., Li, M."、顿号/分号/&/中文逗号、全名式（Jörg Henseler）与机构作者，1-3 人全列、≥4 人列前三加"等/et al"，欧美著者姓全大写、名缩写不带点；`--name-case 2025` 切 GB/T 7714-2025 新口径（外国作者姓氏首字母大写），`--fullwidth` 全角标点，`--no-number` 去编号，`--access-date` 指定电子资源引用日期；DOI 尾随著录；缺字段以【待补：xx】文中标注并在控制台逐条警告（不静默伪造），文件不存在/编码失败/0 数据行/无题名列/不可判类型一律中文提示 rc=1；UTF-8/GBK 自适应；纯标准库、不联网、不生成文献（红线写进输出提示与源码断言）。
- **整理表污染自愈**：organizer 启发式偶发把出处尾部并入标题列（如"李四. 题名. 刊名, , 48(3): 页"丢年份），工具识别卷期页污染后用"原文出处"自由文本还原纯题名（中文"作者. 题名. 刊名, 年, 卷(期): 页"与英文 APA 前缀均覆盖），黄金用例锁定。
- **文档**：`workflows/writing-guide.md` §四重写（六类著录格式＋工具用法＋口径开关＋红线）、START 工具清单与用法、README 工具清单/目录树/版本轮换（448 项）、AGENTS 文件地图（17 脚本）、paper-outline 参考文献节、PROJECT_PLAN §十五、e2e-test 测试64 同步。
- **验证**：黄金用例（作者格式化 10 例＋出处解析/题名还原＋六类著录＋全角）全绿；CLI 端到端 16 组场景（paper_search 表、整理表、模板生成回填、GBK、五类 rc=1、全角/无编号/2025 口径）全过；菜单 16 项交互冒烟（含模板生成→回填）通过、测后产物清理干净；`full_e2e.py` 427 → **448 项全过**（+21：纯标准库、菜单接线与 16 项制、模板与六类著录黄金串、APA 截 et al、整理表还原、全角/2025/GBK、五类硬失败、红线声明、文档接线）；`consistency_check.py`、`doubao-skill/validate.py`、全量 `py_compile` 全绿。
- **范围控制**：doubao-skill 轻量版本轮无改动（参考文献工具属完整版电脑侧能力，stage-9 维持 Zotero/手工口径）；黄金用例、CLI 测试与补丁均在维护者侧 `_scratch`，不入库。

**v1.64 全流程三轮演练健壮性闭环版（ROADMAP 挂账"走流程生成模拟论文、收 bug、循环至少三次"；完整版，doubao-skill 本轮无改动）**
- **背景与三轮演练**：ROADMAP v1.x 挂账要求维护者亲自从学生视角把全流程跑通至少三轮、收集并修复流程 bug。本轮在维护者侧建立端到端演练框架（不入库）：第一轮标准路径 29 步（15 个菜单项＋多种样本量/效应量组合）；第二轮边界与坏参数 34 步（GBK 编码、3% 缺失、n=120/40、无 scales、非参数/Spearman/偏相关/Mahalanobis/EFA、约 15 组坏参）；第三轮修复后全新目录干净重跑，并对照 `workflows/data-analysis-auto.md` 与 `templates/paper-outline.md` 逐节核对"论文每个产物都有工具出口"。
- **缺陷①模型图契约矛盾（P0）**：菜单引导只有 X、Y 两个变量的学生出"简单模型图"，但 chart_generator 的 simple 实为 3 变量简单中介、2 变量必被拒（菜单↔工具契约矛盾）。新增 `--type direct`（2 变量 X→Y 单箭头；系数缺失按既有约定以 0 占位并在控制台提示，不伪造显著性）；chain/simple/direct 变量数不符时不硬画，给"该用哪种类型"的交叉引导；菜单按 n=4/3/2 自动选型，并对 1 个、5+ 个变量前置中文守卫；工作流文档第 10 步补 direct 用法与三型对应表。
- **缺陷② scales 错配静默退化（P0，四工具统一）**：显式传 `--scales 不存在.txt` 时 auto_stats 静默退化为全量分析；题项与数据列不匹配时信度 α/ω 与量表总分被静默按部分题计算——这是学生侧最常见的隐性错数来源（列名拼写、题号不一致、用错数据版本）。auto_stats、data_cleaner 在分析前硬校验文件存在与逐题列名匹配；item_analysis 与 validity_cr_ave 的 HTMT 模式同步加固；校验逻辑下沉为 `stats/dataio.py` 的 `find_missing_items()`/`assert_items_exist()` 单一实现，三处复用，auto_stats 保持 <300 行薄编排器架构约束。
- **缺陷③清洗器坏参数出英文 usage（P1）**：`--min-seconds abc` 触发 argparse 英文 usage 与 rc=2。四个数值参数改为字符串手工校验，统一中文提示并校验取值范围（min-seconds≥0、longstring≥1、low-sd≥0、max-missing∈[0,1]），失败 rc=1。
- **缺陷④效应量坏参"假成功"（P1）**：effect_size 共 16 处参数校验只打印 ✗ 后 return、进程退出码仍为 0，自动化流水线会把失败当成功。统一改为 raise ValueError、main 捕获后 `sys.exit(1)`。
- **缺陷⑤文献整理空跑"假成功"（P1）**：空文件读出 0 篇仍导出空整理表并报成功；读取失败（load 返回 None）时 main 直接 return 也是 rc=0。统一为中文提示＋rc=1（loader 已打印具体原因的位置只补退出码，不重复报错）。
- **验证**：`full_e2e.py` 414 → **427 项全过**（新增 direct 出图与误型引导、四工具 scales 缺失/题项错配两类硬失败、清洗非数字与越界、效应量坏参与缺参、空文献共 13 项；并把 2 条旧断言收紧到新契约：效应量坏参由 rc=0 改 rc=1、整理器空表由弱断言改 rc=1 且含具体原因）；三轮演练 29/29、34/34；`consistency_check.py`（90 个 CLI 开关/25 个 csv 后缀）、`doubao-skill/validate.py`、全量 `py_compile` 全绿。产物链核对结论：论文大纲结果章每个要素均有工具出口，唯一断档"GB/T 7714 参考文献格式化"挂账下一版（v1.65）。
- **范围控制**：16 个工具脚本/15 个菜单项数量不变（仅扩展与加固既有工具）；doubao-skill 本轮无改动；演练脚本、补丁与产物均在维护者侧 `_scratch`，不入库。

**v1.63 高频量表库再扩充版（29→39 组、10→11 大类；完整版，doubao-skill 本轮无改动）**
- **背景**：v1.59/v1.60 两轮把量表库从 16 组扩到 29 组，但学生高频选题仍有一批常用量表缺失：坚毅/自我同情/心理资本等积极品质，交往焦虑/人际信任/社会比较等人际过程变量，错失焦虑等新媒体行为，经验性回避等 ACT 过程变量，以及教育情境最高频的学习投入与学业倦怠。本轮一次性补齐 10 组，全部硬事实联网多源核查。
- **新增 10 组（按类别）**：二、自我与人格——坚毅 Grit-S（Duckworth & Quinn, 2009，8 题：兴趣一致性/坚持努力各 4）；三、社会与人际——交往焦虑 IAS（Leary, 1983，15 题，反向题 3/6/10/15，总分 15-75）、人际信任 ITS（Rotter, 1967，25 题，总分 25-125、中值 75，约半数反向题）、社会比较倾向 INCOM 中文版（王明姬、王垒、施俊琦, 2006，11 题，α约.88）＋上行社会比较 6 题版（白学军等, 2013 译，α约.84-.89，新媒体研究最常用）；四、问题行为——错失焦虑 FoMOs（Przybylski 等, 2013，10 题单维；李琦、王佳宁、赵思琦、贾彦茹, 2019 中文版）；六、积极心理与觉察——自我同情 SCS-SF（Neff, 2003 长版 26 题；Raes 等, 2011 简版 12 题 6 成分，三个负性成分反向计分，简版依建议只用总分；中文版陈健等修订）、积极心理资本 PPQ（张阔、张赛、董颖红, 2010，26 题 4 维）；八、压力、应对与自我调节——接纳与行动问卷第二版 AAQ-II（Bond 等, 2011，7 题，无反向题，总分 7-49；曹静、吉阳、黄峥, 2013 中文版 α约.88）；新开十一、学习心理与教育情境类——学习投入 UWES-S（Schaufeli 等, 2002，17 题：活力6/奉献5/专注6；方来坛、时勘、张风华, 2008 与李西营、黄荣怀, 2010 中文版）、学业倦怠（主推连榕、杨丽娴, 2005 本土《大学生学习倦怠调查量表》20 题：情绪低落8/行为不当6/成就感低6，总α约.86；备选 MBI-SS 15 题，两表题项锚点不同、不可混用，MBI-SS 有 Mind Garden 商业授权要求）。
- **事实纪律（不凭记忆报数）**：作者年份、题数、维度构成、反向题号、评分锚点、中文版出处与信度逐条多源核查；存在版本分歧的全部显式标注"以所引题本原文为准"——PPQ 有 7 点（26-182）与 5 点（26-130）两种题本、韧性/希望的 7/6 题分配记载不一、反向题常见为 8/10/12/14/25；FoMO 有 10 题单维原版与国内 8 题二维改编版；UWES-S 原版 0-6 频率、中文版多为 1-7 同意度；连榕量表"成就感低"维度为正向表述、需反向计分；Grit-S 中文修订者仅标"Li 等, 2018"并提示核对题本，不杜撰全名。
- **工程做法**：插入与重编号由维护者侧确定性脚本完成（按类别锚点插入后，全文 `### N.` 小节统一重排为 1–39），不手工改号、不留断号；新类别"十一、学习心理与教育情境类"置于"量表选择原则"之前。
- **验证**：`full_e2e.py` 404 → **414 项全过**（10 组新量表各 1 条硬事实断言，编号/大类断言更新为 39/11 并含新类别标题与 38/39 小节）；`consistency_check.py`、`doubao-skill/validate.py` 退出 0；本轮无工具脚本改动（16 脚本/15 菜单项不变）；量表正文不附题目，版权与授权纪律不变。

**v1.62 现代信效度指标补齐版（McDonald's ω ＋ HTMT；完整版，doubao-skill 本轮无改动）**
- **背景**：JASP 信度分析默认同时给 α 与 McDonald's ω，越来越多评审要求报告不依赖"各题等载荷"假设的 ω；区分效度方面，Henseler、Ringle 与 Sarstedt（2015）的模拟证明 Fornell-Larcker 准则对区分问题不敏感，HTMT（异质-单质比率）成为当代标准，而 v1.60 的效度工具只能由 CFA 载荷算 CR/AVE、并建议学生"去 CFA 软件补报 HTMT"——没装 AMOS/JASP 的学生卡在这里。本轮把两个指标在纯标准库内补齐，且不替代 CFA 软件的正式口径（全部标注复核路径）。
- **McDonald's ω 进入自动统计（P0）**：`tools/stats/reliability.py` 新增 `mcdonald_omega()` 与单因子主因子法 `_paf_one_factor()`（共同度 SMC 初值 1−1/diag(R⁻¹)、奇异阵退化、迭代至 1e-9、Heywood 截断），ω=(Σλ)²/[(Σλ)²+Σ(1−λ²)]；`auto_stats.py` 信度节在每个量表 α 之后自动打印 ω 与等级、ω 明显低于 α 时提示查反向题/单维性，`_信度分析.csv` 新增 `McDonald_ω` 列；题数<3/完整样本<10/零方差/奇异阵给中文"本次未算"而非崩溃。
- **HTMT 区分效度模式（P0，菜单13选2）**：`validity_cr_ave.py` 新增 `--htmt 数据.csv --scales scales.txt [--boot 2000 --seed 20260918 --only-scales A,B] [--csv-out]`：自动反向计分（scales.txt 的 (R) 标记）、全部题项完整样本上逐对构念算 HTMT（跨构念题项 |r| 均值 ÷ √两块内 |r| 均值之积）、固定种子 Bootstrap 百分位 95%CI，点估计 .85/.90 双门槛与"CI 上限<1"推断标准分别判读，块内平均相关≤0 直接提示反向题可能没标对，导出 `_HTMT区分效度.csv`（构念A/B/完整N/HTMT/CI下限/CI上限/点估计判定/CI判定）；菜单第13项改为模式1（CR/AVE 原流程）/模式2（HTMT）二选一引导。
- **黄金对照（P0）**：独立 numpy 实现（脚本不入库，维护者侧 `_scratch`）——①已知载荷 .60–.78 的 n=20000 单因子模拟，PAF-ω 还原理论 ω 误差<.001；②本质 τ 等价数据 ω≈α（差<1e-4）；③正交双因子 n=4000 HTMT=.024、CI 上限 .059<1，单因子拆两半 HTMT=.998、CI 上限 1.014≥1，因子真相关 r=.6 时 HTMT≈.60，判定方向全部正确；④新增夹具 `tests/test-data/demo_htmt.csv`（正交 4+4 题，n=220，5 点 Likert）与 `htmt_scales.txt`，锁定 ωA=.744、ωB=.716、HTMT=.088；stdlib 实现与 numpy 点估计逐位一致（.088）。
- **红线与边界（P1）**：ω 标注为"单因子 PAF 快速预览，多维量表按因子分别报、正式以 JASP Reliability→ω 或 lavaan+semTools 的 CFA-ω 复核"；HTMT 标注为"Likert 题 Pearson 相关口径，有序类别的正式 polychoric HTMT/HTMT2 在 lavaan/SmartPLS 复核"；不达标（HTMT≥.90 或 CI 上限≥1）要求如实报告并做合并构念/删交叉题/重构模型处理，明确禁止删题凑数；`--boot 0` 只给点估计时总结语不冒称 CI 通过。
- **健壮性（P1）**：缺 `--scales`、可计算量表不足 2 个、数据/配置文件不存在、`--boot` 负数、某量表题项缺失均中文提示退出 1 且无 Traceback；HTMT 反向计分不变性测试（B 构念整体 6−x 反转并标 (R)，点估计保持 .088）。
- **文档（P1）**：stats-guide 第二节新增"McDonald's ω"小节（公式/门槛/JASP·lavaan 路径/多维注意）、信度 CSV 列与论文表述同步；第三节 HTMT 由"建议去 CFA 软件报"改为完整用法块（命令/口径/判读/红线）；data-analysis-auto 流程图、第3步信度、第4步效度、质量闸清单同步；START、README（当前版本摘要+v1.61 压缩为上一版本、断言计数 234→404）、QUICKSTART、菜单主屏提示同步；tests/e2e-test.md 新增测试61与回归清单 #17/#17b。
- **验证**：`full_e2e.py` 390 → **404 项**（新增 ω 入列/数值关系/stdout/纯标准库 PAF、HTMT 正交夹具黄金值/CI 上限/CSV 导出与表头/反向题等价/同因子阴性判失败/缺 scales·单量表·负 boot 三守卫/菜单 HTMT 接线共 14 项）全绿；`consistency_check.py`、`doubao-skill/validate.py` 退出 0；工具脚本仍为 16 个、菜单 15 项（本轮扩展既有工具，未新增脚本）。

**v1.61 Skill 补丁集版（doubao-skill 升级 v1.3 → v1.4；完整版本体工具无改动）**
- **背景**：维护者侧走查发现手机能力边界自相矛盾（"手机能做阶段 0–7"但阶段 7 预处理/清洗实际必须回电脑）、语气文件名残留人设称谓、统计阈值被写成一刀切、量表授权与伦理要求未本地化、留痕靠学生手动、以及 AI 能否起草正文的口径在"一律不代写"与实际需求之间过严。经用户确认形成 8 个补丁（编号 1/5/6/7/8/9/10/11），其中补丁 10 经用户口头授权调整为"AI 可直接生成可编辑正文草稿，但用户须万分小心、自行修改编辑，AI 必须报告风险"。
- **补丁1（P0）手机能力边界修正**：SKILL.md"手机能力边界"整节、mobile-guide.md、tools.md、build_mobile_single.py 头部三句话统一改为四类划分——阶段 0–6 手机可全程；阶段 7 收数/监控/备份/导出可手机，预处理、五指标清洗、反向计分、量表总分必须回电脑；阶段 8 全部统计分析与出图必须回电脑；阶段 9/11 手机起草+电脑定稿（排版、三线表、参考文献、查重、AI 检测、PPT 定稿回电脑）。三条出路（借电脑按 stage-8 做/结果带回手机解读/找导师申请机房）与"不编造手机替代方案（不教表格 App 硬算 α/KMO/中介）"保留；设备交接提醒从"第 7 阶段结束前"提前到"阶段 6 开题通过后"。
- **补丁5（P1）规则优先级**：SKILL.md 顶部与手机合并单文件开头新增"规则优先级（冲突时按此顺序）"：1 coaching-protocol 2 companionship 3 stages/stage-N 4 encouragement-guide 5 语气文件（只改说法不改规则）6 参考常识；任何语气、任何难度不得违反第 1、2 层。
- **补丁6（P1）语气文件去人设化命名**：git mv gentle-sister→gentle-patient（gentle-patient.md）、strict-ceo→concise-direct（concise-direct.md）、puppy→lively-warm（lively-warm.md）（default.md 不变；文件内标题本已为"温和耐心/简洁直接/活泼热情/自然"，无需改标题）；SKILL.md、coaching-protocol、encouragement-guide、build_mobile_single.py、validate.py、full_e2e.py 全部引用同步；CHANGELOG 历史条目保留旧名。
- **补丁7（P1）统计阈值标争议**：stage-8-analysis.md 第三节开头加"判读标准说明"块引用（阈值是常见经验范围，不同教材与领域有差异，以导师意见、量表原文、软件输出和领域惯例为准；α 过高、Harman 偏低、中介 CI 含 0 先排查再下结论，不机械套数）；第 2 步 α/分半、第 3 步 KMO/CFA、第 4 步 Harman、第 7 步中介四处"助手动作"各加"[阈值因教材而异，结合导师意见]"。
- **补丁8（P1）量表授权分级与伦理本地化**：stage-4-scale.md 第 5 步改为"授权与使用许可（分级处理）"：开源/免费研究使用按要求引用；需邮件申请取书面许可留存回信；需修订/翻译授权另走流程；商业或明确限制不进入正式发放。数据保存年限、未成年人同意、敏感主题流程以本校文件为准；12356 与 120/110 使用前核实当前信息。
- **补丁9（P1）进度卡更新块**：doubao-skill/references/coaching-protocol.md 第六节续接协议后、完整版 core/coach-rules.md 第五节同步新增"每阶段结束由 AI 生成'进度卡更新块'"：过闸后 AI 主动输出含过闸日期/本阶段产出/关键决策/下次从哪继续/当前卡点/要提醒下一个 AI 的事的可粘贴 markdown 模板，降低留痕失败率。
- **补丁10（P1，用户授权）AI 正文边界调整**：academic-norms.md 在"AI 使用声明"前新增"AI 可做与不可做（边界清单）"——AI 可做：检索式/题录整理、问卷骨架与知情同意指导语草稿、PPT 大纲/检查清单/表格框架、改语言查逻辑模拟问答，以及**经学生明确要求并基于其真实材料（数据、软件输出、文献笔记）直接生成可编辑的论文正文草稿（待修改草稿，不是可提交稿）**；每次生成正文草稿必须同时做三件事：①标注"AI 草稿、不可直接提交"②逐段报告风险（编造文献/数字/信效度、与真实数据或软件输出不符、各校 AI 规定与 AIGC 检测差异、重复率、答辩讲不清；不确定标 [需核实]）③给核对清单要求逐句核对并用自己的话改写。AI 不可做：生成量表题目、编造文献数据信效度学校规定、无真实材料凭空生成结果/结论/整章、替做选题量表样本方法等高风险决定、把草稿包装成可交定稿。直接复制 AI 段落不改不核实就提交，仍按全文代写/抄袭红线处理。SKILL.md（不应触发/复合请求/铁律1/文件树）、coaching-protocol（角色边界/可以做/不可以做/紧急红线/门禁）、stage-9（第3步/检查点/红线/质量闸）、stage-checklist、ai-basics、faq、self-test，以及完整版 core/coach-rules、core/coaching-protocol、core/ai-literacy、workflows/writing-guide、psychology/ethics、CONSTITUTION、AGENTS、START、两份行为自测同步对齐。
- **补丁11（P2）安装与获取**：mobile-guide.md 方式二后补"合并单文件在部分平台可能因长度被截断；若 AI 只读到一半，改用'分步喂'：先发 SKILL.md，按提示逐段补发"；tools.md 完整版获取句改为"获取方式问维护者；拿到后核对版本号与校验值，不从非官方渠道下载"。
- **测试与校验**：validate.py 更新语气文件名清单与手机边界关键词断言（旧词"手机做不了/第 8 阶段只能做一半"断言替换为新词并反向断言旧词不得出现），新增进度卡更新块与 AI 草稿边界清单关键词检查；full_e2e.py 更新 v12 边界一致断言与合并单文件断言，新增 v1.61 断言块 14 项（改名文件存在/旧名清零/阈值注 4 处/授权分级/进度卡/草稿边界三处同步/截断提示/校验值提示）；behavior-self-test.md、e2e-test.md 测试 7 预期行为改为"拒绝直接交定稿但可给可编辑草稿+风险报告"。
- **验证**：`doubao-skill/validate.py` 退出 0；`tests/full_e2e.py` 全过；`tests/consistency_check.py` 退出 0；合并单文件重新生成且内容指纹同步校验通过；发布前在项目外干净解压副本复验。

**v1.60 测量学闭环补齐版——把自编/修订量表从"专家内容效度→预试项目分析→EFA/CFA→聚合区分效度→样本量"整条链路的工具缺口补齐**
- **背景**：v1.59 补齐了隐私、异常值与效应量，但问卷测量学链条仍有四个教材必做环节缺工具或只覆盖一半：①CFA 后的组合信度 CR、平均方差抽取 AVE 与 Fornell-Larcker 区分效度要手算；②预试问卷的决断值 CR（高低 27% 独立样本 t）auto_stats 未给（只有 CITC/删题 α）；③开题样本量只支持相关/回归/R²增量/ANOVA，缺最常用的独立两样本 t 与配对 t；④自编量表在结构效度之前的**内容效度**（专家 CVI）完全空白。本版新增 3 个工具、扩 1 个工具、扩充量表库并加固共同方法偏差方法学；全部纯 Python 标准库（绘图可选），统计口径与 `psychology/stats-guide.md` 严格对齐，菜单由 12 项增至 **15 项**。
- **【聚合/区分效度】新增 `tools/validity_cr_ave.py`（菜单第 13 项）**：由 CFA **标准化因子载荷**算组合信度 CR、平均方差抽取 AVE、√AVE，并结合因子间相关做 Fornell-Larcker 区分效度判定（CR≥.70、AVE≥.50 常用门槛，.36–.50 且 CR 良好可接受需说明；√AVE 应大于该因子与其它因子的 |r|）；支持 `--factor "名称=载荷,..."`（可重复）、`--corr "A,B,r"`（可重复）或 `--loadings-csv/--corr-csv`，导出 `_聚合区分效度.csv`；载荷 ≥1/未知因子等坏参中文守卫。文档注明**单因子测量模型下 CR 与 McDonald's ω 数学等价**（多维按因子算即各因子 ω），并建议在 CFA 软件补报更现代的 HTMT；载荷必须来自真实测量模型，不得为达标手改。
- **【预试项目分析】新增 `tools/item_analysis.py`（菜单第 14 项）**：复用 stats 包（不重复实现统计量），按量表总分取高/低各 27%（比例 `--group` 可在 .10–.50 调），逐题给等方差独立样本 t 的**决断值 CR**（含 df、双侧 p）、均值标准差、CITC、删题后 α 与"保留/建议结合理论讨论删改"，标记 CR 不显著、|CR|<3、CITC<.40、删题 α 升高>.02，导出 `_项目分析.csv`（与 auto_stats 的 `_题项分析.csv` 区分）；只提示不替学生删题，正式数据不反复套用预试删题标准。
- **【样本量补 t 设计】`tools/sample_size.py`（菜单第 8 项扩为 6 设计）**：利用 df1=1 时 **t²=F 的精确等价**复用已黄金验证的非中心 F 引擎——独立两样本 t（等组，λ=d²N/4，按每组 n 搜索保证总 N 为偶数）与配对/单样本 t（λ=dz²N，N 为配对数）；d/dz 基准 .2/.5/.8，结论行给独立设计每组人数，坏参中文守卫。最小 N 经 `scipy.stats.nct` 逐位核对（差 0）：独立 d=.5 总 128（每组 64）、d=.2 总 788、d=.8 总 52；配对 dz=.5 N=34、.2 N=199、.8 N=15。
- **【内容效度 CVI】新增 `tools/content_cvi.py`（菜单第 15 项）**：自编/修订量表请专家小组（至少 3 人、建议 5–10 人）对条目按 1–4 评相关性，逐条算 **I-CVI**、机遇一致 Pc=C(N,A)·0.5^N 与**校正 κ\***=(I-CVI−Pc)/(1−Pc)，量表层算 **S-CVI/Ave**（≥.90）与 **S-CVI/UA**（≥.80），按 Lynn(1986)（3–5 人 I-CVI 须=1.00、≥6 人 ≥.78）与 Polit & Beck(2006)/Polit 等(2007) κ\* 分界（>.74 优秀）给保留/修改/重审建议，导出 `_内容效度CVI.csv`；非整数/越界/缺文件/空条目中文守卫；引用成熟量表免做。
- **【量表库 24→29 组、9→10 大类】`psychology/scale-library.md`**：新增 PANAS 正负性情绪、IRI-C 人际反应指针（共情）、GQ-6 感恩、GHQ-12 一般健康、PSQI 匹兹堡睡眠质量（新开"睡眠与心身健康类"）；全部小节确定性重编号 1–29，题数/维度/计分/分级/中文版出处逐条联网多源核查，口径有出入处（如 IRI-C 22 题构成、PSQI 中文分界 ≥8、PANAS 中文版题数差异）明确标注"以所引原文/题本为准"，不杜撰。
- **【开题/模板接线与健壮性】**：`proposal-template.md` 研究工具增"自编/修订量表先专家 CVI、再预试项目分析"条目、数据处理信效度条补 CVI/项目分析/CR-AVE；`proposal-guide.md` 答辩问答增"自编量表怎么保证质量"。效度工具补负标准化载荷警示（反向题未重编码会低估 CR）与 CSV 文件缺失的中文友好报错（不抛 Traceback）。
- **【共同方法偏差方法学加固】`stats-guide.md` 第四节、`data-analysis-auto.md` 第 5 步**：明确 Harman 单因子是最宽松的事后检验、不能证明无 CMV；补**程序控制为首选**（匿名、变量分区、反向题打乱、量表顺序随机、多时点/多来源，Podsakoff 等 2003）与更强统计控制（CFA 潜在方法因子 ULMC、标记变量法 Lindell & Whitney 2001、偏相关校正），标明工具边界（auto_stats 只做 Harman，ULMC 需 AMOS/Mplus/lavaan），措辞改"未发现严重共同方法偏差"，质量闸要求写明已采取的程序控制。
- **验证**：三个新工具的统计量均先用本机 scipy/numpy 黄金对照（CR/AVE 五组随机载荷+固定黄金值；决断值 CR `ttest_ind(equal_var=True)` 稳定排序；t 样本量 `scipy.stats.nct`；CVI 用 R `contentValidity` 包手册算例 5 专家×4 题逐位一致）再固化为 e2e 硬事实断言；新增测试夹具 `tests/test-data/demo_cvi.csv`。`full_e2e.py` 343 → **377 项全过**；`consistency_check.py` 退出 0（**16 工具脚本/含 stats 共 26 实现模块/88 CLI 开关/24 csv 后缀**无漂移）；`doubao-skill/validate.py` 通过。发布前在项目外干净解压副本复验。
- **边界（如实标注）**：CR/AVE、CVI 都是对既有专家评定/CFA 输出的量化整理，不产生数据、不能替代专业判断；载荷与专家评分必须真实，不得为凑阈值改数；CVI 的 κ\* 基于"每位专家以 .5 概率评相关"的机遇假设；t 样本量为 t²=F 精确等价但仅适用简单两均数/配对设计，纵向、实验、多层模型仍需专门估算；新增量表分级阈值以所引原文与手册为准。豆包轻量 Skill 本版保持 v1.3（新工具属完整版本体能力，未改 Skill 阶段）。

**v1.59 数据隐私与统计严谨性增强版——外发前先去标识化、多元异常值只标记不删除、效应量必报可复核，并把量表库与网页范例做厚**
- **背景**：在 v1.58 基础上，针对问卷实证全流程三类高频风险加固：①数据外发（发给 AI/上传/给外校）前的隐私泄露；②多元异常值容易"一删了之"、人为改变结论；③结果章只报 p 值、缺效应量与置信区间，且量表候选面偏窄、学生面对一堆检验不知该选哪种方法。本版新增 2 个工具、1 项统计筛查、1 个网页范例并扩充量表库；全部纯 Python 标准库（绘图可选），统计口径与 `psychology/stats-guide.md` 严格对齐。
- **【隐私闸】新增 `tools/anonymize_data.py`（菜单第 11 项）**：外发前自动假名化或删除直接标识符（姓名、学号、手机、邮箱、身份证、微信/QQ、IP、住址；表头没写明的按内容模式识别），对性别/年级/专业/生源地等准标识符组合做 **k-匿名体检**；只读原文件、另存 `_去标识化.csv`，**绝不覆盖原始数据**（同名同路径直接拒绝）；可生成单独保管的"假名对照表"用于前后测配对（`--no-key` 则不可复原）；`--dry-run` 只体检不写盘，`--columns` 手动指定，UTF-8/GBK 都能读，识别不到标识符时友好退出。接线 `core/ai-literacy.md`、`workflows/data-analysis-auto.md`（外发前隐私闸步骤）、START/QUICKSTART/README。
- **【多元异常值只标记不删除】`tools/stats/regress.py` 新增 `mahalanobis_outliers()`/`_chi2_critical()`，`auto_stats.py` 新增 `--mahalanobis`/`--mah-alpha`**：按 Mahalanobis D² 与 χ² 临界值（默认 p<.001）筛查多元异常个案，**只在报告中标记、不自动剔除**，并给敏感性分析建议（含/不含异常值各跑一次、结论是否翻转如实对比），避免为"好看"删数据。
- **【量表库扩充 16→24 组、9 大类】`psychology/scale-library.md`**：新增 DASS-21 抑郁-焦虑-压力、压力知觉、应对方式、情绪调节（过程模型）、自我控制、核心自我评价、生活满意度、基本心理需要（SDT）8 个小节，并在手机/网络成瘾下补 **BSMAS 社交媒体成瘾量表**；每条题数、计分、分级阈值均经联网核查，口径有出入处明确标注"以所引原文/手册为准"，不杜撰。
- **【第 4 个网页范例】`templates/网页范例/04-统计方法选择器/`**：单文件、零 CDN/外链、系统字体、青灰配色的问答式决策树（研究问题→变量类型→组数→正态/方差齐性→是否配对），输出推荐方法＋前提条件＋应报告的统计量与效应量阈值＋脚本命令＋JASP/SPSS 路径＋数字留空的论文句式＋数据真实性提醒；JS 数据（10 问/26 结果）与渲染逻辑分离，底部静态效应量速查表；带可见横幅与 HTML 注释双标注"范例，请勿直接使用"。
- **【效应量换算复核】新增 `tools/effect_size.py`（菜单第 12 项）**：8 个子命令——`d`（均值标准差→合并 Sp、Cohen's d、Hedges' g、d 的 95%CI）、`d-t`、`paired-d`（配对 d_z）、`r`（r＋n→Fisher z 区间并换算 d）、`r-t`、`eta`（F→偏 η²，给平方和再出 η²/ε²）、`v`（χ²→Cramér's V，2×2 给 φ）、`convert`（r↔d）；阈值 r .1/.3/.5、d .2/.5/.8、η²/ε² .01/.06/.14、V .1/.3/.5 与 stats-guide、`compare.py` 一致；固定页脚强调数字须来自真实检验输出、不显著也如实报告、不得为凑阈值反推改数；坏参数中文提示、不抛 Traceback。
- **验证**：effect_size 全子命令＋边界用本机 scipy/numpy 交叉核对（t↔r 恒等式、Fisher 区间端点一致）后固化为 e2e 断言；选择器经 Node.js 语法检查、DOM 桩遍历（10 问 26 结果全可达、无悬空跳转）、无头 Edge 逐结果页渲染与截图核对；本分支合并 master v1.58 后 `full_e2e.py` **343 项全过**、`consistency_check.py` 退出 0（13 脚本/23 模块/79 开关/21 csv 后缀无漂移）、`doubao-skill/validate.py` 通过；菜单最终 12 项（卡片 10、去标识化 11、效应量 12）。
- **边界（如实标注）**：去标识化降低再识别风险、不能替代正式伦理审查，低基数准标识符组合仍可能被重识别，k-匿名体检结果需人工判读；Mahalanobis 只标记不替代专业判断，剔除与否须有依据并做敏感性分析；effect_size 只做换算、不产生数据，显著性与方向以真实检验为准；新增量表的分级阈值以所引原文与手册为准。豆包轻量 Skill 本版保持 v1.3（新工具属完整版本体能力，未改 Skill 阶段）。

**v1.58 定位/陪伴/检索/卡片/手机原生适配版——把 AI 从"严肃导师"调成"好用的学习伙伴"，并把文献检索做厚、把手机版做真**
- **背景**：用户反馈五类问题：①鼓励两极（热情档太夸张、标准档太冷淡），AI 不该以老师自居、不该被工作流"喂台词"，应定位为工具并按学生状态与任务难度自然发挥；②整体定位偏"严肃老师"，需参考 AI 情感依赖/"数字伙伴"研究，构建稳定但不越界、不制造情感依附的陪伴；③文献与量表检索只搜一个词就下结论；④自动检索文献太少，应凑够约 90 篇并把重点做成手机友好的网页卡片（参考随包现成网页）；⑤手机版只是电脑版套壳，很多功能在手机上做不到，需真正原生适配。本版围绕这五点系统改造。
- **【定位与陪伴】新增 `core/companionship.md`**：明确 AI 是工具/学习伙伴（不是老师、不是心理咨询师、不是虚拟伴侣）；循证整理情感代偿（评价/人情/可及三成本）、刺激假说 vs 替代假说、情感依赖信号、拟人化监管（含我国关于不得向未成年人提供虚拟伴侣类服务的规定）与教育侧"脚手架非拐杖"原则；给出十条稳定陪伴原则、依赖信号与温和拉回对照表、未成年人条款、回复前自检与参考来源（用户点名的《数字伙伴：AI陪伴下的青少年情感补偿机制研究》等未能核得原文的来源如实标 [需核实]，不杜撰）。
- **【去老师化】**：`coach-rules.md`/`coaching-protocol.md`/`START.md`/`README.md`/`QUICKSTART.md`/`AGENTS.md` 与手机 skill 全面去掉"导师/老师"自称（学生真正的学校导师称谓保留）；"人格系统"改为"语气风格系统"，默认**自然、不套人设**，另留简洁直接/温和耐心/活泼热情三种语气（人设文件名保持 default/strict-ceo/gentle-sister/puppy 不变以免破坏校验），活泼热情档大幅降温、去除撒娇与依附暗示。
- **【鼓励自然化】重写 `core/encouragement-guide.md`**：删除逐句台词模板与"每轮至多一次肯定"式机械计数，改为只给原则（鼓励什么、何时鼓励、P0 不包装、挫折时刻协议），密度自然、不堆砌，结合学生状态与任务难度自由发挥；六个理论锚点（正强化/成长型思维/反馈干预/自我决定/建设性反馈/积极情绪）与规范出处全部保留。
- **【多词检索】**：文献与量表检索都改为"概念—检索词矩阵"：每个核心概念至少 3 组同义/近义/缩写/作者名词、中英文，概念内 OR、概念间 AND、跨多库；只有多词多库都查不到才能判"没人做过/没有现成量表"，单词 0 命中不再下结论。落到 `workflows/literature-auto-search.md`、`psychology/scale-library.md`、`templates/检索记录模板.md`（新增矩阵表与去重累计列）及预置 live 检索记录。
- **【≥90 篇候选池】`tools/paper_search.py` 重写**：`--query` 可重复、新增 `--queries`（分号/换行分隔）、`--source all`（OpenAlex+Semantic Scholar 双源）、`--min N`（逐词逐源翻页、按 DOI/规范标题去重直到 N 篇或库内穷尽；不足时明确提示补词，不静默成功）；纯函数化（split_queries/normalize_doi/dedup_key/merge_records/collect 可注入 fetcher 便于离线测试），完全兼容旧 `--query X --limit N`。口径说明：≥90 是题录/摘要层面的**候选池**，不是引用数，在此基础上精读 10–20 篇。
- **【重点文献卡片】新增 `tools/literature_cards.py`（菜单第 10 项）**：吃多个检索 CSV/整理表（UTF-8-sig/UTF-8/GBK 容错）、复用去重；精读/★标记优先，否则按被引对数＋近年＋综述/Meta＋`--focus` 相关度＋有摘要打分选出重点（默认约 15%，夹 8–30 篇）；生成与随包 `01-文献笔记网页` 同语言的**单文件、手机优先**卡片 HTML（sticky 搜索、分类 chips、⭐重点筛选、响应式 1/2/3 列、整卡可点弹 modal、42px 触控），CSS/JS 全内联无 CDN、数据放 JSON script 且转义、JS 用 textContent 防注入、页脚含来源/时间/"需核对原文、不构成引用依据、勿录隐私"。归入 `webpage-guide.md` 既有"①文献笔记网页"类型（不新增第六类）。
- **【手机原生适配】Skill 升 v1.3**：重写 `references/mobile-guide.md`，按手机真实能力给能力边界表与各环节原生做法（知网"全球学术快报"App、Scholar/PubMed 移动网页、PDF 拍照、问卷星 App、语音输入、卡片网页在手机生成/打开），新增手机↔电脑"设备交接单"，明确手机装不了 Python/虚拟电脑、不教手机表格硬算统计；skill 镜像同步陪伴/鼓励/多词检索/卡片/去老师化，合并单文件构建脚本纳入 companionship 并去老师化。
- **验证**：`tools/paper_search.py`、`tools/literature_cards.py` 离线端到端验证（合成数据、UTF-8/GBK、重复 DOI、强制精读、注入假 fetcher）；`consistency_check.py` 退出 0；`doubao-skill/validate.py` 通过；合并单文件重新生成（27 源文件、约 7.5 万字符）；`full_e2e.py` 断言同步演进并新增工具离线用例（详见本次提交）。发布前项目外干净副本复验。
- **边界（如实标注）**：90 篇为候选池目标，冷门窄题可能穷尽后仍不足，需写清各词各库命中数并与导师确认；卡片只做"先读哪篇"的导航，不替代精读、不构成引用依据；陪伴研究的部分二手来源（含用户点名论文）未能核得原文，已在 `companionship.md` 标 [需核实]，未补造；手机端统计等重活仍需回到电脑，本版只把交接与边界做清楚。

**v1.57 文献检索闭环版——把知网链路接进系统工作流，并把并行会话遗留的冲突与断链收口**
- **背景**：并行会话留下的未提交改动（`workflows/literature-auto-search.md` 重构、`tools/literature_organizer.py` 增强）在开发树里能跑，但**没有闭环接入系统**：工作流被重写成"知网可选功能＋决策配置＋实测附录"，却与本体规则、菜单入口、进度卡、发布包形态多处不接。本版做的是接线与收口，不新增大功能。
- **【P0 收口】删除"AI 代填图书馆账号密码"档**：重构稿新增的"档位A：AI 读取本地凭据文件（学生工作区内的一个 `.local.json`）代填并提交"，与 `CONSTITUTION.md` 第七条"不索取、不代填账号密码与验证码"、`core/ai-literacy.md`"账号密码绝不发给AI，也不要让它代填"、`core/coaching-protocol.md` 第二节"不可以做"、`tests/behavior-self-test.md` 用例 T25 四处**直接冲突**——等于在最高原则之外开了一个例外。已删掉该档，登录方式改为两档（学生自填／暂不登录），并把红线写进工作流正文（含"学生主动授权也必须拒绝""不读取任何凭据文件"）。开发仓库里真实存在的明文凭据文件已删除，`.gitignore` 增加凭据类兜底规则。
- **【门禁缺口】补"凭据代填红线"回归断言**：该冲突能一路通过四道检查，根因是**没有任何断言守这条线**。`full_e2e.py` 新增一组断言：全仓文档不得出现代填/凭据文件写法、工作流须写明"学生本人输入"与 T25、学生数据与凭据不得入库（`git ls-files` 反向核查）、预置文件必须已入库。
- **【发布形态缺口】预置文件差点进不了包**：新加的 `我的工作区/01-文献PDF/检索记录.md` 被 `.gitignore` 的目录级排除挡住（git 无法重新包含落在被排除目录里的文件），开发树看着存在、`git archive` 打出的包里却没有。已加白名单，并新增"预置文件已入库"断言防复发；同时按文件名排除**学生本人的知网题录**（属学生成果，不该随包分发给别的同学）。
- **【闭环接线】把文献工作流接进本体**：`core/coach-rules.md` 阶段2 补登（可选功能、登录交学生、检索式与命中数记入 `检索记录.md`、防假空白反查）；`menu.py` 第 5 项原本只提示"每行一篇的txt"，与增强后的整理器不符，改为明示可吃检索导出的标准 CSV；`我的工作区/先读我.md` 登记检索记录落点并新增"一条安全规矩（不放账号密码）"；预置 `检索记录.md`、`templates/检索记录模板.md`，进度卡新增"三之二、文献与检索留痕"节承接反查证据。
- **整理器健壮性（沿用并行会话成果并加固）**：`literature_organizer.py` 支持带中文表头的 CSV（paper_search 导出／知网导出／Excel 另存），列名兼容 标题/题名、年份/年、期刊/来源/刊名，自动分类关键词补英文。**修复其静默失败**：原实现的"识别列名"与"解析列名"用了不同编码，GBK 导出的中文 CSV 会被 utf-8 解成乱码、认不出列名，最后**静默导出 0 篇整理表**（学生会以为"去重成功"）。现改为识别与解析同编码，并对"有表头但解析不到任何一行"给中文提示而非沉默。
- **测试自身缺陷修复**：① `full_e2e.py` 阴性测试在 `tests/test-data` 内 `mkdtemp`+`copytree`，Windows 下被杀软/索引器锁住时抛 `PermissionError` **直接打断整个回归**（本次即被此打断一次），且 `rmtree(ignore_errors=True)` 会静默留下删不掉的残留目录；已改为在系统临时区建目录 + 小退避重试 + cleanup 兜底，并区分"删除挂起态"（句柄释放后自行消失，不判失败）与真残留（判失败）。② `tests/test-data/sample_literature_整理表.csv` 是整理器产物却被误跟踪，跑一次测试就变"脏"，且其内容随分类规则变化——已取消跟踪并 gitignore，新增断言只在临时目录校验整理器输出。
- **验证**：`full_e2e.py` 233 → **247 项**全量通过；`consistency_check.py` 退出 0；`doubao-skill/validate.py` 通过（skill 侧同步补"不许 AI 去读写着密码的文件"）；发布前在项目外干净解压副本复验。
- **边界（如实标注）**：知网链路实测附录来自项目作者本人 2026-09-17 的真机验证（图书馆→OA资源→知网→登录→检索→题录导出→整理成表），属**单校单次**环境，其他学校的入口与登录次数需按 0.3 的通用找法重新确认；本版未做真人多轮走查，v1.56.6 标注的"开新会话交另一个 AI 演练"仍是最高优先级遗留。

**v1.56.6 专业文档接线版＋行为自测补盲——把规则接到 psychology 专业层，并给自测体系做可追溯走查**
- **专业文档接线（延续 v1.56.4 的 workflows 接线）**：自查发现 `psychology/` 三份按需查阅文档同样对 core 规则零引用。三份顶部各加一条逐字一致的"使用约定"指针（声明专业参考不替代带教规则、冲突时从严）；`ethics.md` 补危机口径（12356、即刻危险 120/110、不做临床诊断只转介），`stats-guide.md` 补数据真实性 P0（不伪造、不为显著删改、不显著也是结果），`scale-library.md` 补"不在场标需核实、不凭记忆报数"。
- **行为自测补盲**：`tests/behavior-self-test.md` 新增 6 条用例——T33（长对话进入阶段 8 仍走三段式/过闸/守鼓励档，压测 v1.56.4 接线）、T34（单次 0 命中不等于空白、须换词反查留证）、T35（量表/分析方法等高风险决策不替学生拍板）、T36（学生坚持要做 P0 的二次处置：拒绝后不协助该部分但可继续其余）、T37（未成年人缺监护人知情同意/assent 为 P0）、T38（学生前后说法矛盾时列冲突请确认、不擅自选版本）。用例总数 32 → 38，编号连续。
- **首次可追溯走查并如实记录**：对 T1–T38 逐条做"用例↔规则条款"追溯，旧 32 条无"有用例无规则"断档；反向发现上述 4 处"有规则无用例"盲区并补齐。走查记录表明确标注本次为**构建者桌面静态走查、非真人/独立 AI 对话证据**，把"开新会话交另一 AI 演练"列为最高优先级遗留，不把自评冒充实测。
- **验证**：`full_e2e.py` 225 → **234 项**（新增专业文档接线、用例连续与新增场景、走查诚实标注 9 条）、`consistency_check.py` 退出 0；发布前项目外干净副本复验。
- **边界**：并行会话正在重构 `workflows/literature-auto-search.md`、`tools/literature_organizer.py`（文献检索迁移），其未提交改动不属本版、未卷入本版提交。

**v1.56.5 测试加固版——给 Welch ANOVA 补数值回归断言，锁死可翻转结论的 df2 缺陷**
- **背景**：v1.56.3 修复了 Welch ANOVA 分母自由度公式错误（旧式 df2=(k³−k)/(3D) 把分母自由度放大 k 倍、p 值系统性偏小，强异方差下可把不显著误判为显著），但 full_e2e 对 Welch 只有"字符串存在性"检查，公式若在未来重构中被改回错误式，现有测试拦不住。
- **新增数值基准断言**：`full_e2e.py` 统计段新增"Welch强异方差数值基准"，用 3 组各 n=8、第二组高均值且大方差的固定数据，直接调用 `stats.compare._welch_anova`，锁定 F=16.7979、df1=2、df2=12.5210、p=2.852e-4（容差 2e-3 / 2e-5）；基准经教科书公式另写与 scipy.stats.f.sf 三方一致核对。旧式错误式在该数据上得 df2≈37.56（恰为 k=3 倍）、p≈6.15e-6（偏小约 46 倍），会被新断言直接判失败。
- **验证**：`full_e2e.py` 224 → **225 项**全量通过；`consistency_check.py` 退出 0、skill 自检通过；发布前在项目外干净副本复验。

**v1.56.4 反馈规则接线版——让 v1.56 的反馈协议与鼓励系统真正落到每个阶段手册**
- **背景**：v1.56 在 `core/` 建立了反馈协议与鼓励系统，但独立 QA 发现 10 份 `workflows/` 手册中 9 份对这些规则零引用；长对话进入具体阶段、上下文被压缩后，反馈三段式、P0/P1/P2、鼓励档、过闸可能被遗忘。本版做"接线"而非新增规则。
- **统一带教约定指针**：全部 10 份阶段/主题手册顶部新增一条**逐字一致**的"带教约定"，指向 `core/coaching-protocol.md`、`core/encouragement-guide.md` 与 `core/coach-rules.md` 第十一节三闸；手册**不复制规则正文**，规则仍只存 core 单一来源（延续 v1.56.2 单源化原则，消除未来双源漂移面）。
- **鼓励理论补规范出处**：`encouragement-guide.md` 四理论从只写理论名补全为可查的规范条目——Kluger & DeNisi (1996, *Psychological Bulletin*, 119(2), 254–284)、Mueller & Dweck (1998, *JPSP*, 75(1), 33–52)、Ryan & Deci (2000, *American Psychologist*, 55(1), 68–78)、Skinner (1953, *Science and Human Behavior*, Macmillan)，APA 第7版格式，标注 2026-09-17 核对；并特别注明勿与 Deci & Ryan (2000, *Psychological Inquiry*) 混写。对写论文的学生同时是引用格式示范。
- **文献检索防假空白**：`literature-auto-search.md` 研究空白判定补反查要求——一次 0 命中不等于"没人做过"，须换同义词/上位词/英文词重检并留存检索平台、检索式、时间与 0 命中结果，防止检索词过窄造成假空白（提炼自项目作者本人知网检索的"0 结果硬证据"实操经验）。
- **验证**：`full_e2e.py` 218 → **224 项**（新增工作流接线齐全、指针逐字一致、规范出处与核对日期、防假空白等 6 条）、`consistency_check.py` 退出 0；发布前在项目外干净副本复验。

**v1.56.3 系统测试缺陷修复版——端到端模拟论文流程发现的 7 项缺陷修复**
- **背景**：在 v1.56.2 基础上以模拟学生身份完整走"数据生成→预处理→清洗→主分析（EFA/链式中介/调节）→稳健性→样本量→模型图→文献检索→论文组装"全流程（多轮），发现并修复以下缺陷；`full_e2e.py` 218 项断言全部通过。
- 修复 `auto_stats.py` 调节效应不支持人口学数值列：`--moderator 性别` 报"列不存在"（调节/回归/中介只在量表总分矩阵取列）；新增"量表总分＋原始数值列"合并分析池（analysis_pool），性别/年级等已编码人口学列可作 X/W；偏相关原有的 extra_matrix 同口径。
- 修复列名/量表名拼错时 `linear_regression` 直接抛 `KeyError` Traceback：新增统一的 `_resolve_or_warn` 解析与中文友好提示，回归/中介/调节找不到列时跳过并列出可用量表，不再崩溃；`stats/regress.py` 守卫同步改为通用中文提示。
- `paper_search.py` 新增 429/超时自动等待重试一次、首选源失败自动换另一数据源（区分"网络失败 None"与"零结果 []"，零结果不换源），两源均不可达时给出中文处置建议并以退出码 1 结束。
- `chart_generator.py` 链式模型系数位与 PROCESS 模型 6 口径对齐（a、b1、b2 → **a1、d21、b2、c′**，帮助文本/docstring 同步写明顺序＝X→M1、M1→M2、M2→Y、直接效应），各箭头标注路径名（简单模型标 a/b）；直接效应弧线改为从节点上方绕行（rad=-0.5）且节点 zorder 压在箭头之上，不再穿过中介框。
- `sample_size.py` 新增入参校验：越界效应量（如 r=2）、alpha/power/extra 越界、组数/预测变量数非法、效应量过小导致 N>20000 均给中文友好提示并以退出码 1 结束，不再抛 `math.atanh` 等英文 Traceback。
- 本 CHANGELOG 补登记上述系统测试修复（v1.56.3）。

**v1.56.2 本体口径硬化——人格挫折协议一致、反攀比回归锁定、规则单源化**
- **背景**：v1.56.1 由独立复核在轻量版抓到"霸道总裁焦虑示例不接情绪""无凭据横向比较"两类缺陷并修了 Skill 侧；本轮回查完整版本体，发现同类风险与两处规则双源，按"两版口径一致"（DEVELOPMENT §5.5）在本体侧追平并加回归锁。
- **coach-rules 霸道总裁条目**："不允许学生说'我不行'，会怼回去"改为"先一句话正常化情绪，再用事实把'我不行'翻成'我卡在哪'——强势对事，不羞辱、不评价情绪"，与 `core/encouragement-guide.md` 挫折时刻协议第一步及行为自测 T31 对齐（强势人格保留，但四种人格都必须先接住情绪）。
- **encouragement-guide 专业导师示例**：焦虑处置从"直接给秩序感"补为"先一句话正常化，再给秩序感"，并注明四人格结构相同、仅措辞不同；段三补"结构定义以 `coaching-protocol.md` 第三节为唯一来源"，消除三段式两处各自维护的漂移面。
- **coaching-protocol 降档规则单源**：删除与 coach-rules 第四节重复的降档表述，改为指针引用；难度降档规则今后只在 coach-rules 维护一处。
- **验证**：本版本体口径新增 4 条断言（含 2 条阴性回归）；发布树同时含同期并入的 Skill v1.2 手机独立版 15 条断言（隔壁会话成果，本版本一并在干净副本复验），`full_e2e.py` 全量计数 **218 项**；`consistency_check.py` 退出 0；发布前在全新解压副本复验。

**v1.56.1 口径纠错版——doubao-skill 与完整版口径追平＋行为规范纠错＋两处门禁修复**

- **背景**：对 `doubao-skill/` 做独立复核时，逐条比对了完整版权威口径（`psychology/stats-guide.md`、`workflows/proposal-guide.md`、`templates/proposal-template.md`、`tools/data_cleaner.py` 与 `tools/stats/compare.py` 的实际实现），发现轻量版的多处判据与完整版漂移。共同病根是**把"有条件的、分级的"判据压成了"单一无条件阈值"**——每句单看都像对的，所以四道检查都没抓到。

- **统计判据纠错（`doubao-skill/`）**
  - **CITC**：原 `<.3` → 完整版分级「≥.50 良好、.40–.50 可接受、<.40（严格用 .35）需排查」（`stages/stage-8-analysis.md`、`references/faq.md`）
  - **KMO**：原 `>..7 适合，.6–.7 一般` 既有**双点笔误**，又把 `.7–.8` **误标为"适合"**（完整版该档是"可接受"，"适合"对应 `>.80`）→ 改为完整版五级「>.90 极佳／>.80 适合／>.70 可接受／>.60 勉强／<.60 不宜」
  - **α**：补全为完整版五档（>.90 优秀／.80–.90 好／.70–.80 可接受／.60–.70 勉强接受需说明／<.60 不可接受）
  - **载荷**：`>.4–.5` 书写歧义 → 完整版表述「≥.40 可接受、≥.50 较理想」
  - **卡方期望频数**：原「两个分类变量用卡方（期望<5 用 Fisher）」把完整版中**仅适用于 2×2** 的规则泛化成了通则 → 改为「2×2 默认 Yates 连续校正；**仅当**期望<5 的单元格超过 20%、或任一期望<1、或 2×2 且期望<5 时才改 Fisher；更大的表考虑合并稀有类别」
  - **缺失值插补**：原 `<5%` 与完整版 `<20%` 不一致 → 按完整版对齐
  - **开题报告结构**：原写「通常**八节**」却列了 **10 项**，且**漏掉"研究创新点"**（完整版第五节）→ 改为与完整版一一对应的八节
  - **查重率**：原 `≤20%–30%` → 完整版 `<10%–30%`（保留"以本校文件为准"）
  - 同步 `doubao-skill/CHANGELOG.md` 记为 **Skill v1.1.1**（`SKILL.md`／`README.md` 版本号同步）

- **行为规范纠错**
  - doubao-skill/personalities/concise-direct.md（v1.4 前文件名 strict-ceo）：焦虑示例原为「焦虑没用……别空想」，**违反** `coaching-protocol` 挫折时刻协议第 1 步（"接住情绪、不评价'想太多'"）——而该文件自己写着协议优先级更高 → 改为「先正常化接住情绪 → 再给一个最小任务」
  - **无凭据比较**：删除 5 处无法核实的横向比较（`strict-ceo`×2、`puppy`×1、技能版与完整版 `encouragement-guide` 各×1 的"超过我带过的大半学生／赢过很多人／超过大半同级学生"）→ 替换为指向学生**真实做出的具体动作**的肯定，与鼓励指南自己的"禁止无凭据表述"一致

- **覆盖补齐**
  - `doubao-skill/references/self-test.md`：新增第五节「情绪与挫折应对」（T27 焦虑／T28 自我否定／T29 情绪崩溃）→ **26 → 29 条**。此前"挫折时刻"在全表出现 **0 次**，正是上述 strict-ceo 缺陷所在路径的盲区
  - `tests/behavior-self-test.md`：同类补 T31／T32；并把「无残留占位符」检查项**限定为 `TODO/TBD/FIXME/待补充/待完善`**——原写法会误报 `defense-ppt-outline.md`、`ethics.md` 里的模板填空位 `XXX`

- **门禁自身修复（`tests/full_e2e.py`）**
  - **编码守卫断言覆盖不全**：原只扫 `tools/` 与 `tests/`，`doubao-skill/validate.py` 的守卫**没有回归保护**（日后被删无门禁可见）→ 改为同时扫三个目录，覆盖面 14 → 15 个脚本
  - **「自测用例编号连续」名不副实**：原用 `("T%d" % i) in bt` 子串判断且只抽查 4 个编号——`"T1"` 会匹配上 `T10–T19`，删掉 T7 也发现不了 → 改为真正抽出编号验证 1..N 连续，并新增「情绪与挫折用例」断言

- **验证**：`doubao-skill/validate.py` 退出 0；`tests/consistency_check.py` 退出 0；`tests/full_e2e.py` **199/199**（v1.56 为 198，+1 即本版新增的情绪用例断言）。

**v1.56 本体导师反馈协议与鼓励系统——学习成熟技能范式，完整版补齐"怎么带"的科学基准**
- **背景**：v1.55 的协议化引导与鼓励系统只落在豆包 Skill 轻量版；完整版本体仍是"coach-rules 单文件＋专业导师不给鼓励"旧形态。本轮把同一能力按完整版架构补进本体，术语与轻量版一致（落实 DEVELOPMENT §5.5 两版口径一致）。
- **新增 `core/coaching-protocol.md`（启动必读，新文件）**：每轮对话的强制行为基准——引导循环六步（锚定/唯一任务/支架/等待/三段式反馈/过闸留痕，每轮≤3要点）、问题分级 P0/P1/P2（含本体特有情形：模拟数据写进真论文、反向题未核对就跑总分、问卷缺质量埋点）、决策协议（低风险可代劳、高风险学生拍板、导师意见优先）、触发边界（承接/转人工/复合请求）、输入与边界降级表（零散信息/文件打不开/说法矛盾/失联拖延/坚持 P0/偏好记忆）、学生指令速查、学生本人心理危机应对（直接询问计划、12356、即刻危险 120/110/急诊、不允诺保密；受访者危机转 psychology/ethics.md）、回复前门禁 8 条。
- **新增 `core/encouragement-guide.md`（启动必读，新文件）**：鼓励与奖赏式反馈系统，**默认"标准"档打开、学生随时可关**（"关闭鼓励/鼓励精简一点/开启鼓励"，即时确认并写入进度卡）。设计依据四条教育心理学结论：正强化、成长型思维（Dweck）、反馈干预理论（Kluger & DeNisi, 1996）、自我决定理论（Deci & Ryan）。含反馈三段式、P0 不包装、五级奖赏节点（检查点/过闸/阶段/大节点/挫折返工）、四种人格措辞差异、挫折时刻四步、六条禁止事项（禁夸天赋/空夸/包装 P0/羞辱/奖错/刷屏）、五条反馈自检；并给出工具场景口径（夸学生"先核对反向题再跑""先拿演示数据练手"等判断，不夸工具）。
- **改造 `core/coach-rules.md`**：人格系统改为"人格管措辞、鼓励档管规则"正交设计；移除"不给无意义鼓励"旧表述，专业导师默认执行标准档（肯定具体动作，不空夸不夸天赋），其余三人格补关闭档行为；难度系统新增"卡壳两次临时降一档、过后恢复、不说破"；阶段 0 与进度卡首次填写收集三项偏好（风格/难度/鼓励档）；检查闸机制接 P0/P1/P2 处置口径；第十二节扩为反馈三段式＋矛盾处理＋学生指令。
- **入口与文档同步**：`START.md` 必读 3→5 份、开场 4 问→5 问（第 5 问反馈方式）、重要提醒补鼓励档；进度卡模板与工作区预置卡加"鼓励反馈档"偏好行；`AGENTS.md` 阅读优先级表加两行、目录速查更新；`README.md` 新增"鼓励式反馈"章节、结构图 core 层更新、致谢补四条理论出处、版本块更新；`QUICKSTART.md` 开场模板与 FAQ 补鼓励开关；`PROJECT_PLAN.md` 架构图 core 层补两文件。
- **新增 `tests/behavior-self-test.md`（维护者用）**：T1–T30 导师行为压测用例（启动与边界 7 条、引导循环与三闸 6 条、反馈与鼓励 9 条、事实与工具边界 6 条、续接紧急 2 条）＋结构自检清单＋走查记录表；明确该文件是"测试计划与预期口径，不是已自动通过的证据"，与 full_e2e 的结构断言互补（自动化测不了真实对话质量）。
- **验证**：`full_e2e.py` 175 → **198 项**断言（新增 23 条：两新文件结构要素/四理论/三档与指令/P0 与危机口径/旧鼓励表述阴性断言/临时降档/双进度卡偏好行/各入口登记/与 Skill 版关键术语一致）；`consistency_check.py` 退出 0；发布前在全新解压副本复验。

**v1.55 豆包 Skill 轻量版——可安装技能（doubao-skill/）＋协议化引导＋鼓励系统**
- **新增分发形态：豆包 Skill 轻量版（新功能）**：新增 `doubao-skill/` 目录，作为可直接安装到豆包技能目录的轻量版（Skill v1.1）。面向"不拿整个项目包、只在豆包里启用技能"的学生；**不含 Python 脚本**，需要自动化时指引完整版，两边口径一致。
- **入口协议化（学习成熟技能范式）**：SKILL.md 增加触发边界（应触发/不应触发/复合请求）、启动时一次性收集 4 项偏好（风格/难度/鼓励档/当前阶段与题目）、文件按需加载规则、运行铁律与学生指令速查。
- **默认自然风格，人设全部改为可选**：新增默认"自然专业"风格（不套人设）；原知心姐姐/严格导师/热情陪伴三种角色扮演默认**不加载**，仅学生主动选择时启用。
- **新增鼓励与奖赏式反馈系统（默认打开、可关闭）**：`references/encouragement-guide.md`，标准（默认）/精简/关闭三档，指令即时切换并记入进度文件；反馈三段式（具体肯定→P0/P1/P2 问题→最小下一步）；里程碑奖赏与挫折时刻协议；P0 问题不用鼓励包装；依据正强化、成长型思维、反馈干预、自我决定理论。
- **新增引导协议总纲** `references/coaching-protocol.md`：引导循环、P0/P1/P2 分级、三闸（动机/质量/留痕）、三档难度（卡壳两次临时降档）、进度卡跨对话续接、决策协议（给选项不替学生拍板）、幻觉防范与 `[需核实]`、工具安全、紧急模式（7/3/1 天逐日表+三红线）、心理危机（12356/120/110）与研究伦理（未成年人 assent/双同意、去标识、数据保存≥5 年）、失败降级、产出前门禁。
- **12 阶段文件统一模板重写**：阶段卡→准入检查→产出物→分步引导（导师动作/学生任务/检查点/鼓励时点）→卡点应对（标 P 级）→红线→三闸准出；口径与完整版对齐（文献 30–50/精读 10–15、问卷 60–80 题与质量埋点三件套、功效样本量 α=.05/功效=.8+15% 冗余、简单中介≥200/链式建议≥300、五指标清洗、Harman<40%、Bootstrap 5000/95%CI、JASP/SPSS+PROCESS 正式口径、24 类答辩问题）。
- **新增** `references/stage-checklist.md`（12 阶段准入/准出速查与跳阶段规则）、`templates/我的论文进度模板.md`（轻量版学生续接用）、`references/self-test.md`（26 条维护者行为测试用例）。
- **新增 `doubao-skill/validate.py` 自检门禁（测试金字塔 L7 扩展）**：纯标准库，校验 frontmatter、必备文件、阶段编号连续性、三闸 checkbox、内部链接、鼓励默认开关、安全口径、占位残留；full_e2e 新增 13 项断言（含临时副本植入变异的阴性测试）；`tests/consistency_check.py` 将 doubao-skill 列为独立命名空间跳过（由 validate.py 自检）。
- 维护说明见 `doubao-skill/README.md`；安装位为 `.user_skills/thesis-ai-coach/`，发布时从本目录镜像同步。

**v1.54 学生自己做网页版——网页引导能力＋3 个范例＋治理补齐**
- **新增网页能力（新功能）**：AI 导师可引导学生做一个**给自己用**的网页，把手里多而杂的东西变得能检索、能一眼看懂。五类对应论文不同阶段：
  ① 文献笔记网页（文献检索/精读，几十上百篇要搜要筛）② 数据分析结果看板（分析后把三线表/热图/中介结果整理成一页）
  ③ 研究流程与模型可视化（选题/开题，讲清变量关系）④ 量表与问卷速查页（选量表/设计问卷时对照）⑤ 论文进度看板（全程）
- 新增 `workflows/webpage-guide.md`：判断该不该做（含"该劝住的四种情况"）、五类网页对应阶段、**六条硬规矩**、七步引导流程、常见坑表、三闸检查清单。
  硬规矩重点：**单文件自包含**、**不引用外部资源/CDN**（外链失效会白屏而学生不知道原因）、**不放个人隐私**、**内容真实可追溯**、
  **网页不是论文成果**（论文正文/结果/讨论仍必须学生自己写，不能把网页内容当论文素材搬用）、**不联网不上传不改学生数据**
- 新增 `tools/webpage_preview.py`：纯标准库本地预览器（学生可能没装 Node，项目内原有的 `server.js` 不能依赖）。
  安全设计：**只读**（仅 GET/HEAD，无任何上传/写入）、**限根**（`normpath` + 反斜杠归一 + `relative_to` 双保险，9 项穿越用例实测全拦）、
  **默认只绑 `127.0.0.1`**（`--lan` 才允许手机看并打印风险提示）、文本类型显式声明 `charset=utf-8`（否则非声明 charset 的文件会乱码）、
  端口占用自动顺延、目录不存在给中文引导、`--list` 只列不启
- 新增 `templates/网页范例/`：随包分发 **3 个现成网页范例**（文献阅读笔记 141 篇 / 心理学论文写作术语词典 179 词 / 研究流程一图流）。
  逐字保留内容，仅在 DOCTYPE 后插入一行不可见注释标注"**范例，请勿直接使用**"；README 说明各页示范的做法、并指出其中一处
  外链字体的偏差作为教学点（保留原样是为了让学生看到真实项目里的不一致，而不是把它当标准做法）
- 新增学生网页目录 `我的工作区/04-网页/`（含引导说明）；`.gitignore` 同步只跟踪占位 `*.txt`
- 菜单增至 **9 项**（`【N/8】` 全部重编为 `【N/9】`，新增第 9 项"预览我做的网页"）
- 接入：`START.md` 第四步导航表与第五步工具表、`core/coach-rules.md` 工具表＋阶段 3/阶段 8 的网页钩子、`README.md`、`QUICKSTART.md`、
  进度卡模板与工作区进度文件新增"自己做的网页"栏

- **治理补齐（上轮审计遗留项）**
  - `CONSTITUTION.md` 进入 AI 启动序列：`START.md` 第二步"读取你的规则手册（有优先级）"把它列为**最高优先级第 1 条**，
    并明确规则层级（宪法 > coach-rules > ai-literacy > 按需文档）与"冲突时以宪法为准"；此前它只被 README 结构块提及，AI 永远不会读到
  - `psychology/` 由"启动全读"改为**按需读取表**：此前启动即全读 6 个文件约 77 KB（≈2.6 万汉字），
    而其中最长的 `stats-guide.md`（25 KB）要到数据分析阶段才用得上——等于白占学生第一次对话的上下文
  - `DEVELOPMENT.md` 在启动序列中标注"**只有维护者才读**，学生辅导时不需要遵守九阶段门"
  - 新增 `AGENTS.md`：自动化 AI 助手入口（支持 AGENTS.md 约定的工具会自动加载），给出阅读优先级、维护者验证命令、硬约束、目录速查
  - 新增 `LICENSE`：把宪法第九条的授权落到单独文件（免费非商用、保留宪法、注明修改），并写明条款以 `CONSTITUTION.md` 为准 + 免责声明
  - 新增 `requirements.txt`：**实测**确认全项目第三方依赖只有 matplotlib 与 numpy（`plots.py` 内延迟导入），且均为可选

- **验证**：`full_e2e.py` 138 → **163 项**断言（网页能力新增 25 条：硬规矩/五类网页/三闸/范例齐备与标注/预览器只读与绑定与防穿越与 charset/`--list` 实跑/菜单第 9 项/各入口登记/工作区就位）；
  `consistency_check.py` 在默认 GBK + 管道捕获下退出 0；预览器 9 项路径穿越用例（含反斜杠与双重 URL 编码）全部符合预期

**v1.53.1 工程化版——管道编码崩溃修复＋auto_stats 拆包＋版本日志单文件化**
- **修复中文 Windows 管道运行崩溃（严重）**：9 个工具脚本与 4 个测试脚本统一加"输出编码守卫"，stdout 被管道/重定向时强制 UTF-8。此前 `² χ² ⚠ ↔` 等 GBK 无法编码的字符会让 `auto_stats.py`（L1529 Bartlett χ²）、`consistency_check.py`（L169 ↔）、`full_e2e.py`（打印含替换符的失败详情）抛 `UnicodeEncodeError` 退出；`full_e2e.py` 曾经在第 3 项断言即中断，127 项只跑 2 项。交互式控制台走 PEP 528 通道不受影响，故此前手测发现不了
- **`tools/auto_stats.py` 拆包**：单文件 2694 行 → `tools/stats/` 包 9 个模块（`mathx`/`linalg`/`dataio`/`desc`/`reliability`/`plots`/`efa`/`compare`/`regress`）＋瘦身 CLI。按 AST 精确切片，函数体逐行未改动；**所有 CLI 开关与导出文件名保持不变**，文档命令无需改动
- 拆包连带修复：`sample_size.py` 的 `from auto_stats import betai, f_p_value` 改指 `stats.mathx`；`auto_stats.py` 增加显式 `sys.path` 引导，保证被 `runpy.run_path` 调用（`test_graceful_degradation.py` 的缺库阻断 wrapper 正是这样跑的）不再 `ModuleNotFoundError: stats`
- `consistency_check.py` 改为递归扫描 `tools/**/*.py`：导出文件名与 CLI 开关可能定义在任一实现模块里，只扫顶层会在拆包后漏掉 `_因子分析.csv` 这类由子模块写出的文件而误报漂移
- **版本历史单文件化**：新建 `CHANGELOG.md`（含日期索引表，此前版本记录只有版本号没有日期）；`README.md`（358→136 行）、`PROJECT_PLAN.md`（384→296 行）、`ROADMAP.md`（145→28 行）里的重复版本清单改为指针，同一版本不再需要手改四处
- **验证**：`full_e2e.py` 129 → 138 项断言（新增编码守卫齐备、管道下不崩、管道下中文正常、拆包结构、CLI 瘦身、原大文件已分解、runpy 可调用、一致性检查覆盖子包、CHANGELOG 齐备）

**v1.53 问卷模板人口学适配中学生/混合样本版**
- 问卷基本信息从纯大学框架改为按样本裁剪：性别加"其他/不愿透露"、新增"学段"题（混合样本必收）、年级给中学版/大学版二选一、专业题标注仅大学生、强调个人信息最小化；全项目排查无其他写死大学样本处；full_e2e 增至 129 项

**v1.52 统计指南 PROCESS 域名纠错＋网络分析稳定性版**
- 逐字走查 stats-guide 十一节方法学无误；订正 v1.46 漏改的 stats-guide PROCESS 官网域名（hayesprocess.com→processmacro.org）；网络分析补 strength/expected influence 与 bootnet 边权/删案例自助 CS 系数稳定性检验、定位为补充分析；full_e2e 增至 127 项

**v1.51 未成年人研究伦理要素补强版**
- 联网核实 IRB/Belmont 共识与国内存档规定：assent 按年龄分层（7岁以下仅监护人许可+口头说明，7岁以上适龄书面 assent，青少年近成人）、与监护人许可分开且尊重 dissent；原始数据保存改为毕业后≥5年/科研记录5-7年；新增中学生书面 assent 模板、研究前风险转介预案、校园投放伦理（避免班主任盯填/不挂钩成绩/退出保密）；full_e2e 增至 125 项

**v1.50 数据库批量下载红线＋文献归类/结果顺序规范版**
- 联网核实知网等使用条款后补"频率与批量下载红线"：禁下载工具/爬虫、人工停顿、单次登录≤约30篇、全天≤30-50篇分时段、不整刊不抓全量、全文不传播不上传（过量会封账号/全校IP，附公开封禁案例），AI以题录摘要为主；题录与文献总表统一归 01-文献PDF；写作结果章把人口学差异/卡方从相关中独立；full_e2e 增至 122 项

**v1.49 Zotero/Obsidian 工具链插件职责纠错版**
- 纠正"Zotero Integration 可在正文 Insert Citation"的张冠李戴：该插件只导入文献笔记；正文插引用/参考文献应在 Word 用 Zotero 插件（推荐）或在 Obsidian 装 Citations / Zotero Citations；全景图、3.1/3.5、写作流程、FAQ 同步，Zotero 7 菜单措辞修正；full_e2e 补 3 条断言并修复生成物清理遗漏

**v1.48 一键全量回归脚本固化版**
- 把 115 项端到端断言固化为随包 `tests/full_e2e.py`：脚本真实运行＋统计基准＋缺库降级闭环＋文档一致性＋合规/量表事实，自动定位项目根、自动备份恢复基准样例、自动清理；开发仓库与解压干净副本都能一键跑，新增能力必须同步加断言；DEVELOPMENT 测试金字塔 L5/发布清单同步

**v1.47 量表库硬事实核查纠错版**
- 逐量表联网核查 16 个量表题数/维度/作者年份，纠正 6 处硬伤：RRQ 10→24题、AAS 17→18题、FASM 作者 Kelley 拼写与功能模型、UCLA 第3版归 Russell 1996、CD-RISC-10 简版作者、MPAI 原始 Leung 2008

**v1.46 数据分析主流程走查＋JASP链式/样本量口径纠错版**
- 逐字走查 data-analysis-auto.md；纠正"JASP原生Mediation支持链式中介"（链式需 Process 模块 Model 6 或 SEM），PROCESS 宏官网更正为 processmacro.org
- 统一样本量口径（链式建议300、最低不低于200、预留10-20%无效卷、G*Power 为准），消除质量闸"至少>150"的偏低漂移

**v1.45 新增《跟老师沟通指南》补全程短板版**
- 新增 workflows/communication-guide.md：五原则、沟通节奏、12个场景可套用话术（选题/量表/开题/伦理/收数/初稿/修改/催稿/被批评/不回复/分歧）、礼仪、态度解读、留痕
- coach 阶段10、START 导航、README 接入；修正"16类/10种"数字漂移（实际16种量表分6大类、stats-guide 11章节）

**v1.44 AI使用声明如实化＋文献精读/工作区走查版**
- 修正 AI 使用声明：原"AI未参与的内容"绝对化表述与实际用脚本检索/计算矛盾（签了反而不如实），改为"核心学术贡献由本人独立完成"的可如实签署口径
- 走查文献精读指南（IMRaD 20字段、精读10-15/泛读20-40与总量自洽、质量识别）、工作区引导与进度卡，均准确无需改

**v1.43 开题报告指南走查＋合规口径强化版**
- 走查 proposal-guide（开题三问/八节/PPT/高频问答/检查清单），核对八节与 proposal-template 一一对应
- 强化工具口径：开题与论文正文只写 SPSS/JASP/PROCESS/G*Power 等公认软件，自写脚本仅用于自我预览、不写进论文
- 强化量表授权答法：需授权量表提前邮件申请书面许可并留存

**v1.42 核心规则手册整体走查＋答辩题库数字一致性版**
- 逐节走查 coach-rules（角色/原则/4人格/3难度/进度卡续接/12阶段/红线/8常见错误/紧急模式/工具规范/三检查闸/说话方式），与工具实际能力一致
- 修正数字漂移：defense-guide 题库 v1.29 已扩到 24 问，但 coach-rules 阶段11 与 README 能力表仍写"20个"，统一为 24；核查文献量口径（收集30-50、精读10-15）各文档已一致

**v1.41 环境搭建指南事实准确性走查版（联网核实）**
- JASP 中介改为"先内置 Regression → Mediation（Bootstrap），PROCESS 为模块库可选"，并区分 SPSS 需另装 Hayes PROCESS 宏（Model 4/6）
- 修正"Mac 自带 Python"过时表述（新版 macOS 不预装，给 python3/Xcode 命令行工具/官网 pkg 准确步骤）；Python 版本建议放宽为 3.11 及以上稳定版
- 修正 Zotero 中文格式：GB/T 7714 是"引用样式"不是插件，给出准确安装路径

**v1.40 心理援助热线事实纠错与危机处置版（联网核实）**
- 经国家卫健委/中国政府网核实，全国统一心理援助热线为 **12356**（国卫医政函〔2024〕259号，2025年5月起各地热线统一接入、一号接通），列为问卷首尾、知情同意书、答辩答法首选
- 修正原把 400-161-9995 误标为官方"全国心理援助热线"的错误（其为社会公益"希望24热线"）；北京回龙观 010-82951332 保留
- ethics/问卷模板/答辩指南统一新增"即刻自伤自杀危险拨120或110、前往急诊"的危机处置

**v1.39 AI素养教育增强（会用工具的AI去魅）＋能力清单真实性走查版**
- ai-literacy 新增"AI 能联网/操作虚拟电脑/跑代码（本项目形态）"的去魅：动手查、动手算更可信，但来源要核实、计算要复核、解释仍可能错；登录/验证码/提交/付款/删除/外发等关键动作必须学生亲自做或确认，AI 不碰账号密码、只在项目文件夹操作、原始数据先备份
- 隐私风险强化：含姓名/手机号/身份证号/可识别照片的数据不发给 AI 也不放进项目文件夹，先去标识化
- 逐行走查 START 能力清单，VIF/容差、Bonferroni、Welch/Games-Howell、分半信度、EFA 平行分析、调节简单斜率等均在代码中真实实现，无夸大

**v1.38 菜单产物归位、正态性引导与版权合规版**
- 菜单4英文检索结果默认归位「我的工作区/01-文献PDF」，paper_search 导出到不存在的嵌套目录时自动建目录（回归扩到 13 断言，联网端到端实测通过）
- stats-guide 明确脚本只提供偏度/峰度＋Kline 判据，Shapiro-Wilk/K-S 正式检验与直方图/Q-Q 图引导用 JASP/SPSS，不夸大正态性能力
- 版权合规：移除检索提示中的 Sci-Hub 引导，改为图书馆/馆际互借/合法开放获取/向作者索取

**v1.37 演示数据练手闭环＋启动器路径健壮性版**
- 真人走查"新学生不接真实数据先练手"路径：菜单7全新生成演示数据→菜单3一键统计完全自洽（链式间接效应显著、信度/中介 CSV 与相关热图齐全），并固化进回归测试（test_graceful_degradation 扩到 12 断言）
- 菜单7直接回车时演示数据默认归位「我的工作区/02-问卷数据」而非污染源码根，并提示下一步操作
- 走查确认启动器（cd /d %~dp0）与菜单（脚本绝对路径＋固定 cwd）从任意目录启动都能定位 tools 脚本

**v1.36 缺库优雅降级固化＋菜单1端到端回归版**
- 真人走查"学生只装了 Python、没装 matplotlib/numpy/scipy"场景：确认 auto_stats 纯标准库数值通路完整（信度/相关/中介等 CSV 照常产出、退出0、仅跳过图表并提示 pip install），模型图脚本友好报错退出1引导安装，均无 Traceback
- 新增永久自动化回归 `tests/test_graceful_degradation.py`（9 项断言，阻断第三方库后验证降级、并端到端验证菜单1问卷星预处理，临时文件自清），纳入发布前回归

**v1.35 工具箱菜单4/5/6/7真人端到端走查与模型图崩溃修复版**
- 修复真实崩溃：模型图脚本把路径系数 `--coefs` 误设为必填，学生在菜单第6项直接回车（尚未出结果、想先出框架图）会 argparse 报错退出；改为可省略、自动按模型补 0 占位，系数填少自动补齐、填多自动截断并提示，非数字输入友好报错
- 消除模型图 SimHei 粗体 findfont 警告（避免小白看到警告误以为出错）；菜单提示明确"直接回车先用 0 占位"
- 真人端到端走查确认：菜单4英文文献检索（OpenAlex，联网免费、真实返回并导出 CSV）、菜单5文献去重分类、菜单7演示数据生成均正常可用

**v1.34 进度卡模板对齐与工具链走查版**
- 修复进度卡模板与工作区进度文件的断档：阶段7补剔除份数与五指标标准，数据记录补剔除日志、G*Power 功效最小 N、分半信度、差异/卡方/调节结果栏
- 走查 Zotero+Obsidian 工具链指南：脚本名引用正确、最简替代路径完整，校正 Better BibTeX 依赖表述（Zotero Integration 非必需）

**v1.33 开题报告与答辩PPT模板走查对齐版**
- 开题模板：样本量改为 G*Power 功效分析＋链式中介 300–500＋15% 无效卷的依据写法；研究工具信度改为引已有研究（开题不预填本研究 α）；研究程序补未成年人监护人知情同意/学校批准、注意力题等质量埋点与五指标剔除；数据处理补分半/EFA/差异/卡方/非参数/调节；创新点"首次"改为可辩护的谨慎表述
- 答辩 PPT 大纲：研究对象页补伦理合规（监护人同意/心理援助），研究空白与流程同步谨慎措辞与差异分析

**v1.32 论文大纲模板与统计指南走查对齐版**
- 论文大纲模板结果章与工具完整输出对齐：补正态性、EFA/平行分析、人口学差异（t/Welch/ANOVA/非参数）与卡方、调节与简单斜率；研究程序补未成年人监护人知情同意＋质量控制埋点，数据处理补分半信度与 Bootstrap 口径
- 逐行核实 stats-guide 与代码一致（Welch t/Welch ANOVA/Levene 确已实现、Games-Howell/Dunn 正确引导 JASP、样本量非中心 F 口径），确认无"文档夸大代码能力"
- 走查确认 AI 使用声明模板合规（正文独立撰写、AI 不生成段落、如实申报）

**v1.31 紧急模式可执行化＋量表库循证纠错版**
- 紧急模式补"任何情况不可破的三条红线"（不编数据/不代写不伪引/宁可延期不造假）与 7天/3天/1天逐日最小任务清单，并要求先定位卡点，杜绝赶时间放松学术规范
- 量表库 NSSI 一节循证纠错：删除张冠李戴的"ASFQ/SBI"，改为学界公认的 FASM（含中文版 C-FASM，行为清单＋22/15功能题）、DSHI（Gratz 2001，17题；青少年 DSHI-9）、ISAS（Klonsky 2007），并提示 NSSI 多为 0/1 或计数变量、以所引论文版本为准；修正 UCLA 简表出处

**v1.30 问卷星多选/填空/哑变量列健壮版**
- 预处理识别多选题（`┋` 分隔或表头"哪些/多选/___"）与开放填空题，列映射报告单列"不要编码进量表、scales.txt 勿列入"，与需人工编码的单选（如年级）分开，避免学生误把多选/填空塞进量表
- 清洗器无 scales 兜底时不再把纯 0/1 多选哑变量列、是否题当作 Likert 连续作答题（NSSI 等 0/1 计分量表走 scales.txt 精确通道），杜绝多选列污染长直线/低变异判定
- 新增 tests/test_special_columns.py 合成数据黄金回归（11 断言）；数据分析工作流第0步、问卷设计阶段、问卷模板均补多选/填空处理说明

**v1.29 后半程真人走查·答辩与写作衔接版**
- 答辩高频题库由20问扩到24问：新增问卷清洗剔除标准/份数、未成年人监护人知情同意、敏感话题心理保护、数据隐私等"几乎必问"题，并给样本量题、问卷质量题的标准答法（贴合青少年样本与五指标清洗）
- 写作手册"研究结果"章节顺序与工具完整输出对齐（补正态性、人口学差异/卡方、调节与简单斜率、相关热图插图说明、差异检验统计量写法）
- 走查确认伦理手册（监护人同意+本人assent+学校批准、援助热线、不诊断、转介、数据伦理）与 AI 素养手册（去魅/幻觉/分工/红线）完整无断档

**v1.28 真人视角走查·跨阶段衔接修复版**
- 以"电脑小白第一次拿包"视角从 START 走查全程，补齐阶段断档：问卷模板新增注意力检查题＋作答时长埋点（清洗硬指标在发问卷前就布置好）；coach-rules 阶段5补数据质量埋点、阶段6补 sample_size 样本量估算、阶段7清洗列五指标、阶段8能力描述同步到最新
- 修复 QUICKSTART 菜单漏列第8项（样本量估算）、流程图把"开题"误放在"查文献"之前（已按12阶段纠正为 选题→文献→量表问卷→开题）
- 统一全项目工作区目录名为 `我的工作区/01-文献PDF/02-问卷数据/03-分析结果`（此前多处漏连字符、知网与 PDF 手册把文献落点误写成根目录下不带编号前缀的文件夹，会导致 AI 建错目录）；知网 OA 入口与 PDF/题录落点补全
- START 版本号更新、重复步骤编号修正；consistency_check 增第5类工作区路径检查；新增测试25真人走查断言

**v1.27 相关矩阵热图版**
- `auto_stats.py` 跑量表总分相关时自动导出"数据名_相关热图.png"：下三角为相关系数＋显著性星号（*p<.05 **p<.01 ***p<.001），对角线为 Cronbach α（三位），上三角留白，红蓝色标 −1~1，300dpi 可直接插图；`--spearman` 时为秩相关热图
- 热图系数与已黄金验证的 Pearson/Spearman 矩阵逐元素一致（numpy 对照差 <1e-15）；matplotlib/numpy 为软依赖，未安装时静默跳过、数值结果不受影响

**v1.26 文档导航完整性版**
- `consistency_check.py` 增加第 4 类检查：Markdown 链接与反引号里引用的 `.md` 文档必须真实存在（AI 按引导去读文件不会断链），裸文件名按全项目同名兜底；命令行里出现的脚本（含 tests/ 下、裸调用）也核对存在性；排除通配符与 URL 误切（词边界）
- 当前全项目 106 处明确文件/文档引用零悬空；变异测试可抓出植入的假文档链接与假脚本

**v1.25 文档↔代码一致性自检版**
- 新增 `tests/consistency_check.py`：用 AST 解析每个工具真实的 CLI 开关与导出文件，交叉核对全部文档（README/START/workflows/模板等），自动抓出"文档引用了不存在的脚本、开关、导出文件名"三类漂移；退出码 0 才允许打包，已纳入发布清单与测试金字塔 L7
- 经变异测试验证能稳定抓出植入的假脚本/假开关/假导出，正常状态零误报

**v1.24 数据质量增强版**
- `data_cleaner.py` 在时长/连续相同基础上新增：注意力检查题（instructed response item，`--attention "列名=答案;..."`，答错即剔除，最受认可的硬指标）、长直线作答（阈值可配 `--longstring`）、个体内低变异SD（`--low-sd`）、缺失率过高（`--max-missing`）；质量指标只对量表/Likert作答题计算（提供 `--scales` 最准，否则启发式排除时长/人口学列）
- 新增剔除明细报告 `*_清洗报告.csv`（行号/时长/最长连续相同/SD/缺失率/原因）并按原因汇总份数，直接支撑论文"数据清洗"写明剔除标准；菜单第2项引导填写注意力题与 scales

**v1.23 分半信度版**
- `auto_stats.py` 每个量表在 α/CITC 后自动报分半信度：前后半（SPSS 口径）两半 α、两半相关 r、Spearman-Brown 系数 2r/(1+r)、Guttman λ4；奇数题另报奇偶分半（两半等长）；导出 `_信度分析.csv`（量表/题数/α/最低CITC/两半α/SB/λ4/评价）
- 与 numpy 独立实现逐位一致；信度体系 α＋CITC/删题α＋分半齐备，α 高分半低时提示先查因子结构

**v1.22 卡方独立性检验版**
- `auto_stats.py` 跑完自动对人口学分类列两两做卡方独立性检验（分类×分类，如性别×年级、性别×是否独生）：χ²(df)/p/Cramér's V（.1/.3/.5 小中大），2×2 自动 Yates 连续性校正，期望频数<5 超20%或<1 时提示 Fisher 精确检验/合并类别，导出 `_卡方检验.csv`
- 与 scipy.stats.chi2_contingency 逐位一致；至此连续×连续（Pearson/Spearman/偏相关）、分类×连续（t/ANOVA/MWU/KW）、分类×分类（卡方）三类变量组合分析齐备

**v1.21 Spearman秩相关与偏相关版**
- `auto_stats.py --spearman`：偏态/有序变量的 Spearman 秩相关矩阵（对秩做 Pearson，与 scipy.stats.spearmanr 逐位一致）
- `auto_stats.py --partial "性别,年级"`：控制混淆变量后的偏相关矩阵（相关矩阵求逆法，与回归残差法逐位一致），报偏 r/df/p 并导出 `_偏相关.csv`；控制变量支持人口学数值列与量表总分
- stats-guide 相关章、data-analysis-auto、菜单第3项同步

**v1.20 非参数差异检验版**
- `auto_stats.py --nonparametric`：因变量偏态/有序等级、t 与 ANOVA 正态前提不满足时，2组用 Mann-Whitney U（U/z/p/r，含结校正与连续性校正）、3+组用 Kruskal-Wallis H（H/df/p/ε²，含结校正，事后引导 Dunn），导出 `_差异分析_非参数.csv`；自实现卡方上尾 p（正则不完全 gamma），U/H 与 scipy 逐位一致
- stats-guide 差异小节、data-analysis-auto 增补非参数路径与判定（偏度/峰度）；菜单第3项可勾选非参数

**v1.19 开题样本量/功效估算版**
- 新增 `sample_size.py`：相关/多元回归总体R²/R²增量/单因素ANOVA 的先验功效分析，非中心F（Poisson混合，与 scipy.stats.ncf 逐位一致）＋Fisher z，三档效应量速查＋无效卷冗余建议，开题"需要多少份"直接给依据
- 心理学统计指南新增"十一、样本量与统计功效"

**v1.18 调节效应简单斜率图版**
- `--moderator` 自动出论文可用的简单斜率图（W 低/中/高三水平回归线，标注各斜率与显著性），复用碎石图中文字体与 matplotlib 软依赖（未装自动降级，不影响数值）

**v1.17 方差齐性＋Welch稳健检验版**
- 差异分析先做 Levene/Brown-Forsythe 方差齐性检验：2组方差不齐自动切 Welch t，3组及以上切 Welch ANOVA 并提示事后用 Games-Howell；`_差异分析.csv` 增方差齐性、稳健检验列
- Levene/Welch t 经 scipy、Welch ANOVA 经 Liu(2015)/R oneway.test 公式黄金对照逐位一致

**v1.16 共线性诊断版**
- 多元回归自动输出每个预测变量的容差与 VIF（<5正常/5–10关注/≥10严重），中介把 X 与多个 M 同时放入时可先查共线性；VIF 经 numpy 黄金对照

**v1.15 调节效应（PROCESS模型1）版**
- `--moderator 调节变量`：X、W 自动中心化＋交互项回归（b0–b3/SE/标准化β/t/p/R²），输出 W 低/均值/高三水平简单斜率（解析SE＋Bootstrap 95%CI），导出"数据名_调节效应.csv"
- 判读以 Bootstrap CI 是否含0为准；解析 p 与 CI 冲突时标"边缘、以PROCESS复核为准"，不夸大；系数与简单斜率SE经 numpy 黄金对照

**v1.14 人口学差异分析版**
- 自动识别人口学分组列（性别/是否独生子女等二分类、年级/生源地等多分类，兼容"男/女"文本），对每个量表总分：2组做独立样本 t＋Cohen's d，3组及以上做单因素 ANOVA＋η²＋Bonferroni 事后两两比较
- 导出"数据名_差异分析.csv"；t/F/d/η² 经 scipy 黄金对照逐位一致；演示数据补性别/年级列（独立随机，不改变既有统计基准）

**v1.13 正态性＋描述/相关/α整合三线表版**
- 描述统计新增 SPSS 口径**偏度/峰度与正态性判读**（Kline |偏度|<3、|峰度|<10，明显偏态走 Bootstrap），经 scipy 黄金对照逐位一致
- `_统计结果.csv` 升级：表1描述+正态性；**表2为论文标准"描述统计＋相关矩阵＋信度"整合表（M/SD、下三角相关带星号、对角 α）**；表3相关明细含双尾 p

**v1.12 题项分析（CITC＋删题α）版**
- 信度分析从"只报总α"升级为逐题输出 **CITC 校正项总相关**与**删题后α**，自动标记 CITC<.40、删题后α升高的题
- 导出"数据名_题项分析.csv"（UTF-8-SIG，可直接整理成信度三线表）；2题量表删题α正确显示 NA 不崩溃
- CITC 与删题α经 Wolfram 小样本整数例黄金对照，逐位一致

**v1.11 Horn平行分析版**
- `--efa` 新增 Horn 平行分析（默认500次随机模拟）：给出随机特征值均值/95%分位与建议因子数，因子数判定从"特征值≥1"升级为 Kaiser＋碎石拐点＋平行分析＋理论四依据
- 碎石图叠加平行分析随机均值线（橙）与95%分位线（紫），一张图同时是碎石图与平行分析图，可直接插入论文
- `--pa-rep 次数` 可调模拟次数、`--pa-rep 0` 关闭；随机相关矩阵特征值经 Wolfram 黄金对照，纯随机数据不误判因子

**v1.10 EFA碎石图自动出图版**
- `--efa` 现在除数值结果外，为每个量表自动导出"数据名_量表名_碎石图.png"（300dpi，含 Kaiser λ=1 参考线、保留因子高亮、特征值标注、拐点清晰），可直接插入论文
- matplotlib 为可选依赖：未安装时仅提示安装，数值结果与退出码不受影响（优雅降级）
- stats-guide、数据分析工作流、e2e 测试 14f 同步

**v1.9 完整探索性因子分析EFA版**
- auto_stats 新增 `--efa`：主成分提取＋特征值≥1定因子数＋Kaiser归一化Varimax最大方差旋转，输出 KMO/Bartlett、全部特征值、旋转载荷矩阵、共同度、交叉载荷/低载荷标记、累计方差解释率，导出 `_因子分析.csv`
- 自编或重大修订的多维量表 EFA 一键完成；Jacobi 谱分解与 Varimax 旋转经 Wolfram 独立黄金验证（2/3 因子构造数据逐元素吻合），碎石图/Promax斜交/CFA 仍引导 JASP
- 修复相关矩阵奇异（题目全同/样本过少）时 KMO 计算崩溃的健壮性问题，改为中文提示并跳过
- 菜单第3项可交互选择是否做 EFA；stats-guide 与数据分析工作流补全 EFA 判定口径与 APA 报告五块

**v1.8.2 启动器编码关键修复**
- 「启动工具箱.bat」改为 GBK 编码＋CRLF＋chcp936，修复中文 cmd 下启动器中文被误当命令的断点；新增未解压防呆；.gitattributes 锁定 .bat 不转换行尾

**v1.8.1 量表库扩充**
- 常用量表对比库扩到 16 类（新增正念 FFMQ/MAAS、生命意义感 MLQ 等）

**v1.8 预置学生工作区版**
- 新增 `我的工作区/`：01-文献PDF、02-问卷数据、03-分析结果三个子目录与"先读我"说明，解压即有清晰归档结构
- 进度卡 `我的论文进度.md` 直接就位，首次对话即可带填；.gitignore 只跟踪占位与进度卡，学生真实文件不入库

**v1.7.1 编码健壮性补丁**
- auto_stats / data_cleaner / literature_organizer 读取文件自动兼容 UTF-8(含BOM) 与 GBK/GB18030
- 修复小白跳过预处理、直接喂问卷星 GBK 编码文件会崩溃的断点；菜单与版本标识同步完善

**v1.7 研究对象频数自动化版**
- auto_stats 自动识别人口学分类列（性别/年级/生源地等，兼容文本与1/2编码），输出频数与百分比并导出 `_频数表.csv`
- 自动排除量表题与连续变量，直接服务论文"研究对象/样本构成"部分

**v1.6 结构效度自动化版**
- auto_stats 增加结构效度检验：每个量表自动出 KMO、Bartlett球形检验(χ²/df/p)、第一主成分方差解释率与各题因子载荷
- 单维量表（引用/改编成熟量表）效度一键出齐；多维自编量表仍引导JASP/SPSS做完整EFA/CFA
- 至此本科问卷量化论文标准分析（描述/频数、信度、效度、共同方法偏差、相关、回归、Bootstrap中介）全部脚本化，JASP/SPSS仅作复核

**v1.5 中介分析自动化版**
- auto_stats 增加 Bootstrap 中介分析：`--mediators` 一个中介=模型4、两个=模型6链式
- 自动输出路径系数、各条间接效应、Boot SE、95%CI，判断完全/部分中介与链式路径
- 演示数据升级为 X→孤独感→反刍→NSSI 链式四量表（贴合典型本科问卷模型）
- 数据分析从"相关回归后还要装JASP做中介"变为"一条命令出完整中介结果"，JASP仅作复核

**v1.4 共同方法偏差+练手数据版**
- auto_stats 增加 Harman 单因子检验（自动报告第一公因子解释率与40%判断）
- 新增演示数据生成器，没收回问卷也能跑通全流程；全链路真人走查修复小白断点

**v1.3 统计闭环增强版**
- auto_stats支持反向题标记，修复"含反向题不反转导致α为负"的正确性缺陷
- 自动计算量表总分/均分并导出数据集，JASP/SPSS/PROCESS直接用
- 提供量表配置时，信度/描述/相关/回归自动在量表总分层面完成
- 新增结构真实的演示数据（α .89-.93、链式相关、典型中介回归结果）

**v1.2 零门槛闭环版**
- 问卷星原始数据一键预处理（wjx_preprocess.py，实测中文表头/文本选项/用时全兼容）
- 统一中文菜单 menu.py + 双击启动器，文件可拖拽，无需记命令
- 进度卡文件化（我的论文进度.md），换对话/换AI无缝续接
- QUICKSTART.md 电脑小白快速上手
- 数据分析工作流补全"问卷星→预处理→清洗→统计"完整闭环
- 端到端测试扩充到22个用例 + 脚本回归清单

**v1.1 超级向导版**
- 8个全程工作流手册
- 5个自动化脚本（含联网文献检索、自动统计）
- 环境搭建傻瓜指南、Zotero+Obsidian工具链
- 开题、答辩、写作专项指南
- AI素养教育模块

v1.0：12阶段工作流、4人格、3难度、三道检查闸、8个错误处理、紧急模式