# 待办编号索引（P0–P21）

> **为什么有这个文件**：包内 `CHANGELOG.md` 用"待办 P16 / P9 / P15 / P7"这类编号交代一条改动的来历，
> 但这些编号过去**只在仓库外**的 `_归档/待办计划-拆分与发版收尾.md` 里定义——git 管不到、断言看不见，
> 于是发出去的账本里出现了指向查不到的编号。本文件把这些编号在仓库内补齐成可查的定义。
>
> **本文件只钉不会变的东西**：编号 → 这件事是什么 → 交付在第几版。
> **活的进度（未做/进行中/被什么挡住）不在这里抄一份**——那正是要抄就漂的东西；
> 权威工作队列仍是仓库外那份 `_归档/待办计划-拆分与发版收尾.md`（不受 git 管，只在本机）。
> 规矩与本目录一致：**只进不改**（见 `维护档案/README.md`）。

| 编号 | 这件事是什么 | 交付版本 |
|---|---|---|
| P0 | 推送 GitHub 并建 Release（本地领先远端 32 提交 / 16 tag） | 未交付——链路问题，见下方备注 |
| P1 | `core/coach-rules.md` 三节流程正文下沉为按需读 | v1.84 |
| P2 | 真人走查：填进度卡 → 菜单第 23 项自检 → 第 21 项出 PPT | 第一轮=v1.87；后续轮次未交付 |
| P3 | 存量超标文件随改随降（配合尺寸棘轮） | 长期机制，无单一交付版本 |
| P4 | `_拆分工作区` 的 git worktree 收尾清理 | 已清理 |
| P5 | 开题就绪度自检工具 `tools/proposal_readiness.py`（菜单第 23 项） | v1.85 |
| P6 | 输出编码守卫改为无条件生效（`>/dev/null` 下 GBK 崩溃的根因修复） | v1.86 |
| P7 | 轻量版（豆包 Skill）硬口径追平完整版 | v1.89（Skill v1.5） |
| P8 | 就绪度自检与 `templates/proposal-template.md`（报告体）对齐、结果回写进度卡 | v1.94 |
| P9 | 进度卡去隐私化：包内只留空白基线，填写版由菜单第 22 项"缺才生成" | v1.92 |
| P10 | 论文材料与介绍页的下落（用户自行移出开发目录，介绍页不归本仓管辖） | 已结（无需交付） |
| P11 | 包内示例题目是否换成虚构题（用户明确"不用换"，保留本人选题作示例） | 已结（决定不改） |
| P12 | 发版收尾三件：`_构建验证/` 旧副本清理 ＋ 豆包桌面版安装位镜像同步 | ①② 已做；镜像那一件未交付 |
| P13 | 严谨性行为的真机验证（11 条行为用例目前只有文本级断言） | 未交付 |
| P14 | 取证机制往工具延伸：文献卡片与检索记录带"逐字原文＋位置"字段 | 未交付 |
| P15 | A/B 实验抓到的真缺陷：学生催"只回一个数字"时，装了规则的回答也会掉形制 | v1.94（下界已立，实测"加这条测不出增益"——v1.91 实验发现） |
| P16 | 学生发布包去掉开发文件（`tests/` 与开发流程文档） | v1.93 |
| P16b | 包里剩下的三类开发内容：记账文档、`.git*` 配置、轻量版构建件 | 未交付 |
| P17 | 一键升级入口（`升级.bat` ＋ 菜单第 **26** 项，只覆盖工作区以外、覆盖前整份备份；编号因 v1.96 占用 24/25 而后移） | 未交付 |
| P18 | `core/coaching-protocol.md`（含轻量版镜像）218/220 贴闸，加勾前须先下沉取证细则 | 未交付 |
| P19 | `doubao-skill/CHANGELOG.md` 是记账类却吃着 220 行数闸（分类不一致） | v1.96（走归档，不抬闸） |
| P20 | "包内档案指针不得成坏链"写成断言（v1.95 搬账时差点挤掉一份指针，靠手工比对才发现） | 未交付 |
| P21 | T58（AI 替学生逐句改写）只有行为用例、没有机判尺：v1.96 的 A-B 实验里无规则受试正撞这条却判不出 | 未交付 |

## 备注

- **P0 为什么单独说**：它不是没做而是做不了——到 GitHub 的链路八次实测均 443 不通（记录在仓库外待办文件的"P0"一节）。
  本机 `_发布包/发布记录.md` 里"未推送"是真话，别凭这个编号断定远端有货。
- **新增编号时**：在这张表补一行，同时确认 `CHANGELOG.md` 顶部说明里那句"编号定义在 `维护档案/待办编号索引.md`"仍然成立。
  回归里 `待办编号引用都能在本仓解析` 那条断言（`tests/e2e_cases/case_02.py`）会盯住：
  账本里出现本表没有的 P 号 → 判红。
