**v1.95 两处贴脸的 220 硬闸用拆解决，不抬数字**
> 发布：2026-09-21
- **为什么现在做**：棘轮当天一次打 21 条 WARN，其中三处是**贴着线**——主手册 218/220、两份
  `coaching-protocol.md` 218/220、回归壳 **220/220**。壳最要命：它的长度是**记账式增长**，
  **每注册一个新片段就占一行**，下一版加 `case_23` 就会当场判红。这不是"再压一行注释"能续命的。
- **壳拆助手模块**：`new_tmp`/`rmtree_retry`/`dir_state`/`leftover_dirs`/`cleanup`/`_stray_files` 六段
  实现原样搬进 `tests/e2e_tmp.py`（140 行），壳 220 → **100 行**，只多 4 行 import。
  硬证据两条：拼回与 `git show HEAD:tests/full_e2e.py` **逐字相同**；拆前后 830 条断言的名字与顺序
  **逐字节相同**（这条比"总数看着够"强得多，是 v1.82 拆分那轮定下的验收手法）。
- **主手册再下沉两处**（照 v1.84 的批3 手法）：三种可选语气的措辞分寸 → `core/coach-rules/tones.md`，
  紧急模式 7天/3天/1天逐日方案 → `core/coach-rules/emergency.md`；正文逐字未改、小节标题与指针留在原处、
  `tdoc()` 把它们拼回同一份逻辑文档，所以**盯正文的断言一个字都没动**。主手册 218 → **190 行**、
  14,295 → **13,030 字节**。腾出的 30 行正好覆盖待决的"语气扩展"（用户 09-21 提的搞笑演绎档，约需 15 行）。
- **把"别再把闸填满"变成机器盯的事**：`case_16` 加 6 条，含一条阴性（把分片正文抄回主手册必须判红）
  和一条 `v195 主手册与回归壳都为下次改动留了余量`（主手册 ≥20 行、壳 ≥100 行）——不靠下一个人的自觉。
  包内必带清单 26 → **28 件**（两份新分片必须在包里）。
- **"该拆还是该抬"给了判据，不再每次重吵**：`维护档案/维护决定-文件行数上限与拆分口径-2026-09-21.md`
  按**"这个文件的长度是谁在付成本"**把受管文件分四类——每轮必读的规则文档（只拆不抬：抬一次＝全体学生
  每一轮都多读）、按需读的分册（维持硬闸＋冻结存量，**不新增"可 320 行"那一档**：多一档口径就多一处会漂）、
  代码（220 是可维护性启发式，长度是职责要求就去拆）、记账类（`LEDGER` 豁免＋各配一条自己的硬闸）。
  **整体抬到 300 明确不抬**，谁要抬先回答"这文件是不是每轮必读"，并把理由写进 `size_ratchet.py` 的 docstring。
- **本轮没解决的两件，各留了触发器**：①`core/coaching-protocol.md` 仍是 218/220——**下一版只要往第十节
  加一条勾就必须先拆**（做法与本轮相同；读它的 `tx()` 有六处要换成 `tdoc()`，`case_22` 已是）；
  但 `case_05` 钉着"触发边界/边界情况/学生长时间失联/复合请求"四个锚点，删字面量＝改判据、不是精简。
  ②`doubao-skill/CHANGELOG.md`（197/220）其实是记账类却吃着行数闸，
  **分类不一致**——下一次出 Skill 条目前先归档旧条目、或纳入 `LEDGER` 并同时补一条行数断言。
- **测试**：`full_e2e` 830 → **836**（六条新的，含一条阴性与一条余量闸）；行数上限的余量看
  `python tests/size_ratchet.py` 的 WARN 行，这里不抄数字。
