# 维护决定 · 2026-09-20 · 两份工具的分工与同步方向

> 记账类档案（git 追踪、`export-ignore` 不进发布包）。包内 `CHANGELOG.md` 的"维护决定（不发版）"只留一条指针，
> 正文在这里——这样既不撞它的 `<320 行` 断言，也不让决定文本被删掉。

## 决定（用户 2026-09-20 原话）

> "d 盘那份是给学生用的，E 盘那份是给开发用的。"

| 位置 | 是什么 | 谁改 | memory |
|---|---|---|---|
| `E:\毕业论文\thesis-ai-coach-project\` | **开发源**（唯一 git 仓，发版/门禁/待办都在这儿） | 先改这里 | 项目记忆正常维护（`projects\E------\memory\` 那两份是交接依据） |
| `D:\桌面\thesis-ai-coach-project-v1.86\` | **给学生用的那一份**（发布包解压件，无 `.git`） | **用户自己手动装包，AI 不更新它** | **不写任何 memory**，留痕只进它自己的进度卡 |

**AI 不在 D 那份上做开发、不拿它定口径**；"现在真正的最新版"只看本仓 `CHANGELOG.md` 的版本索引。
本仓是唯一改动场所：改完发版 → 用户手动把发布包装到 D。

## ⚠️ 装包前必读：包里那份进度卡是空白模板，会盖掉学生本人的记录

包是 `git archive` 打的，`.gitattributes` 只 `export-ignore` 了 `维护档案/**`，**`我的工作区/我的论文进度.md` 随包分发**。
三方现状（2026-09-20 实测）：

| 哪一份 | 内容 | 行数 | 最后改动 | 在不在 git 里 |
|---|---|---|---|---|
| **D 那份**（学生用的） | 他本人的真实进展：**读导师截图得出 9-23 开题与四条要求**、**CAIDS-20 双源核实并出问卷 v2**（含注意力检测与质控标准）、D1 待决、4 天倒计时 | 188 | 09-19 21:51 | ❌ D 无 `.git`，**没有第二份** |
| E 本仓（工作区） | 09-19 真人走查填的**测试态**：偏好定档、既有成果归档、跑通就绪度自检——**与上面不同内容** | 137 | 09-19 17:01 | ⚠️ **未提交**（`git status` 显示 M） |
| E 本仓（已提交/即打包进去的那份） | **空白模板**（姓名/题目/量表全空，选项并列） | — | — | ✅ 在 |

**所以：手动装包 = 用空白模板覆盖他唯一的论文记录，且不可回滚。**

1. **装包前**先把 `D:\...\thesis-ai-coach-project\我的工作区\` 整目录复制到仓库外；装完再放回去。
2. 更根本的修法（建议）：把 D 那份的真实进展**逐段合并进本仓的进度卡并提交**，让包里从此带着他的内容——
   需要他本人点头，因为那涉及把他的论文数据写进一个会被下载分发的包（本卡顶部那条隐私警告就是这个意思）。
3. 顺手处理 D 那份自己留的提醒："旧交接文档 `会话交接文档_毕业论文全流程.md`（2026-09-07）里的
   `C:\Users\乏味\Doubao\chats\...` 路径已过期，以进度卡为准"。

## 路径雷（实测）

D 那份解压后**多一层同名目录**，进度卡真实位置是
`D:\桌面\thesis-ai-coach-project-v1.86\thesis-ai-coach-project\我的工作区\我的论文进度.md`。
按 `...v1.86\我的工作区\...` 拼路径会落到**不存在**的位置，容易顺手新建出第二张卡。

## 来源

Qoder 记忆库每周体检：`E:\qoder-config\docs\memory-health.md`（③A-1/A-2、⑦ 执行记录、⑧ 同步警告）。
记忆侧的权威说法：`E:\qoder-config\memory\feedback-no-memory-in-thesis-coach.md`。
