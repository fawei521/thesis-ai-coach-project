# CHANGELOG 历史详情 · v1.88–v1.91（v1.94 发版时归档）

> 记账类档案（git 完整追踪、`.gitattributes` 用 `export-ignore` 排除在发布包之外）。
> 为什么搬：包内 `CHANGELOG.md` 有 `<320 行` 的硬闸（`case_15.py` 盯），每版必须追加就得定期把最老的详情搬出来。
> 包内保留的是**全量版本索引**＋ v1.91 起的近期详情；索引一行没删。
> 这三版里 v1.90/v1.91 是用户 2026-09-20 点名"降低 AI 幻觉、要求反复验证"那条线上的头两版，
> 读"严谨性是怎么一步步接进每轮门禁的"从 v1.90 这块开始看最省事。

---

**v1.91 宪法级严谨性条款＋可复跑的 A/B 对照实验（用户点名要"补进全局宪法"并"测试实验"）**
- **宪法第三条升格为"先有证据，再下结论"**，加四条可判形制（不只是"要准确"这种口号）：
  **顺序不许反**（先贴逐字原文＋位置再判断，反过来即粗心违规）、**空位可以留空**（原文没写就写"原文未报告"，
  不许用印象/推断/**别的研究的数**填坑）、**交叉验证必须换独立来源**（另一个 AI 的复述、同页第二次摘录、
  自己上一轮的记忆都不算第二源）、**流利不等于准确**（句子越顺越要回原文核）。
- **宪法第十条（维护者）加一条**："要求 AI 怎么想"的规则也算功能，新增必须同时交付**行为用例＋能复跑的对照实验**，
  **只把话写进文档＝未解决**——这条是给本版自己定的规矩，也是本条存在的原因。
- **实验逼出来的一条新规矩**：`core/evidence-rigor.md` 三查之后加"**上下文会被压缩，印象不是原文**"
  （上一轮读到的原文，本轮要用就重读；换对话换 AI 更要重读）。用户的原始事故正发生在这一格：不是模型笨，是它手里没有原文。
- **对照实验**（`tests/rigor_experiment.py`，可 `python tests/rigor_experiment.py` 复跑）：夹具 T2「真含糊」
  （压平的介绍页：6 个 α 配 7 个题数、表头全丢）＋ T3「同源盖章」（只有学生抄的错版清单），
  三条件（无规则／v1.89 旧规则／v1.91 新规则）× 独立子代理。**结果不给自己贴金**：
  **没有一组报出错误的 α**——单轮强模型测不出"会不会说错数"的差异；测得出的是**形式可稽核性**
  （三态标注 新规则 4/4、无规则 2/3），以及"被催着只给一个数"时**新规则组也会掉形**（真缺陷，挂待办 P15）。
  判据含阴性：三份植入的坏回答（盖章型／填空型／编数型）必须被判红。完整记录与两次方法学事故见
  `_归档/审查报告/2026-09-20 严谨性闸 A-B 实验.md`。
- 轻量版同步出 **Skill v1.7**：镜像同一批新锚点 ＋ 门禁第 4 条 ＋ 自测 T47（被催"只回一个数字"）与 T48（凭印象复述）。
- `case_20.py` 新片段 17 条（含判据不空转、夹具不用真量表名、结论行不许出现"已解决"）；full_e2e 733→**750**，
  阶段 G 干净副本 **743** 项（比开发树少 7 条：6 条 `.git`/档案目录专属核对＋1 条"`_归档` 实验报告已落盘"，包内自然跳过）。

**v1.90 证据与核验纪律：摘要不算原文（学生侧规则文本两侧同改，工具行为新增一条缺项）**
- **起因是学生报的真实事故形态**：网上抓论文时只读摘要/检索页就报数据、把二手网页里并列的**别的量表**的信度安到学生选的
  量表上、关键词搜漏就宣布"原文没报"、被质疑时用一条新结论盖旧结论。旧文本里这些要求散在 ai-literacy /
  paper-reading-guide / literature-auto-search 多页，且全是软措辞（"必须真实""让学生核实"）——**没有可判定的动作**。
- **新增权威源 `core/evidence-rigor.md`**：三态标注（已核实/推断/未查到）、引用四要素（谁/逐字原文/节号表号/
  读的是什么材料）、切片禁令、**数字三查**（回读定位→对象对齐→换源复核）、否定结论四步（按标题通读＋≥3组词重检＋
  查条数截断＋报"均未检索到"）、说矛盾前先对齐对象、**更正门槛高于原结论**、落盘的错必须清点、`[需核实]` 不进正式稿、紧急模式不打折。
- **接进每轮门禁而不是再强调一次**：`core/coaching-protocol.md` 第十节产出前清单由 1 条变 3 条硬动作，第一节"不可以做"
  写明"把片段当原文与编造同级"；coach-rules 核心原则2、START 按需清单、AGENTS 文件地图、ai-literacy 第 8 条
  （教学生追问"你读到的是全文还是摘要"）同步接线；阶段细则与 `proposal-guide` 量表出处精确到节号/表号。
- **落到取数现场**：IMRaD 卡片加**读取范围声明**与**取证栏**（填不出取证行的数字不得进正式稿）、"AI不能做的"四条扩为七条；
  知网工作流第八节两条软措辞改成三查口径并加一条闭环核对项。
- **机器真拦**：`proposal_readiness.py` 把大纲里未清的 `[需核实]` 按行列成**缺项**（`--strict` 退出码 1），阶段 6 准出与
  `stage-checklist` 同步；工具行数 219→220 卡进棘轮线内（压掉两处旧注释各一行才腾出来，这正是棘轮的设计意图）。
- **轻量版同步出 Skill v1.6**：镜像 `references/evidence-rigor.md` ＋ 门禁同款三条 ＋ SKILL 运行铁律与加载规则、
  academic-norms、ai-basics、stage-2/3/4/6 接线 ＋ T44–T46；`validate.py` 把它列为**必备件**（缺件判红，现场验证过）。
- `case_19.py` 新片段 22 条（两侧十一项口径逐条同源、删一个锚点即判红的阴性、干净大纲不误报）；`case_06` 那条
  "自测用例 ==45" 改为 `>=45 且连续`，否则**每加一条用例就假红一次**。full_e2e 711→**733**，阶段 G 干净副本 **727** 项全绿
  （少的 6 条是 `.git`/档案目录专属核对，属既有口径）。
**v1.89 轻量版口径追平（Skill v1.5）＋三处混行尾雷（学生侧规则文本只在 doubao-skill/ 内变化）**
- **待办 P7 结账**：轻量版停在 v1.61 那套口径，v1.62–v1.87 落下的**能力面**逐节追平（数字本身上一版实测就没漂，
  漂的是"有没有讲到"）：阶段 6 加**开题就绪度自检**（手机对照清单＋回电脑补跑第 23 项，四档只报问题不代写）；
  阶段 8 加 McDonald's ω、CR/AVE/HTMT、前提假设（Shapiro-Wilk＋偏度峰度 Kline 判据＋Brown-Forsythe→Welch）、
  多重比较校正（Bonferroni/Holm 控 FWER、BH/BY 控 FDR，要求报校正前后 p）、配对 d_z/rank-biserial r、回归 D-W 与残差正态；
  阶段 7 缺失从"只看比例"升级为"先看比例再看机制"（MCAR/MAR→MI/FIML/MNAR 三条路＋"处理口径必须在出结果前定下"）；
  `tools.md` §四 的完整版能力清单同步 23 项菜单。详述只在 `doubao-skill/CHANGELOG.md` v1.5 一处。
- **修一处真错**：完整版"答辩高频问题库（**24 问**、7 类）"在轻量版五处被写成"**24 类**高频问题"——学生照着找 24 个分类会找不到。
  新增守卫口径"答辩高频问题条数"（`skill_sync_check.py` 第 7 条）与 `case_18` 锚点钉住两侧写法。
- **`case_18.py` 新片段**（15 条）：追平的口径逐条有锚点；**轻量版点名的完整版脚本与菜单编号必须真实存在**
  （`consistency_check.py` 整目录跳过 `doubao-skill/`，这活儿过去没人干）——菜单项数从标签 `【N/M】` 自己数，
  不写死；配两条阴性：植入 `ghost_tool_xyz`（探针名故意省扩展名，否则被本文件自己的幽灵脚本核对判红——这颗雷第三次踩）与"菜单第 99 项"必须被抓、正常输入不误伤。
- **三处混行尾，其中一处是哑雷**：`tests/full_e2e.py` 里有一个**裸 `\r`** 把 `case_16` 与 `case_17` 挤在同一显示行——
  Python 认它当换行所以 case_17 一直在跑，但 sed/grep/编辑器都看不见这条注册，谁改那行就会把 case_17 的 12 条断言静默摘掉。
  另两处是 `tools/pptx_writer.py`（3 处裸 LF）与 `templates/progress-template.md`（1 处）。全部归一 CRLF 后加断言
  `v189同一文件内行尾单一` 与尺子自检（`v189行尾尺子算得准`）——这条规矩 DEVELOPMENT 早就写了，之前没人量。
- 守卫同步升级：`skill_sync_check.py` 现在比四件事（新增"SKILL.md 自报的 Skill 版本必须等于 CHANGELOG 最新条目"），
  阴性自测相应加一项。**完整版学生侧命令与工具行为零变化**；轻量版镜像 `.user_skills/thesis-ai-coach/` 需整目录覆盖同步。
- **顺手修一把尺子的查找范围**：`v175 ROADMAP引用的脚本都存在` 找脚本的目录清单漏了 `tests/e2e_cases/`（v1.82 拆分时才有的目录），
  所以 ROADMAP 一提 `case_18.py` 就被判幽灵——按"改尺子而不是绕开它"的规矩补上目录，而不是把文档里的名字改没。
  同版本另踩到一次同类雷：本文件写阴性探针名时带了 `.py`，被 consistency 的幽灵脚本核对判红三次，探针名一律省扩展名。
**v1.88 手机保底文件真正进包 + 轻量版跨包口径守卫（学生侧命令与规则零变化）**
- **一条发不出去的承诺**：`doubao-skill/` 的 README 与 SKILL.md 都说手机侧"保底一定能用"的合并单文件
  `thesis-ai-coach-手机版.md`，但它被 `.gitignore` 当构建产物排除，而发布包由 `git archive` 生成、只装跟踪文件
  → 实测 **v1.87 的完整包 209 条目与 Skill 独立包里都没有它**（本地那份 184 KB 只是散在目录里）。
  现改为入库随包分发；`.skillignore` 仍把它挡在技能目录导入之外，豆包侧行为不变。
- **自检盲区**：`validate.py` 第 8 项过去是"产物在才核对同步"，缺件照样打印"全部通过，可以单独分发"。
  现缺件与过期一律判红并给出重新生成命令。阴性验证＝把产物移走再跑 → 退出码 1、提示指对地方。
- **新守卫 `tests/skill_sync_check.py`**（203 行、纯标准库、带 `--selftest`）：`consistency_check.py` 一直整目录
  跳过 `doubao-skill/`，轻量版 `validate.py` 又只看目录内部，"两侧口径同源"这条设计原则过去只有人的记性能发现。
  现用同一条正则从两侧抽取六项硬口径（简单中介 ≥200／链式 ≥300／Harman 40%／Bootstrap 5000／12356／五指标）逐项比对，
  另钉三条版本自述规矩：学生可见文件不得手写完整版版本号、轻量版不得自称超前、落后不得超过 15 个 minor。
  七项阴性自测全过——其中"手写自述要判红"是重写后才抓到的：第一版锚点只盯 `完整版 v`，对"完整版已到 v1.78"这种写法
  **空转报绿**，被自家阴性测试当场判 FAIL（这颗雷本仓库记过三次，第四次仍然靠阴性测试兜住）。
- **实测订正一份外部审计报告**：它称"完整版刚把开局必读改小、轻量版反而还要求连读 5 份 48.5 KB"。同尺量下来
  完整版启动必读 7 份 **87.3 KB**、轻量版 5 份 **47.3 KB**，两侧都是"规则必读＋流程按需读"，**方向性落后不成立**；
  六项硬口径数字也全部一致。真正落后的是能力面（ω/HTMT/前提假设/多重比较校正/缺失机制/就绪度自检在轻量版零命中），
  挂 ROADMAP 待出 Skill v1.5，并记下轻量版 stage-11 把完整版"答辩高频问题库（24 问、7 类）"误写成"24 类"。
- **版本自述收敛**：删掉 `doubao-skill/README.md` 末尾那份重复的七条版本清单，SKILL.md 顶行不再手写完整版号；
  同一个数字过去四处各写一遍，实测已是三个互相矛盾的值（v1.61／v1.78／v1.81）。
- **测试**：+3 条（`v188跨包口径同源核对0`、`v188跨包守卫抓得到变异`、`v188手机合并单文件已入库随包`），
  写在 `case_05.py`；`validate.py` 240→239 行（新增两项检查、压缩常量与 `md_files()` 抵回，冻结值同步收紧）。
  full_e2e 693→696 项。