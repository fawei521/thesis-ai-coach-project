# Skill 版本历史（thesis-ai-coach · 豆包 Skill 版）

> Skill 独立于完整版项目做语义化版本；**每条注明对应的完整版版本，且这一处是全包唯一的版本自述**
> （SKILL.md 与 README 都不再复写完整版版本号，由 `tests/skill_sync_check.py` 盯住）。

## v1.4.2（2026-09-19，对应完整版项目 v1.88）：手机保底文件真正随包＋版本自述收敛（学生读到的规则零变化）

- **修一条发不出去的承诺**：README 与 SKILL.md 都说手机侧"保底一定能用"的合并单文件 `thesis-ai-coach-手机版.md`，
  但它过去被 `.gitignore` 当构建产物排除，而发布包由 `git archive` 生成、只装 git 跟踪的文件 →
  实测 v1.87 的完整包（209 条目）与 Skill 独立包里**都没有这份文件**，手机学生拿到包也发不出它。
  现改为**入库随包分发**（`.skillignore` 仍把它挡在技能目录导入之外，豆包侧行为不变）。
- **补一处自检盲区**：`validate.py` 第 8 项过去是"产物存在才核对同步"，缺件时照样打印"全部通过，可以单独分发"。
  现缺件与过期一律判红，并给出可执行的重新生成命令（实测移走产物 → 退出码 1、提示准确）。
- **版本自述收敛到一处**：删掉 README 末尾整份重复的版本清单（七条、各带一个完整版号），
  SKILL.md 顶行不再手写完整版号。四处手写实测已漂成三个互相矛盾的数字——
  SKILL.md 说同步点 v1.61、README 与 CHANGELOG 说 v1.78、完整版 ROADMAP 说 v1.81。
- **新增跨包守卫**（完整版侧，`python tests/skill_sync_check.py`）：两侧用同一条正则抽取样本量下限、
  清洗指标数、Harman 门槛、Bootstrap 次数、热线号码等硬口径逐项比对，并钉住"学生可见文件不得手写完整版版本号"
  "轻量版不得落后完整版超过 15 个 minor"。七项阴性自测（`--selftest`）确认这把尺抓得到植入的漂移。
  实测六条硬口径**两侧一致**（数字本身没漂）；真正落后的是能力面（ω/HTMT/前提假设/多重比较校正/就绪度自检），
  已连同"stage-11 把完整版'24 问'误写成'24 类'"一并挂进 ROADMAP，逐节核对后出 v1.5。
- **影响面**：只动打包链路与版本自述，12 阶段、规则、口径文本一字未改；已发给学生的手机版本身内容正确。
  校验：`validate.py` 退出 0、`build_mobile_single.py --check` 通过、`skill_sync_check` 与 `--selftest` 均退出 0、
  完整版 `full_e2e.py` 全绿。

## v1.4.1（2026-09-19，对应完整版项目 v1.78）：合并单文件生成路径修复（维护者侧，学生读到的规则零变化）

- **问题**：`build_mobile_single.py` 的 `--out` 默认按**当前目录**解析，从项目根执行会把合并单文件写到项目根
  （那条路径不在 `.gitignore` 里，容易被 `git add -A` 误提交），而 `validate.py` 检查的是 `doubao-skill/` 里那份
  → 无论重新生成多少次，自检都报"与源文件不同步"；本 README 的发布流程第 2 步又写成只往分发包生成，
  受检那份必然长期过期（v1.4 改名后的三个旧语气文件名就一直留在里面）。
- **修**：默认输出改为技能目录内那一份（与 `validate.py` 受检、`.gitignore` 排除的路径完全一致）；
  显式 `--out` 的相对路径语义不变；`validate.py` 报不同步时给出可直接执行的重新生成命令；
  README 用法与发布流程改为"先在本目录生成、再拷进分发包"。
- **影响面**：只动 `build_mobile_single.py` / `validate.py` / `README.md` 三处维护者工具与说明，
  12 阶段、规则、口径文本一字未改；已发布给学生的 `thesis-ai-coach-手机版.md` 本来就是正确版本。
- 校验：`python validate.py` 退出 0；`build_mobile_single.py --check` 通过；完整版 `full_e2e.py` 636 项全过
  （含两条新加的"生成方与校验方同一路径"行为锁定）。

## v1.4（2026-09-18，对应完整版 v1.61）：手机边界修正＋规则优先级＋去人设命名＋AI 正文草稿边界

- **手机能力边界（P0 修正）**：旧表述"手机能做阶段 0–7"与阶段 7 预处理必须回电脑矛盾。现统一：阶段 0–6 手机全程可做；阶段 7 仅收数/监控/备份/导出可手机，预处理、五指标清洗、反向计分、量表总分必须回电脑；阶段 8 全部统计与出图必须回电脑；阶段 9/11 手机起草+电脑定稿（排版、查重、AI 检测、PPT 定稿回电脑）。设备交接提醒提前到阶段 6 开题通过后；合并单文件头部"给学生的三句话"同步改写。
- **规则优先级（P1）**：SKILL.md 与合并单文件开头新增六层冲突顺序（coaching-protocol → companionship → stages → encouragement-guide → 语气文件 → 参考常识），语气文件只改说法不改规则，任何语气不得违反前两层。
- **语气文件去人设化命名（P1）**：gentle-sister.md→gentle-patient.md、strict-ceo.md→concise-direct.md、puppy.md→lively-warm.md（default.md 不变），全部引用与校验同步。
- **统计阈值标争议（P1）**：stage-8 第三节加"判读标准说明"；α、KMO、Harman、中介四处各加"[阈值因教材而异，结合导师意见]"。
- **授权分级与伦理本地化（P1）**：stage-4 第 5 步改四级授权分类；数据保存年限、未成年人同意、敏感主题流程以本校文件为准。
- **进度卡更新块（P1）**：coaching-protocol 第六节新增每阶段过闸后 AI 主动输出的可粘贴更新模板。
- **AI 正文草稿边界（P1，用户授权）**：academic-norms 新增"AI 可做与不可做（边界清单）"——经学生明确要求、基于真实材料可生成**可编辑的论文正文草稿**，但必须标注"AI 草稿、不可直接提交"、逐段报告风险、给核对清单，学生逐句核实并用自己的话改写；量表题目、编造、无材料凭空生成、高风险决定、可直接交定稿仍禁止；不改不核实直接提交仍按全文代写红线处理。SKILL/coaching-protocol/stage-9/stage-checklist/ai-basics/faq/self-test 同步。
- **安装/获取说明（P2）**：mobile-guide 补合并单文件截断时"分步喂"提示；tools 完整版获取要求核对版本号与校验值。
- 校验：`python validate.py` 退出 0；合并单文件重新生成、`--check` 通过；full_e2e 新增 v1.61 断言块并全过。

## v1.3（2026-09-17，对应完整版 v1.58）：去老师化＋稳定陪伴＋多词检索＋重点卡片＋手机原生适配

- **身份与陪伴**：新增 `references/companionship.md`（镜像完整版 `core/companionship.md`，伦理指针指向本包 `coaching-protocol.md` 第十一节）：AI 是工具/学习伙伴，不自称老师、不扮演虚拟伴侣；给稳定但不越界的陪伴，识别情感依赖信号并温和拉回，含未成年人条款与回复前自检。`SKILL.md`、四份人设与 12 个阶段文件全面去老师化（"导师动作"改"助手动作"，指学生真正学校导师的称谓保留）。
- **语气系统替代人格系统**：默认"自然、不套人设"；三种可选人设改为三种**语气**——简洁直接(strict-ceo)/温和耐心(gentle-sister)/活泼热情(puppy，大幅降温、去撒娇与依附暗示)；人设文件名不变以免破坏校验。
- **鼓励自然化**：重写 `references/encouragement-guide.md`，删逐句台词与机械计数，只给原则、结合学生状态与任务难度自然发挥；理论锚点全保留。
- **多词检索 + 90 篇候选池 + 卡片**：阶段 2/4 改为概念—检索词矩阵（每概念 ≥3 近义词、概念内 OR/概念间 AND、多库综合，禁止单词下结论）；英文候选池目标 ≥90 篇、精读 10–20；重点文献用 `literature_cards.py` 生成手机友好卡片网页（完整版工具，手机端可让 AI 按同版式在对话里生成单文件 HTML）。
- **手机原生适配**：重写 `references/mobile-guide.md`，按手机真实能力给能力边界表与各环节原生做法（全球学术快报 App、Scholar/PubMed 移动网页、PDF 拍照、问卷星 App、语音输入），新增手机↔电脑"设备交接单"，明确手机装不了 Python/虚拟电脑、统计重活回电脑；合并单文件构建脚本同步纳入 companionship 并去老师化。
- **自测与校验**：`self-test.md` 用例重排为连续 T1–T36 并补陪伴/多词/卡片/交接场景；`validate.py` 增加 companionship 必备文件、关键词与去老师化检查。

## v1.2（2026-09-17，对应完整版 v1.56.1）：手机独立版——可单独拿走、手机可用

**目标**：本 Skill 此前虽然是"轻量版"，但仍嵌在完整版项目里、并多处引导使用电脑专属工具
（`启动工具箱.bat`、`wjx_preprocess`、`data_cleaner`）。本轮把它做成**可单独分发、手机可独立使用**的形态。

- **新增 `references/mobile-guide.md`**（手机使用说明与能力边界）
  - **先说清不能做什么**：手机能做阶段 0–7 与 9–11；**数据预处理、五指标清洗、全部统计分析、出图必须回电脑**
    （JASP/SPSS/PROCESS 都是电脑软件）。**第 8 阶段只能做一半**——带学生搞懂"要跑哪些分析、指标怎么读"，
    但跑数换设备。
  - **学生只有手机时的三条路**：① 借电脑按阶段 8 做；② 电脑跑完把结果带回手机解读；③ 借不到就跟导师说明、
    申请机房或请同学协助跑数（**分析思路与结论仍必须是他自己的**）。
  - **三种启用方式**：手机豆包 App 的技能入口（**入口措辞随版本变化，标 `[需核实]`**）／合并单文件（保底）／桌面版技能目录。
  - 手机操作贴士（长文本粘贴校验、拍照发 PDF、进度文件存哪儿、语音输入的坑）与手机端隐私注意（截图去标识、账号密码本人操作）。
- **新增 `build_mobile_single.py`**：把整个 Skill **逐文件原样拼成一个 md**（`thesis-ai-coach-手机版.md`），
  供只收一个文件的手机 AI 使用；带**内容指纹**，`--check` 可校验合并版是否与源文件同步。不改写任何规则文字。
- **`SKILL.md`**：新增"你在手机上工作——先说清能力边界"一节（含"第 7 阶段收尾前主动提醒下一步要电脑"、
  "不教手机硬算、不凭口述数字估结果"）；文件加载规则与文件结构同步新增项；版本号 → v1.2。
- **`stages/stage-7-data.md`**：加"📱 本阶段是手机与电脑的分界点"横幅——手机管发问卷/回收/备份，**预处理与清洗换电脑**。
- **`stages/stage-8-analysis.md`**：加"⛔ 本阶段需要电脑（手机上只能做一半）"横幅 + 能/不能对照表 + 三条路。
- **`references/tools.md`**：新增"〇、手机上能用什么、不能用什么"对照表；软件表逐行标注是否电脑专属；
  第四节由"完整版项目包"改为"完整版项目包（电脑用，可选）"并给出四个"什么时候才需要完整版"的判据；
  **修正学校示例措辞**（原写"如九江学院"，改为"示例，请替换成你自己学校的入口"——可分发版本不该绑定某一所学校）。
- **`README.md`**：改为"独立分发的轻量版（手机可用）"，补三种启用方式、合并单文件用法、发布流程（含"必须重新生成合并版"）。

> 校验：`python validate.py` 退出 0。

## v1.1.1（2026-09-17，对应完整版 v1.56）：口径纠错与覆盖补齐

逐条比对完整版项目的权威口径（统计指南的判据章、开题指南与开题模板、数据清洗脚本与统计模块的实际实现）后修正的偏差：

**统计判据（此前把"有条件的、分级的"判据压成了"单一无条件阈值"）**

- **CITC**：原 `<.3` → 改为完整版分级「≥.50 良好、.40–.50 可接受、<.40（严格用 .35）需排查」；`references/faq.md` 同步。
- **KMO**：原文 `KMO（>..7 适合，.6–.7 一般）` 既有**双点笔误**，又把 `.7–.8` **误标为"适合"**（完整版该档是"可接受"，"适合"对应 `>.80`）→ 改为完整版五级「>.90 极佳／>.80 适合／>.70 可接受／>.60 勉强／<.60 不宜」。
- **α**：补全为完整版五档「>.90 优秀／.80–.90 好／.70–.80 可接受／.60–.70 勉强接受（需说明）／<.60 不可接受」。
- **卡方期望频数**：原「两个分类变量用卡方（期望<5 用 Fisher）」把完整版中**仅适用于 2×2** 的规则泛化成了通则 → 改为「2×2 默认 Yates 校正；**仅当**期望<5 的单元格超过 20%、或任一期望<1、或 2×2 且期望<5 时才改 Fisher；更大的表考虑合并稀有类别」。
- **缺失值插补**：原「量表题缺失 <5% 按均分替代」与完整版「该量表缺失 <20% 可均分替代、缺失多则整卷作废」不一致 → 按完整版对齐。
- **开题报告结构**：原写「通常**八节**」却列了 **10 项**，且**漏掉"研究创新点"**（完整版第五节）→ 改为与完整版一一对应的八节。
- **查重率**：原「≤20%–30%」→ 对齐完整版「<10%–30%（以本校文件为准）」。
- **载荷阈值**：`载荷>.4–.5` → 完整版表述「≥.40 可接受、≥.50 较理想」。

**行为规范**

- personalities/concise-direct.md（v1.4 前文件名 strict-ceo.md）：焦虑示例原为「焦虑没用……别空想」，**违反** `references/coaching-protocol.md` 挫折时刻协议第 1 步（"接住情绪、不评价'想太多'"），而该文件自己写着协议优先级更高 → 改为「先正常化接住情绪，再给一个最小任务」。
- **无凭据比较**：删除 4 处无法核实的横向比较（`strict-ceo` ×2、`puppy` ×1、`encouragement-guide` ×1 的"超过我带过的大半学生／赢过很多人／超过大半同级学生"），替换为指向学生**真实做出的具体动作**的肯定——与鼓励指南自己的"禁止无凭据表述"一致。
- **自测覆盖补齐**：新增第五节「情绪与挫折应对」（T27 焦虑 / T28 自我否定 / T29 情绪崩溃）。此前 26 条用例中"挫折时刻"出现 **0 次**，恰好正是上述 strict-ceo 缺陷所在路径的盲区。自测用例 **26 → 29 条**。

> 校验：`python doubao-skill/validate.py` 退出 0。

## v1.1（2026-09-17，对应完整版 v1.55）：协议化、鼓励系统、默认自然风格

- **入口重构**：SKILL.md 增加触发边界（应触发/不应触发/复合请求）、4 项偏好一次性收集、文件加载规则、学生指令速查、运行铁律。
- **默认不套人设**：新增默认"自然专业风格"（personalities/default.md），原三种角色扮演改为**可选人设**（知心姐姐/严格导师/热情陪伴），默认不加载；同步降低人设文本的夸张感。
- **新增引导协议总纲** `references/coaching-protocol.md`：引导循环、P0/P1/P2 问题分级、三闸质量机制、三档难度、进度卡续接、决策协议、幻觉防范、工具安全、紧急模式（7/3/1 天）、心理危机与研究伦理安全、失败降级、产出前门禁。
- **新增鼓励与奖赏式反馈系统** `references/encouragement-guide.md`：标准（默认）/精简/关闭三档可切换；反馈三段式；里程碑奖赏；挫折时刻协议；四条心理学依据（正强化、成长型思维、反馈干预、自我决定理论）；P0 不包装。
- **12 阶段文件全部按统一模板重写**：准入检查、产出物、分步引导（导师动作/学生任务/检查点/鼓励时点）、卡点应对、红线、三闸准出；口径与完整版对齐（五指标清洗、功效样本量、Harman、Bootstrap、质量埋点、未成年人双同意、24 类答辩问题等）。
- **新增** `references/stage-checklist.md`（12 阶段准入/准出速查、跳阶段规则）与 `references/self-test.md`（26 条行为测试用例，维护者用；v1.1.1 起扩至 29 条）。
- **新增** `validate.py` 结构自检；新增 .skillignore。
- 参考资料更新：tools.md 改为 JASP/SPSS 并列并说明完整版工具链边界；faq.md 校正样本量与软件口径；academic-norms.md 的学校规定改为 `[需核实]` 口径。

## v1.0（2026-09-17）

- 初版：SKILL.md + 4 人格（专业导师/霸道总裁/知心姐姐/小奶狗）+ 12 阶段引导 + 4 个参考文件（AI 常识、工具、学术规范、FAQ）。
