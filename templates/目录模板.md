# 〈这一层名〉 · 这一层有什么、该看哪份

> 最后整理：YYYY-MM-DD｜整理人：〈AI 或本人〉
> **写法三条**（这套说明会不会腐，全看这三条）：① 只写现状，不写"这里原来记错了"；
> ② 数字按磁盘实数，别处抄来的数不写（抄一次就漂一次）；③ 被取代的东西移进 `旧版/`，不在原地解释。
> 子层有哪四类、什么能往里放：机器可读名单在 `setup_workspace.py` 的 `SLOTS`，说明文字在
> `core/literature-kb.md` 第七节（以那份名单为准）——本文件不重抄，抄一次就漂一次。

## 一、你要看的，按这个顺序

| 什么时候看 | 文件 | 是什么（一句话） |
|---|---|---|
| **当前版**：〈什么时候要用它〉 | `〈文件名〉` | 〈一句〉 |
| 〈下一场景〉 | `〈文件名〉` | 〈一句〉 |

## 二、不用当"当前版"看的

- `〈文件名〉`（YYYY-MM-DD）：〈为什么不算当前版、还能当什么用〉。
- 机器产物在 `〈子层〉/`：〈要看某条的原始字段时才进这里〉。

## 三、磁盘实数（按今天点的写）

- `〈子层〉/`：N 个文件（构成拆开写）。命名格式 〈pattern〉，编号与〈台账/题录〉对得上。
- **别处口径不一样时，把两边都写下来并说清为什么**：例——某文件写"18 篇已下载"是只数 PDF；
  本层实数 20 件，含 2 个 XML 全文。两处都没错，看到两个数不要以为哪份坏了。
- 缺项：缺哪些、为什么缺、谁去做。（缺号不写原因，等于让人以为数据坏了。）

## 四、这次动了什么（要退回去照这张表做）

| 动作 | 明细 |
|---|---|
| 新建 | |
| 移动 | |
| 删除 / 改名 | **无**（默认一个都不删、不改名） |
| 留给你定的 | |

---

> 本层文件超过一个、又没填第一节"当前版"这一列，`tools/folder_audit.py` 会判红。
> 这张表**不是给人自查的仪式**：它是下一个 AI 唯一的入口。他不读这份就会另起一套结构。
